---
phase: 08-scene-transport
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - AbletonMCP_Remote_Script/handlers/transport.py
  - MCP_Server/connection.py
autonomous: true
requirements: [TRNS-01, TRNS-02, TRNS-03, TRNS-04, TRNS-05, TRNS-06, TRNS-07, TRNS-08, TRNS-09, TRNS-10]

must_haves:
  truths:
    - "continue_playback resumes from current position (not from start marker)"
    - "stop_all_clips stops all playing clips but transport continues playing"
    - "set_tempo validates range 20-999 BPM with current value in error message"
    - "set_time_signature validates numerator 1-32 and denominator is power of 2"
    - "set_loop_region can set enabled, start, and length independently (conditional params)"
    - "get_playback_position returns position in beats only (no playing status)"
    - "get_transport_state returns full composite state (is_playing, tempo, time_sig, position, loop)"
    - "undo checks can_undo before calling and returns informative message if nothing to undo"
    - "redo checks can_redo before calling and returns informative message if nothing to redo"
  artifacts:
    - path: "AbletonMCP_Remote_Script/handlers/transport.py"
      provides: "TransportHandlers mixin extended with 8 new @command handlers"
      contains: "continue_playing"
    - path: "MCP_Server/connection.py"
      provides: "Updated _WRITE_COMMANDS with Phase 8 scene + transport commands"
      contains: "stop_all_clips"
  key_links:
    - from: "AbletonMCP_Remote_Script/handlers/transport.py"
      to: "AbletonMCP_Remote_Script/registry.py"
      via: "@command decorator registration"
      pattern: "@command\\("
    - from: "MCP_Server/connection.py"
      to: "AbletonMCP_Remote_Script/handlers/transport.py"
      via: "_WRITE_COMMANDS frozenset matches handler command names"
      pattern: "_WRITE_COMMANDS"
---

<objective>
Extend TransportHandlers with 8 new command handlers for complete transport control, add validation to set_tempo, and update connection.py _WRITE_COMMANDS for correct timeout routing.

Purpose: Three transport commands exist (start_playback, stop_playback, set_tempo) but lack validation. This plan completes the remaining 8 transport commands and enhances set_tempo with range validation, enabling full session control.

Output: TransportHandlers with 11 total handlers (3 existing + 8 new), updated _WRITE_COMMANDS with all Phase 8 commands.
</objective>

<execution_context>
@C:/Users/ksthu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ksthu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-scene-transport/08-CONTEXT.md
@.planning/phases/08-scene-transport/08-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From AbletonMCP_Remote_Script/handlers/transport.py (current implementation):
```python
from AbletonMCP_Remote_Script.registry import command

class TransportHandlers:
    @command("start_playback", write=True)
    def _start_playback(self, params=None):
        self._song.start_playing()
        return {"playing": self._song.is_playing}

    @command("stop_playback", write=True)
    def _stop_playback(self, params=None):
        self._song.stop_playing()
        return {"playing": self._song.is_playing}

    @command("set_tempo", write=True)
    def _set_tempo(self, params):
        tempo = params.get("tempo", 120.0)
        self._song.tempo = tempo
        return {"tempo": self._song.tempo}
```

From MCP_Server/connection.py:
```python
_WRITE_COMMANDS = frozenset([
    # ... 30 existing entries ...
    "delete_device",  # last entry (Phase 7)
])
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend TransportHandlers with 8 new handlers and enhance set_tempo validation</name>
  <files>AbletonMCP_Remote_Script/handlers/transport.py</files>
  <action>
Edit handlers/transport.py. Keep the 3 existing handlers intact. Enhance set_tempo with validation, then add 8 new handlers.

1. **Enhance existing `_set_tempo`** -- add range validation BEFORE setting:
   - Validate `20.0 <= tempo <= 999.0`. If out of range, get current tempo via `self._song.tempo` and raise ValueError: `f"Tempo {tempo} out of range (20-999 BPM). Current value: {current}"`
   - Follow the Phase 4 validation-with-current-value pattern exactly.
   - Keep existing return format: `{"tempo": self._song.tempo}`

2. `_continue_playback` -- `@command("continue_playback", write=True)`
   - Call `self._song.continue_playing()` (resumes from current_song_time, unlike start_playing which goes to start marker)
   - Return `{"playing": self._song.is_playing}`

3. `_stop_all_clips` -- `@command("stop_all_clips", write=True)`
   - Call `self._song.stop_all_clips()` -- this stops all playing clips but Song continues playing (Ableton native behavior)
   - Return `{"stopped": True, "transport_playing": self._song.is_playing}`
   - Claude's discretion: match Ableton native behavior (clips only, not transport)

4. `_set_time_signature` -- `@command("set_time_signature", write=True)`
   - Extract numerator and denominator from params
   - Validate numerator: 1-32 range. If invalid, raise ValueError with current value.
   - Validate denominator: must be in {1, 2, 4, 8, 16} (power of 2). If invalid, raise ValueError listing valid values.
   - Set `self._song.signature_numerator = numerator` and `self._song.signature_denominator = denominator`
   - Return `{"numerator": self._song.signature_numerator, "denominator": self._song.signature_denominator}`

5. `_set_loop_region` -- `@command("set_loop_region", write=True)`
   - Use conditional param pattern (same as set_clip_loop from Phase 5):
   - If "enabled" in params: set `self._song.loop = params["enabled"]`
   - If "start" in params: set `self._song.loop_start = params["start"]` (beats float)
   - If "length" in params: set `self._song.loop_length = params["length"]` (beats float)
   - Return `{"loop_enabled": self._song.loop, "loop_start": self._song.loop_start, "loop_length": self._song.loop_length}`

6. `_get_playback_position` -- `@command("get_playback_position")` (READ command, no write=True)
   - Per user decision: returns position only, not playing status or tempo (lightweight check)
   - Return `{"position": self._song.current_song_time}` (beats float)

7. `_get_transport_state` -- `@command("get_transport_state")` (READ command, no write=True)
   - Per user decision: returns full composite state
   - Return dict with: is_playing, tempo, time_signature (nested dict with numerator/denominator), position, loop_enabled, loop_start, loop_length

8. `_undo` -- `@command("undo", write=True)`
   - Check `self._song.can_undo`. If False, return `{"undone": False, "message": "Nothing to undo"}`
   - Call `self._song.undo()`
   - Return `{"undone": True}`

9. `_redo` -- `@command("redo", write=True)`
   - Check `self._song.can_redo`. If False, return `{"undone": False, "message": "Nothing to redo"}`
   - Call `self._song.redo()`
   - Return `{"undone": True}`

All handlers follow the established pattern: try/except with self.log_message on error, then re-raise.

Do NOT add a "panic" tool (Claude's discretion: users can call stop_all_clips + stop_playback separately).
  </action>
  <verify>
    <automated>cd I:/ableton-mcp && python -c "import sys; sys.modules['_Framework'] = type(sys)('_Framework'); sys.modules['_Framework.ControlSurface'] = type(sys)('_Framework.ControlSurface'); sys.modules['_Framework.ControlSurface'].ControlSurface = type('ControlSurface', (), {}); from AbletonMCP_Remote_Script.handlers.transport import TransportHandlers; methods = [m for m in dir(TransportHandlers) if not m.startswith('__')]; print(f'Methods ({len(methods)}): {methods}'); assert len(methods) == 11, f'Expected 11, got {len(methods)}'"</automated>
  </verify>
  <done>TransportHandlers has 11 total methods (3 existing + 8 new). set_tempo has range validation with current value in error. continue_playback uses continue_playing (not start_playing). get_transport_state returns full composite state. get_playback_position returns position only. Undo/redo check can_undo/can_redo before calling.</done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 8 commands to _WRITE_COMMANDS in connection.py</name>
  <files>MCP_Server/connection.py</files>
  <action>
In MCP_Server/connection.py, add new commands to the _WRITE_COMMANDS frozenset with a "# Phase 8: Scene and Transport" comment.

Add these write commands:
- "create_scene"
- "set_scene_name"
- "fire_scene"
- "delete_scene"
- "continue_playback"
- "stop_all_clips"
- "set_time_signature"
- "set_loop_region"
- "undo"
- "redo"

Do NOT add read commands: "get_playback_position" and "get_transport_state" are READ commands and should NOT be in _WRITE_COMMANDS (they use the default 10s read timeout).

Note: "start_playback", "stop_playback", and "set_tempo" are already in _WRITE_COMMANDS -- do not duplicate them.
  </action>
  <verify>
    <automated>cd I:/ableton-mcp && python -c "from MCP_Server.connection import _WRITE_COMMANDS; phase8 = ['create_scene', 'set_scene_name', 'fire_scene', 'delete_scene', 'continue_playback', 'stop_all_clips', 'set_time_signature', 'set_loop_region', 'undo', 'redo']; missing = [c for c in phase8 if c not in _WRITE_COMMANDS]; reads = ['get_playback_position', 'get_transport_state']; wrongly_added = [c for c in reads if c in _WRITE_COMMANDS]; print(f'Missing: {missing}'); print(f'Wrongly added reads: {wrongly_added}'); assert not missing, f'Missing: {missing}'; assert not wrongly_added, f'Read commands wrongly in WRITE: {wrongly_added}'"</automated>
  </verify>
  <done>_WRITE_COMMANDS contains all 10 new Phase 8 write commands. get_playback_position and get_transport_state are NOT in _WRITE_COMMANDS (they are read commands with 10s timeout).</done>
</task>

</tasks>

<verification>
- TransportHandlers has exactly 11 methods (3 existing + 8 new)
- set_tempo has range validation (20-999 BPM) with current value in error message
- continue_playback calls self._song.continue_playing() (not start_playing)
- stop_all_clips calls self._song.stop_all_clips() (clips only, transport keeps playing)
- get_playback_position returns position only (not full state)
- get_transport_state returns full composite state with 7 fields
- undo/redo check can_undo/can_redo before calling
- _WRITE_COMMANDS has 10 new Phase 8 entries
- get_playback_position and get_transport_state NOT in _WRITE_COMMANDS
</verification>

<success_criteria>
- TransportHandlers mixin has exactly 11 @command handlers
- set_tempo validates 20-999 BPM and includes current value in error
- set_time_signature validates numerator 1-32 and denominator in {1,2,4,8,16}
- set_loop_region uses conditional param pattern (only set provided params)
- Undo/redo guard with can_undo/can_redo check
- _WRITE_COMMANDS includes all 10 new Phase 8 write commands
- Read commands not in _WRITE_COMMANDS
</success_criteria>

<output>
After completion, create `.planning/phases/08-scene-transport/08-02-SUMMARY.md`
</output>
