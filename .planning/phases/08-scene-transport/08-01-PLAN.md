---
phase: 08-scene-transport
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AbletonMCP_Remote_Script/handlers/scenes.py
autonomous: true
requirements: [SCNE-01, SCNE-02, SCNE-03, SCNE-04]

must_haves:
  truths:
    - "create_scene creates a scene at the given index and optionally names it"
    - "set_scene_name renames any scene by index"
    - "fire_scene launches all clip slots in a scene"
    - "delete_scene removes a scene by index, refusing to delete the last scene"
  artifacts:
    - path: "AbletonMCP_Remote_Script/handlers/scenes.py"
      provides: "SceneHandlers mixin with 4 @command handlers"
      contains: "create_scene"
  key_links:
    - from: "AbletonMCP_Remote_Script/handlers/scenes.py"
      to: "AbletonMCP_Remote_Script/registry.py"
      via: "@command decorator registration"
      pattern: "@command\\("
    - from: "AbletonMCP_Remote_Script/handlers/scenes.py"
      to: "self._song.scenes"
      via: "Ableton Song API for scene access"
      pattern: "self\\._song"
---

<objective>
Implement the SceneHandlers mixin with 4 command handlers for complete scene management in the Remote Script.

Purpose: The SceneHandlers placeholder is currently empty. This plan fills it with create, name, fire, and delete handlers using the Ableton Live Song/Scene API, following the exact patterns established by ClipHandlers and MixerHandlers.

Output: SceneHandlers mixin with 4 registered @command handlers in handlers/scenes.py.
</objective>

<execution_context>
@C:/Users/ksthu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ksthu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-scene-transport/08-CONTEXT.md
@.planning/phases/08-scene-transport/08-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From AbletonMCP_Remote_Script/registry.py:
```python
command = CommandRegistry.command
# Usage: @command("name") for read, @command("name", write=True) for write
```

From AbletonMCP_Remote_Script/handlers/scenes.py (current placeholder):
```python
class SceneHandlers:
    """Mixin class for scene command handlers."""
    pass
```

From AbletonMCP_Remote_Script/__init__.py:
```python
# SceneHandlers already in MRO -- class body just needs methods
class AbletonMCP(
    ..., SceneHandlers, ..., ControlSurface
):
```

From tests/test_registry.py:
```python
# Currently expects 48 registered commands total
assert len(registered) == 48
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement SceneHandlers mixin with 4 @command handlers</name>
  <files>AbletonMCP_Remote_Script/handlers/scenes.py</files>
  <action>
Rewrite handlers/scenes.py completely. Import command from registry.

Implement 4 handlers in SceneHandlers mixin class:

1. `_create_scene` -- `@command("create_scene", write=True)`
   - Extract index (default -1, meaning append at end) and optional name from params
   - Call `self._song.create_scene(index)` which returns a Scene object
   - Compute actual_index: if index == -1, use `len(self._song.scenes) - 1`, else use index
   - If name param provided, set `scene.name = name`
   - Return structured JSON: `{"index": actual_index, "name": scene.name}`
   - Per user decision: returns structured JSON response consistent with track/clip creation patterns

2. `_set_scene_name` -- `@command("set_scene_name", write=True)`
   - Extract scene_index and name from params
   - Validate scene_index in range `0` to `len(self._song.scenes) - 1`, raise IndexError with range info if invalid
   - Set `self._song.scenes[scene_index].name = name`
   - Return `{"index": scene_index, "name": name}`

3. `_fire_scene` -- `@command("fire_scene", write=True)`
   - Extract scene_index from params
   - Validate scene_index in range, raise IndexError with range info if invalid
   - Call `self._song.scenes[scene_index].fire()` -- fire immediately, do NOT expose quantization param (Claude's discretion: simplicity wins, defaults are sensible)
   - Return `{"fired": True, "index": scene_index, "name": scene.name}`

4. `_delete_scene` -- `@command("delete_scene", write=True)`
   - Extract scene_index from params
   - Validate scene_index in range, raise IndexError if invalid
   - Check `len(self._song.scenes) > 1` before delete -- raise ValueError "Cannot delete the last scene" if only 1 scene exists
   - Store scene name before deletion for response
   - Call `self._song.delete_scene(scene_index)`
   - Return `{"deleted": True, "index": scene_index, "name": deleted_name}`
   - Claude's discretion: if scene is playing, let Ableton handle it naturally (API will either succeed or raise)

All handlers follow the established pattern: try/except with self.log_message on error, then re-raise.
  </action>
  <verify>
    <automated>cd I:/ableton-mcp && python -c "import sys; sys.modules['_Framework'] = type(sys)('_Framework'); sys.modules['_Framework.ControlSurface'] = type(sys)('_Framework.ControlSurface'); sys.modules['_Framework.ControlSurface'].ControlSurface = type('ControlSurface', (), {}); from AbletonMCP_Remote_Script.handlers.scenes import SceneHandlers; methods = [m for m in dir(SceneHandlers) if not m.startswith('__')]; print(f'Methods: {methods}'); assert len(methods) == 4, f'Expected 4, got {len(methods)}'"</automated>
  </verify>
  <done>SceneHandlers mixin has 4 @command-decorated methods: _create_scene, _set_scene_name, _fire_scene, _delete_scene. All use self._song.scenes or self._song.create_scene/delete_scene API. Module imports cleanly with stubbed _Framework module.</done>
</task>

<task type="auto">
  <name>Task 2: Update registry test to include 4 new scene commands</name>
  <files>tests/test_registry.py</files>
  <action>
In tests/test_registry.py, update test_all_commands_registered:
1. Change expected count from 48 to 52 (adding create_scene, set_scene_name, fire_scene, delete_scene)
2. Add 4 new command names to the expected set with a "# Phase 8: Scene Management" comment:
   - "create_scene"
   - "set_scene_name"
   - "fire_scene"
   - "delete_scene"
  </action>
  <verify>
    <automated>cd I:/ableton-mcp && python -m pytest tests/test_registry.py -x -q</automated>
  </verify>
  <done>Registry test expects 52 commands and includes all 4 scene command strings. Test passes green.</done>
</task>

</tasks>

<verification>
- All 4 scene commands registered in CommandRegistry._entries
- SceneHandlers has exactly 4 public methods
- Registry test passes with 52 expected commands
- handlers/scenes.py uses self._song.create_scene, self._song.delete_scene, scene.fire(), scene.name APIs
</verification>

<success_criteria>
- SceneHandlers mixin has exactly 4 @command handlers
- create_scene returns structured JSON with index and name
- create_scene supports optional name parameter (auto-names if not provided)
- delete_scene refuses to delete the last scene
- fire_scene fires immediately without quantization parameter
- pytest tests/test_registry.py -x passes
</success_criteria>

<output>
After completion, create `.planning/phases/08-scene-transport/08-01-SUMMARY.md`
</output>
