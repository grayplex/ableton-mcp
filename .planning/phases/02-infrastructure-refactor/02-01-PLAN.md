---
phase: 02-infrastructure-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AbletonMCP_Remote_Script/registry.py
  - AbletonMCP_Remote_Script/handlers/__init__.py
  - AbletonMCP_Remote_Script/handlers/base.py
  - AbletonMCP_Remote_Script/handlers/transport.py
  - AbletonMCP_Remote_Script/handlers/tracks.py
  - AbletonMCP_Remote_Script/handlers/clips.py
  - AbletonMCP_Remote_Script/handlers/notes.py
  - AbletonMCP_Remote_Script/handlers/devices.py
  - AbletonMCP_Remote_Script/handlers/mixer.py
  - AbletonMCP_Remote_Script/handlers/scenes.py
  - AbletonMCP_Remote_Script/handlers/browser.py
  - AbletonMCP_Remote_Script/__init__.py
autonomous: true
requirements: [FNDN-08]

must_haves:
  truths:
    - "Each domain handler module exists as a separate file with @command-decorated methods"
    - "CommandRegistry.build_tables() produces read_commands and write_commands dicts matching Phase 1 dispatch tables"
    - "Self-scheduling commands (load_browser_item, load_instrument_or_effect) are flagged in the registry"
    - "AbletonMCP.__init__.py is slim (server + client handling + imports) with no handler business logic"
  artifacts:
    - path: "AbletonMCP_Remote_Script/registry.py"
      provides: "CommandRegistry class with @command decorator"
      exports: ["CommandRegistry", "command"]
    - path: "AbletonMCP_Remote_Script/handlers/__init__.py"
      provides: "Import manifest for all handler modules"
    - path: "AbletonMCP_Remote_Script/handlers/base.py"
      provides: "_ping, _get_session_info handlers"
    - path: "AbletonMCP_Remote_Script/handlers/transport.py"
      provides: "_start_playback, _stop_playback, _set_tempo handlers"
    - path: "AbletonMCP_Remote_Script/handlers/tracks.py"
      provides: "_create_midi_track, _set_track_name, _get_track_info handlers"
    - path: "AbletonMCP_Remote_Script/handlers/clips.py"
      provides: "_create_clip, _set_clip_name, _fire_clip, _stop_clip handlers"
    - path: "AbletonMCP_Remote_Script/handlers/notes.py"
      provides: "_add_notes_to_clip handler"
    - path: "AbletonMCP_Remote_Script/handlers/devices.py"
      provides: "_set_device_parameter, _get_device_type handlers"
    - path: "AbletonMCP_Remote_Script/handlers/browser.py"
      provides: "_load_browser_item, _load_instrument_or_effect, get_browser_tree, get_browser_items_at_path and related handlers"
    - path: "AbletonMCP_Remote_Script/handlers/mixer.py"
      provides: "Empty placeholder for Phase 4"
    - path: "AbletonMCP_Remote_Script/handlers/scenes.py"
      provides: "Empty placeholder for Phase 8"
    - path: "AbletonMCP_Remote_Script/__init__.py"
      provides: "Slim AbletonMCP class: init, server, client handling, registry integration"
  key_links:
    - from: "AbletonMCP_Remote_Script/handlers/*.py"
      to: "AbletonMCP_Remote_Script/registry.py"
      via: "@command decorator import"
      pattern: "from AbletonMCP_Remote_Script\\.registry import command"
    - from: "AbletonMCP_Remote_Script/__init__.py"
      to: "AbletonMCP_Remote_Script/registry.py"
      via: "CommandRegistry.build_tables(self) call in __init__"
      pattern: "CommandRegistry\\.build_tables"
    - from: "AbletonMCP_Remote_Script/handlers/__init__.py"
      to: "AbletonMCP_Remote_Script/handlers/*.py"
      via: "Explicit imports triggering decorator registration"
      pattern: "from \\. import base, transport"
---

<objective>
Extract all Remote Script command handlers from the monolithic AbletonMCP_Remote_Script/__init__.py (1229 lines) into a handlers/ package with domain-organized modules, using a @command('name') decorator registry pattern that evolves the Phase 1 dict-based dispatch.

Purpose: Make adding a new Ableton command a one-file-touch operation. The registry auto-collects decorated handlers so _build_command_table() is replaced by CommandRegistry.build_tables().
Output: handlers/ package with 9 domain modules + registry.py + slim __init__.py
</objective>

<execution_context>
@C:/Users/ksthu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ksthu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-infrastructure-refactor/02-CONTEXT.md
@.planning/phases/02-infrastructure-refactor/02-RESEARCH.md

@AbletonMCP_Remote_Script/__init__.py

<interfaces>
<!-- Current Remote Script dispatch tables (from __init__.py lines 99-125) -->

Read commands (socket thread):
- get_session_info -> self._get_session_info
- get_track_info -> self._get_track_info
- get_browser_tree -> self.get_browser_tree
- get_browser_items_at_path -> self.get_browser_items_at_path
- get_browser_item -> self._get_browser_item
- get_browser_categories -> self._get_browser_categories
- get_browser_items -> self._get_browser_items
- ping -> self._ping

Write commands (main thread):
- create_midi_track -> self._create_midi_track
- set_track_name -> self._set_track_name
- create_clip -> self._create_clip
- add_notes_to_clip -> self._add_notes_to_clip
- set_clip_name -> self._set_clip_name
- set_tempo -> self._set_tempo
- fire_clip -> self._fire_clip
- stop_clip -> self._stop_clip
- start_playback -> self._start_playback
- stop_playback -> self._stop_playback
- load_browser_item -> self._load_browser_item (self_scheduling=True)
- set_device_parameter -> self._set_device_parameter
- load_instrument_or_effect -> self._load_instrument_or_effect (self_scheduling=True)

Self-scheduling commands (manage own schedule_message lifecycle):
- load_browser_item
- load_instrument_or_effect
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CommandRegistry and extract handlers into domain modules</name>
  <files>
    AbletonMCP_Remote_Script/registry.py,
    AbletonMCP_Remote_Script/handlers/__init__.py,
    AbletonMCP_Remote_Script/handlers/base.py,
    AbletonMCP_Remote_Script/handlers/transport.py,
    AbletonMCP_Remote_Script/handlers/tracks.py,
    AbletonMCP_Remote_Script/handlers/clips.py,
    AbletonMCP_Remote_Script/handlers/notes.py,
    AbletonMCP_Remote_Script/handlers/devices.py,
    AbletonMCP_Remote_Script/handlers/mixer.py,
    AbletonMCP_Remote_Script/handlers/scenes.py,
    AbletonMCP_Remote_Script/handlers/browser.py
  </files>
  <action>
    1. Create `AbletonMCP_Remote_Script/registry.py` with the CommandRegistry class:
       - Class attribute `_entries: list[tuple[str, str, bool, bool]]` (cmd_name, method_name, is_write, is_self_scheduling)
       - `@classmethod command(cls, name, *, write=False, self_scheduling=False)` decorator that appends to _entries
       - `@classmethod build_tables(cls, instance)` that returns (read_commands_dict, write_commands_dict, self_scheduling_set) by binding handlers to instance via getattr
       - Module-level alias: `command = CommandRegistry.command`
       - Follow the exact pattern from 02-RESEARCH.md "Complete CommandRegistry Implementation"

    2. Create `AbletonMCP_Remote_Script/handlers/__init__.py`:
       - Import all handler modules: `from . import base, transport, tracks, clips, notes, devices, mixer, scenes, browser`
       - This triggers @command decorator registration at import time

    3. Extract handlers from __init__.py into domain modules. Each module:
       - Imports: `from AbletonMCP_Remote_Script.registry import command`
       - Contains handler methods as standalone functions that will be methods on AbletonMCP (they use `self` parameter -- see note below)
       - Uses @command decorator with correct name, write flag, and self_scheduling flag

       IMPORTANT CONSTRAINT: Handler methods need `self` (the AbletonMCP instance) to access self._song, self.application(), self.log_message(), self.schedule_message(), etc. Use a **mixin class** pattern per module: each module defines a class (e.g., TransportHandlers) with the handler methods. AbletonMCP will inherit from all mixin classes. The @command decorator goes on the methods.

       Domain module contents (extract from __init__.py):

       **handlers/base.py** - BaseHandlers mixin:
       - @command("ping"): _ping (read)
       - @command("get_session_info"): _get_session_info (read)

       **handlers/transport.py** - TransportHandlers mixin:
       - @command("start_playback", write=True): _start_playback
       - @command("stop_playback", write=True): _stop_playback
       - @command("set_tempo", write=True): _set_tempo

       **handlers/tracks.py** - TrackHandlers mixin:
       - @command("create_midi_track", write=True): _create_midi_track
       - @command("set_track_name", write=True): _set_track_name
       - @command("get_track_info"): _get_track_info (read)

       **handlers/clips.py** - ClipHandlers mixin:
       - @command("create_clip", write=True): _create_clip
       - @command("set_clip_name", write=True): _set_clip_name
       - @command("fire_clip", write=True): _fire_clip
       - @command("stop_clip", write=True): _stop_clip

       **handlers/notes.py** - NoteHandlers mixin:
       - @command("add_notes_to_clip", write=True): _add_notes_to_clip

       **handlers/devices.py** - DeviceHandlers mixin:
       - @command("set_device_parameter", write=True): _set_device_parameter
       - Any _get_device_type helper if it exists

       **handlers/mixer.py** - MixerHandlers mixin:
       - Empty placeholder class with docstring: "Mixer handlers -- populated in Phase 4 (MIX-01..08)"

       **handlers/scenes.py** - SceneHandlers mixin:
       - Empty placeholder class with docstring: "Scene handlers -- populated in Phase 8 (SCNE-01..04)"

       **handlers/browser.py** - BrowserHandlers mixin:
       - @command("load_browser_item", write=True, self_scheduling=True): _load_browser_item
       - @command("load_instrument_or_effect", write=True, self_scheduling=True): _load_instrument_or_effect
       - @command("get_browser_tree"): get_browser_tree (read)
       - @command("get_browser_items_at_path"): get_browser_items_at_path (read)
       - @command("get_browser_item"): _get_browser_item (read)
       - @command("get_browser_categories"): _get_browser_categories (read)
       - @command("get_browser_items"): _get_browser_items (read)
       - Include all browser helper methods: _find_browser_item_by_uri, _CATEGORY_MAP, _browser_path_cache, _load_and_verify, _verify_load, etc.
       - The _browser_path_cache should be an instance variable (self._browser_path_cache) -- it's already set in __init__

    4. If relative imports (`from .registry import command`) fail in Ableton's runtime, the fallback is absolute imports (`from AbletonMCP_Remote_Script.registry import command`). Start with relative imports (standard Python). Add a comment in handlers/__init__.py noting the fallback.
  </action>
  <verify>
    <automated>cd I:/ableton-mcp && python -c "from AbletonMCP_Remote_Script.registry import CommandRegistry, command; print('Registry imports OK')" && python -c "from AbletonMCP_Remote_Script.handlers import base, transport, tracks, clips, notes, devices, mixer, scenes, browser; print('All handler modules import OK')" && python -c "from AbletonMCP_Remote_Script.registry import CommandRegistry; entries = CommandRegistry._entries; names = [e[0] for e in entries]; assert len(names) == 21, f'Expected 21 commands, got {len(names)}: {names}'; print(f'Registry has {len(names)} commands registered')"</automated>
  </verify>
  <done>
    - registry.py exists with CommandRegistry class and @command decorator
    - All 9 handler modules exist in handlers/ package
    - All 21 commands (8 read + 13 write) are registered via @command decorators
    - Handler modules import cleanly without Ableton runtime (no _Framework imports at module level in handler files -- only used at runtime via self)
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite __init__.py to slim orchestrator using registry and handler mixins</name>
  <files>
    AbletonMCP_Remote_Script/__init__.py
  </files>
  <action>
    Rewrite AbletonMCP_Remote_Script/__init__.py to be a slim orchestrator (~150-200 lines) that:

    1. Keeps these imports:
       - `from _Framework.ControlSurface import ControlSurface` (Ableton runtime)
       - `from .registry import CommandRegistry` (registry)
       - `from .handlers import *` (or explicit mixin imports -- this triggers decorator registration)

    2. AbletonMCP class inherits from ControlSurface AND all handler mixin classes:
       ```python
       from .handlers.base import BaseHandlers
       from .handlers.transport import TransportHandlers
       from .handlers.tracks import TrackHandlers
       from .handlers.clips import ClipHandlers
       from .handlers.notes import NoteHandlers
       from .handlers.devices import DeviceHandlers
       from .handlers.mixer import MixerHandlers
       from .handlers.scenes import SceneHandlers
       from .handlers.browser import BrowserHandlers

       class AbletonMCP(
           BaseHandlers,
           TransportHandlers,
           TrackHandlers,
           ClipHandlers,
           NoteHandlers,
           DeviceHandlers,
           MixerHandlers,
           SceneHandlers,
           BrowserHandlers,
           ControlSurface,
       ):
       ```
       NOTE: ControlSurface must be LAST in MRO so mixin methods take precedence.

    3. __init__ method:
       - Keep: super().__init__(c_instance), self._song, server setup, threading, DEFAULT_PORT
       - Replace: `self._build_command_table()` with `self._read_commands, self._write_commands, self._self_scheduling = CommandRegistry.build_tables(self)`
       - Keep: self._browser_path_cache = {} initialization
       - Keep: self.show_message() call

    4. Keep these methods in __init__.py (they are infrastructure, not domain handlers):
       - __init__
       - disconnect
       - _start_server
       - _handle_client (the socket protocol loop)
       - _process_command (the dispatcher -- now uses self._read_commands and self._write_commands from registry)
       - _dispatch_write_command (main thread scheduling wrapper -- check self._self_scheduling set for self-scheduling commands)
       - send_message / recv_message / _recv_exact (protocol functions -- keep inline OR import from a protocol.py if one exists)

    5. Remove ALL handler method bodies from __init__.py -- they now live in handlers/ modules.

    6. Keep create_instance(c_instance) function at module level.

    7. Ensure _dispatch_write_command checks self._self_scheduling set:
       ```python
       if command_type in self._self_scheduling:
           # Self-scheduling commands manage their own schedule_message lifecycle
           handler(params)
       else:
           # Standard write commands: wrap in schedule_message
           ...existing pattern...
       ```
       This replaces the old hardcoded `_SELF_SCHEDULING_COMMANDS` set or inline checks.

    8. Verify the protocol functions (send_message, recv_message, _recv_exact) are kept in __init__.py or moved to a shared protocol.py. Since the MCP Server already has its own protocol functions in server.py, and the Remote Script protocol functions are identical, keep them in __init__.py for now -- Phase 2 scope is splitting handlers, not deduplicating protocol code across the two codebases (they run in different Python runtimes).
  </action>
  <verify>
    <automated>cd I:/ableton-mcp && python -c "
import ast, sys
source = open('AbletonMCP_Remote_Script/__init__.py').read()
tree = ast.parse(source)
lines = len(source.splitlines())
# Should be slim: under 300 lines
assert lines < 300, f'__init__.py is {lines} lines, expected < 300'
# Should have AbletonMCP class
classes = [n.name for n in ast.walk(tree) if isinstance(n, ast.ClassDef)]
assert 'AbletonMCP' in classes, f'AbletonMCP class not found, got {classes}'
# Should have create_instance function
funcs = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)]
assert 'create_instance' in funcs, f'create_instance not found'
# Should NOT have handler methods (they moved to handlers/)
handler_methods = [n for n in funcs if n.startswith('_get_session') or n.startswith('_create_midi') or n.startswith('_add_notes') or n.startswith('_start_playback')]
assert len(handler_methods) == 0, f'Handler methods still in __init__.py: {handler_methods}'
print(f'__init__.py is {lines} lines, slim and clean')
"</automated>
  </verify>
  <done>
    - __init__.py is under 300 lines (down from 1229)
    - AbletonMCP class inherits from all handler mixins + ControlSurface
    - _build_command_table() is replaced by CommandRegistry.build_tables(self)
    - All handler method bodies removed from __init__.py
    - _process_command and _dispatch_write_command still work with registry-built dicts
    - create_instance() factory function preserved
    - Protocol functions preserved in __init__.py
  </done>
</task>

</tasks>

<verification>
1. All 21 commands registered: `python -c "from AbletonMCP_Remote_Script.registry import CommandRegistry; print(len(CommandRegistry._entries))"`
2. __init__.py is slim: `wc -l AbletonMCP_Remote_Script/__init__.py` should show < 300
3. No handler bodies in __init__.py: grep for specific handler signatures should return empty
4. All handler modules importable: `python -c "import AbletonMCP_Remote_Script.handlers"`
</verification>

<success_criteria>
- Remote Script has handlers/ package with 9 domain modules
- CommandRegistry with @command decorator replaces _build_command_table()
- __init__.py is a slim orchestrator (< 300 lines)
- All 21 existing commands are registered and dispatch correctly
- Self-scheduling commands properly flagged in registry
- Adding a new command in future phases requires only: (1) add @command-decorated method to handler module
</success_criteria>

<output>
After completion, create `.planning/phases/02-infrastructure-refactor/02-01-SUMMARY.md`
</output>
