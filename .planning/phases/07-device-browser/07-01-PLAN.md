---
phase: 07-device-browser
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AbletonMCP_Remote_Script/handlers/devices.py
autonomous: true
requirements:
  - DEV-03
  - DEV-04
  - DEV-07

must_haves:
  truths:
    - "get_device_parameters returns all parameters with name, value, min, max, is_quantized for any device on any track type"
    - "set_device_parameter accepts parameter_name (case-insensitive) or parameter_index, clamps out-of-range values, works on all track types"
    - "delete_device removes a device from any track's device chain and returns the updated chain"
    - "get_rack_chains returns chain names, indices, and device lists for Instrument/Effect Racks, and pad details for Drum Racks"
  artifacts:
    - path: "AbletonMCP_Remote_Script/handlers/devices.py"
      provides: "get_device_parameters, enhanced set_device_parameter, delete_device, get_rack_chains handlers"
      contains: "def _get_device_parameters"
  key_links:
    - from: "AbletonMCP_Remote_Script/handlers/devices.py"
      to: "AbletonMCP_Remote_Script/handlers/tracks.py"
      via: "_resolve_track import"
      pattern: "from AbletonMCP_Remote_Script.handlers.tracks import _resolve_track"
---

<objective>
Implement four Remote Script device command handlers: get_device_parameters, enhanced set_device_parameter (name + index + clamping + track_type + chain support), delete_device, and get_rack_chains (with Drum Rack pad navigation).

Purpose: These handlers provide the Ableton-side implementation for device parameter control and rack chain navigation. They are the foundation for the MCP tools built in Plan 03.

Output: Extended `AbletonMCP_Remote_Script/handlers/devices.py` with 4 registered commands.
</objective>

<execution_context>
@C:/Users/ksthu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ksthu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-device-browser/07-CONTEXT.md
@.planning/phases/07-device-browser/07-RESEARCH.md

@AbletonMCP_Remote_Script/handlers/devices.py
@AbletonMCP_Remote_Script/handlers/tracks.py
@AbletonMCP_Remote_Script/registry.py

<interfaces>
<!-- Key types and contracts the executor needs. -->

From AbletonMCP_Remote_Script/registry.py:
```python
command = CommandRegistry.command
# Usage: @command("name") for read, @command("name", write=True) for write
```

From AbletonMCP_Remote_Script/handlers/tracks.py:
```python
def _resolve_track(song, track_type, track_index):
    """Resolve a track reference from type and index.
    track_type: "track", "return", or "master"
    Returns track object. Raises ValueError/IndexError.
    """
```

From AbletonMCP_Remote_Script/handlers/devices.py (existing):
```python
class DeviceHandlers:
    @command("set_device_parameter", write=True)
    def _set_device_parameter(self, params): ...

    def _get_device_type(self, device):
        """Returns: 'drum_machine', 'rack', 'instrument', 'audio_effect', 'midi_effect', or 'unknown'"""
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement get_device_parameters and enhanced set_device_parameter</name>
  <files>AbletonMCP_Remote_Script/handlers/devices.py</files>
  <action>
Rewrite `AbletonMCP_Remote_Script/handlers/devices.py` with the following changes:

1. Add import at top: `from AbletonMCP_Remote_Script.handlers.tracks import _resolve_track`

2. Add helper function `_resolve_device(self, params)` that:
   - Extracts track_index, device_index, track_type, chain_index, chain_device_index from params
   - Uses `_resolve_track(self._song, track_type, track_index)` to get track
   - Validates device_index against `len(track.devices)`
   - Gets device at `track.devices[device_index]`
   - If `chain_index` is not None: validates device.can_have_chains, validates chain_index, gets chain
   - If `chain_device_index` is not None: validates against chain.devices length, returns chain.devices[chain_device_index]
   - If only chain_index (no chain_device_index): this is for get_rack_chains-like operations, return the chain itself
   - Returns (device, track) tuple for the resolved device

3. Add `@command("get_device_parameters")` handler:
   - Params: track_index, device_index, track_type (default "track"), chain_index (optional), chain_device_index (optional)
   - Uses _resolve_track then navigates to device (same pattern as _resolve_device)
   - If chain_index provided, navigates into rack chain; if chain_device_index also provided, navigates to device in chain
   - Iterates `device.parameters`, building list of dicts with: index, name, value, min, max, is_quantized
   - Returns: device_name, device_type (via self._get_device_type), class_name, parameter_count, parameters list

4. Rewrite `@command("set_device_parameter", write=True)` handler:
   - Params: track_index, device_index, track_type (default "track"), value, parameter_name (optional), parameter_index (optional), chain_index (optional), chain_device_index (optional)
   - Uses _resolve_track, then navigates to device (including chain navigation if chain_index provided)
   - Parameter resolution: if parameter_name provided, do case-insensitive first-match lookup; if parameter_index provided, use directly; if neither, raise ValueError("Provide parameter_name or parameter_index")
   - Value clamping: if value < param.min, clamp to min and set warning; if value > param.max, clamp to max and set warning
   - Set param.value, then read back actual value
   - Returns: device_name, parameter_name, value (actual), min, max, is_quantized. Add "warning" key if clamped.
   - On name lookup failure, include available parameter names list in error message

Per user decision (Claude's Discretion): Use first-match strategy for parameter name ambiguity. Include matched parameter's index in the response so the AI can switch to index-based addressing if needed.
  </action>
  <verify>
    <automated>cd I:/ableton-mcp && python -c "from AbletonMCP_Remote_Script.handlers.devices import DeviceHandlers; print('OK')"</automated>
  </verify>
  <done>get_device_parameters and enhanced set_device_parameter are registered commands. set_device_parameter supports name/index lookup, clamping, track_type, and chain device addressing.</done>
</task>

<task type="auto">
  <name>Task 2: Implement delete_device and get_rack_chains handlers</name>
  <files>AbletonMCP_Remote_Script/handlers/devices.py</files>
  <action>
Add two more command handlers to the DeviceHandlers class in `AbletonMCP_Remote_Script/handlers/devices.py`:

1. Add `@command("delete_device", write=True)` handler:
   - Params: track_index, device_index, track_type (default "track"), chain_index (optional), chain_device_index (optional)
   - Uses _resolve_track to get track
   - Validates device_index
   - If chain_index provided: navigate into rack chain, validate chain_device_index, call `chain.delete_device(chain_device_index)`
   - If no chain_index: call `track.delete_device(device_index)` to remove from top-level
   - After deletion, build updated device chain list: [{"index": i, "name": d.name, "type": self._get_device_type(d)} for i, d in enumerate(track.devices)] (or chain.devices if deleting from chain)
   - Returns: deleted_device_name, track_name, track_type, updated_devices list

2. Add `@command("get_rack_chains")` handler:
   - Params: track_index, device_index, track_type (default "track"), chain_index (optional), chain_device_index (optional) -- chain_index/chain_device_index allow navigating to a nested rack
   - Uses _resolve_track, gets device at device_index (or navigates to chain device if chain params provided)
   - Validates device.can_have_chains: if not, raise ValueError(f"Device '{device.name}' is not a rack and does not have chains")
   - For Drum Racks (device.can_have_drum_pads):
     - Iterate device.drum_pads, but ONLY include pads with content: `if pad.chains and len(pad.chains) > 0`
     - For each pad: note (pad.note), name (pad.name), chains list
     - For each chain in pad.chains: name, devices list
     - For each device in chain: index, name, type (via _get_device_type), can_have_chains (bool)
     - Return: device_name, device_type, pads list
   - For Instrument/Effect Racks (not drum):
     - Iterate device.chains
     - For each chain: index, name, devices list
     - For each device: index, name, type, can_have_chains
     - Return: device_name, device_type, chains list

Ensure all new handlers follow the existing pattern: log_message on error, re-raise exceptions. Use self._get_device_type(device) for type classification in all device info dicts.
  </action>
  <verify>
    <automated>cd I:/ableton-mcp && python -c "from AbletonMCP_Remote_Script.handlers.devices import DeviceHandlers; methods = [m for m in dir(DeviceHandlers) if not m.startswith('__')]; print(methods); assert '_get_rack_chains' in methods or 'get_rack_chains' in methods; assert '_delete_device' in methods; print('OK')"</automated>
  </verify>
  <done>delete_device and get_rack_chains are registered commands. Drum Rack handler filters empty pads and exposes pad note/name/chain devices. Rack handler exposes chain names, indices, and device lists.</done>
</task>

</tasks>

<verification>
All four new/enhanced commands are registered via @command decorator:
- get_device_parameters (read)
- set_device_parameter (write) -- enhanced with name lookup, clamping, track_type, chain support
- delete_device (write)
- get_rack_chains (read)

Import check passes: `python -c "from AbletonMCP_Remote_Script.handlers.devices import DeviceHandlers"`
</verification>

<success_criteria>
- DeviceHandlers class has 4 registered commands (get_device_parameters, set_device_parameter, delete_device, get_rack_chains) plus _get_device_type helper
- get_device_parameters returns full parameter list with name, value, min, max, is_quantized
- set_device_parameter supports both parameter_name (case-insensitive first-match) and parameter_index
- set_device_parameter clamps out-of-range values and includes warning in response
- All handlers use _resolve_track for track_type support (regular, return, master)
- All handlers support optional chain_index / chain_device_index for rack chain navigation
- get_rack_chains handles both Instrument/Effect Racks and Drum Racks (pads with note/name)
- delete_device returns updated device chain after removal
</success_criteria>

<output>
After completion, create `.planning/phases/07-device-browser/07-01-SUMMARY.md`
</output>
