---
phase: 04-mixing-controls
task: dB calibration
total_tasks: N/A (UAT follow-up)
status: in_progress
last_updated: 2026-03-14T21:10:33.467Z
---

<current_state>
Phase 04 execution is COMPLETE. UAT passed all 8 tests. We are mid-way through calibrating the `_to_db()` volume-to-dB conversion formula in `mixer_helpers.py`.

Currently using a lookup table with linear interpolation (committed in ce3f074). Need to replace it with a proper fitted formula derived from 77 calibration data points collected from Ableton Live.
</current_state>

<completed_work>

- Phase 04 Plan 01: Mixer command handlers (6 handlers) - Done
- Phase 04 Plan 02: MCP mixer tools (6 tools + 12 smoke tests) - Done
- UAT: 8/8 tests passed - Done (04-UAT.md committed)
- fix(tests): Updated 26 tests across 6 files for MCP SDK 1.26.0 call_tool tuple return (d82c894)
- fix(mixer): Three iterations on _to_db formula:
  1. Original: 20*log10(v) — wrong, treated 1.0 as 0 dB
  2. Attempt: 85*log10(v/0.85) — matched 0.85=0dB and 1.0=+6dB but wrong at lower values
  3. Attempt: 40*v-34 (linear) — perfect above v=0.4 but wrong below
  4. Current: lookup table with 10 calibration points + interpolation (ce3f074)
- Collected 77 calibration points from Ableton Live (user set each track to 1dB increments from -inf to +6dB)
</completed_work>

<remaining_work>

- Fit a mathematical formula to the 77 calibration data points (instead of lookup table)
  - Upper range (v >= 0.4): dB = 40*v - 34 (already solved, perfectly linear)
  - Lower range (v < 0.4): needs curve fitting (power law, log, or polynomial)
  - Must be continuous at v=0.4 (both give -18 dB)
  - Max error target: < 0.5 dB
  - Only Python math module available (no numpy/scipy at runtime)
- Replace lookup table in mixer_helpers.py with fitted formula
- Delete the 77 test tracks from Ableton session (user's responsibility)
</remaining_work>

<decisions_made>

- Ableton's volume fader is LINEAR in dB space for v >= 0.4: dB = 40*value - 34
- Unity (0 dB) is at parameter value 0.85, max (+6 dB) at 1.0
- MIDI tracks without instruments have NO volume fader (only MIDI input meter)
- MCP SDK 1.26.0 changed call_tool return to tuple: use result[0][0].text
- _to_db and _pan_label live in mixer_helpers.py (not mixer.py) to prevent circular imports
</decisions_made>

<blockers>
- Need formula fitting for lower range — user has a prompt ready to paste into a separate Claude conversation for curve fitting research
</blockers>

<context>
The full 77-point calibration dataset is embedded in the prompt the user will paste into another Claude session. That session will produce the formula. Once we have it, just replace the lookup table in mixer_helpers.py with:

```python
def _to_db(value):
    if value <= 0:
        return "-inf dB"
    if value >= 0.4:
        db = 40 * value - 34
    else:
        db = <fitted_formula_here>
    return f"{db:.1f} dB"
```

Key file: AbletonMCP_Remote_Script/handlers/mixer_helpers.py
</context>

<next_action>
Start with: Get the fitted formula from the curve-fitting research, update _to_db() in mixer_helpers.py, run tests, reload Ableton Remote Script, verify with a few spot checks, and commit.
</next_action>
