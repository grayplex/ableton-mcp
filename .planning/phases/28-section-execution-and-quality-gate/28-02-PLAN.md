---
phase: 28-section-execution-and-quality-gate
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified: []
autonomous: false
requirements: [EXEC-01, EXEC-02]

must_haves:
  truths:
    - "End-to-end workflow succeeds: blueprint -> production plan -> scaffold -> section checklist -> progress check in live Ableton"
    - "Section checklist shows correct pending/done status for real Ableton tracks"
    - "Arrangement progress correctly identifies tracks without instruments"
  artifacts: []
  key_links: []
---

<objective>
Verify the full Phase 28 workflow in a live Ableton session: scaffold an arrangement, call get_section_checklist for a section, then call get_arrangement_progress to check for empty tracks.

Purpose: Success criterion 3 requires end-to-end verification that cannot be automated without a running Ableton Live instance.
Output: Human-verified confirmation that both tools work correctly against real Ableton state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-section-execution-and-quality-gate/28-CONTEXT.md
@.planning/phases/28-section-execution-and-quality-gate/28-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify execution tools in live Ableton session</name>
  <files>MCP_Server/tools/execution.py</files>
  <read_first>
    .planning/phases/28-section-execution-and-quality-gate/28-01-SUMMARY.md,
    MCP_Server/tools/execution.py
  </read_first>
  <action>
This is a human verification checkpoint. No code changes needed.

Plan 01 implemented two MCP tools:
- `get_section_checklist(plan, section_name)` -- returns per-role instrument status for a named section
- `get_arrangement_progress()` -- returns which MIDI tracks have no instrument loaded

Present the verification steps below to the user and wait for confirmation.
  </action>
  <verify>
Human confirms all verification steps pass:

1. Open Ableton Live with the Remote Script connected
2. In Claude, generate a production plan: call `generate_production_plan` with genre="house", key="Am", bpm=125
3. Scaffold the arrangement: call `scaffold_arrangement` with the plan output
4. Verify scaffolded tracks appear in Ableton Arrangement view (locators + MIDI tracks)
5. Call `get_arrangement_progress` -- all tracks should show as empty (no instruments loaded yet)
   - Expected: `{"empty_tracks": ["kick", "bass", ...], "total_tracks": N, "empty_count": N}` where empty_count equals total_tracks
6. Load an instrument on one of the scaffolded tracks (e.g., drag a Simpler onto the "kick" track)
7. Call `get_arrangement_progress` again -- "kick" should no longer appear in empty_tracks
   - Expected: empty_count decreased by 1, "kick" not in empty_tracks
8. Call `get_section_checklist` with the plan and section_name="intro"
   - Expected: "kick" shows status "done", other roles in intro show "pending"
9. Verify the role-to-track mapping is correct (e.g., if "kick" appears in multiple sections, the checklist should reference the correct numbered track for the queried section)
  </verify>
  <acceptance_criteria>
    - get_arrangement_progress returns correct empty_tracks list matching Ableton state
    - get_section_checklist returns correct done/pending status per role
    - Track names in checklist match the scaffolded track names exactly
    - No errors or connection issues during the workflow
  </acceptance_criteria>
  <done>End-to-end workflow verified in live Ableton: blueprint to production plan to scaffold to section checklist to progress check all working correctly.</done>
</task>

</tasks>

<verification>
Human confirms all 9 verification steps pass in live Ableton session.
</verification>

<success_criteria>
- End-to-end workflow succeeds without errors in live Ableton
- Section checklist accurately reflects instrument status of real tracks
- Arrangement progress correctly detects empty vs loaded tracks
- Phase 28 success criteria 1, 2, and 3 from ROADMAP all satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/28-section-execution-and-quality-gate/28-02-SUMMARY.md`
</output>
