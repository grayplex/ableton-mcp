---
phase: 01-foundation-repair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AbletonMCP_Remote_Script/__init__.py
  - MCP_Server/server.py
  - pyproject.toml
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_python3_cleanup.py
autonomous: true
requirements:
  - FNDN-01
  - FNDN-02
  - FNDN-05

must_haves:
  truths:
    - "No Python 2 compatibility code exists anywhere in the codebase"
    - "All Python 3.11 idioms are used consistently (super(), f-strings, direct queue import)"
    - "No bare except: blocks exist -- every exception handler catches a specific type and logs"
  artifacts:
    - path: "AbletonMCP_Remote_Script/__init__.py"
      provides: "Remote Script cleaned of all Python 2 code and bare excepts"
      contains: "super().__init__"
    - path: "MCP_Server/server.py"
      provides: "MCP server with no bare excepts"
      contains: "except Exception as e"
    - path: "tests/test_python3_cleanup.py"
      provides: "Grep-based tests verifying Py2 code removal and bare except elimination"
      exports: ["test_no_future_imports", "test_no_queue_compat", "test_no_attribute_error_decode", "test_super_calls", "test_f_strings_used", "test_no_bare_excepts"]
    - path: "pyproject.toml"
      provides: "pytest configuration and dev dependencies"
      contains: "[tool.pytest.ini_options]"
    - path: "tests/conftest.py"
      provides: "Shared test fixtures"
  key_links:
    - from: "tests/test_python3_cleanup.py"
      to: "AbletonMCP_Remote_Script/__init__.py"
      via: "file content grep assertions"
      pattern: "open.*__init__\\.py"
    - from: "tests/test_python3_cleanup.py"
      to: "MCP_Server/server.py"
      via: "file content grep assertions"
      pattern: "open.*server\\.py"
---

<objective>
Strip all Python 2 compatibility code, upgrade to Python 3.11 idioms, and replace every bare except block with specific exception handling across both the Remote Script and MCP server.

Purpose: The Remote Script has dead Python 2 code paths (from __future__, Queue compat hack, three AttributeError decode branches, old-style super calls) and five bare except:pass blocks that silently swallow errors. These mask real bugs and make debugging impossible. This plan removes all of it and establishes the test infrastructure needed for verification.

Output: Both source files cleaned of all Py2 remnants and bare excepts. Test infrastructure set up with pytest. Grep-based tests confirming the cleanup is complete.
</objective>

<execution_context>
@C:/Users/ksthu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ksthu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-repair/01-CONTEXT.md
@.planning/phases/01-foundation-repair/01-RESEARCH.md
@.planning/phases/01-foundation-repair/01-VALIDATION.md

<interfaces>
<!-- Current source files that will be modified. Read these before making changes. -->

From AbletonMCP_Remote_Script/__init__.py (1062 lines):
- Line 2: `from __future__ import absolute_import, print_function, unicode_literals` -- DELETE
- Lines 11-15: `try: import Queue as queue` compat block -- REPLACE with `import queue`
- Line 30: `ControlSurface.__init__(self, c_instance)` -- REPLACE with `super().__init__(c_instance)`
- Line 48: String concatenation with `+` -- REPLACE with f-string
- Line 59: `except: pass` (server.close) -- REPLACE with `except Exception as e: self.log_message(...)`
- Line 72: `ControlSurface.disconnect(self)` -- REPLACE with `super().disconnect()`
- Lines 88, 90, 91: String concatenation with `+` -- REPLACE with f-strings
- Line 137: `buffer = ''  # Changed from b'' to '' for Python 2` -- leave buffer for now (Plan 02 replaces entire _handle_client)
- Lines 151-156: `try: buffer += data.decode('utf-8') except AttributeError:` -- REPLACE with direct `buffer += data.decode('utf-8')`
- Lines 169-174: `try: client.sendall(...).encode('utf-8') except AttributeError:` -- REPLACE with direct `.encode('utf-8')`
- Lines 189-193: second encode try/except -- same fix
- Line 194: `except: break` -- REPLACE with `except Exception as e: self.log_message(...); break`
- Line 206: `except: pass` (client.close) -- REPLACE with `except Exception as e: self.log_message(...)`
- Line 820: `except: return "unknown"` (_get_device_type) -- REPLACE with `except Exception as e: self.log_message(...); return "unknown"`
- Various: `.format()` calls -- REPLACE with f-strings
- Various: string concatenation with `+` -- REPLACE with f-strings

From MCP_Server/server.py (659 lines):
- Line 211: `except: pass` (connection cleanup in get_ableton_connection) -- REPLACE with `except Exception as e: logger.warning(...)`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up test infrastructure and write cleanup verification tests</name>
  <files>pyproject.toml, tests/__init__.py, tests/conftest.py, tests/test_python3_cleanup.py</files>
  <action>
1. Add dev dependencies to pyproject.toml: run `uv add --dev pytest>=8.3 pytest-asyncio>=0.25 pytest-timeout>=2.0`

2. Add pytest config to pyproject.toml under `[tool.pytest.ini_options]`:
   - `asyncio_mode = "auto"`
   - `testpaths = ["tests"]`
   - `timeout = 10`

3. Create `tests/__init__.py` (empty file)

4. Create `tests/conftest.py` with:
   - `ROOT_DIR` fixture pointing to project root
   - `remote_script_source` fixture that reads `AbletonMCP_Remote_Script/__init__.py` content
   - `server_source` fixture that reads `MCP_Server/server.py` content

5. Create `tests/test_python3_cleanup.py` with the following test functions (all grep the source files for forbidden patterns):
   - `test_no_future_imports(remote_script_source)`: assert `from __future__` not in source
   - `test_no_queue_compat_hack(remote_script_source)`: assert `import Queue` not in source (case-sensitive), assert `import queue` IS present
   - `test_no_attribute_error_decode_branches(remote_script_source)`: assert there are zero `except AttributeError` blocks that are part of encode/decode patterns (search for `except AttributeError` followed within 2 lines by `# Python 2`)
   - `test_super_calls_used(remote_script_source)`: assert `ControlSurface.__init__` not in source, assert `ControlSurface.disconnect(self)` not in source, assert `super().__init__` in source, assert `super().disconnect()` in source
   - `test_no_bare_excepts(remote_script_source, server_source)`: for both sources, use regex `r'except\s*:'` to find bare except blocks. Assert zero matches. Do NOT match `except SomeError:` which is fine.
   - `test_f_strings_used(remote_script_source)`: assert count of `".format("` calls is zero or near-zero (allow up to 2 for edge cases in Ableton API string building). Assert count of f-string usage (`f"` or `f'`) is greater than 10.

All tests should read the ACTUAL source files from disk (not test fixtures with fake content). This ensures the tests validate the real codebase state.
  </action>
  <verify>
    <automated>cd /i/ableton-mcp && uv run pytest tests/test_python3_cleanup.py -x -v 2>&1 | head -50</automated>
  </verify>
  <done>Test infrastructure exists. All 6 cleanup tests run but FAIL (red phase of TDD -- the source files have not been cleaned yet). pytest discovers and runs tests without import errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Strip Python 2 code, upgrade to Python 3.11 idioms, and replace bare excepts</name>
  <files>AbletonMCP_Remote_Script/__init__.py, MCP_Server/server.py</files>
  <behavior>
    - test_no_future_imports: `from __future__` line is gone
    - test_no_queue_compat_hack: `import Queue` hack is gone, direct `import queue` exists
    - test_no_attribute_error_decode_branches: all three try/except AttributeError blocks in _handle_client are simplified to direct calls
    - test_super_calls_used: old-style ControlSurface.__init__ and .disconnect replaced with super()
    - test_no_bare_excepts: all 5 bare except blocks replaced with `except Exception as e:` plus logging
    - test_f_strings_used: string concatenation and .format() replaced with f-strings throughout
  </behavior>
  <action>
Modify `AbletonMCP_Remote_Script/__init__.py`:

1. **Line 2**: Delete `from __future__ import absolute_import, print_function, unicode_literals`

2. **Lines 11-15**: Replace the try/except Queue import block with just `import queue`

3. **Line 30**: Replace `ControlSurface.__init__(self, c_instance)` with `super().__init__(c_instance)`

4. **Line 48**: Replace `"AbletonMCP: Listening for commands on port " + str(DEFAULT_PORT)` with `f"AbletonMCP: Listening for commands on port {DEFAULT_PORT}"`

5. **Line 59**: Replace `except: pass` with `except Exception as e: self.log_message(f"[ERROR] Error closing server socket: {e}")`

6. **Line 72**: Replace `ControlSurface.disconnect(self)` with `super().disconnect()`

7. **Lines 88, 90, 91**: Replace string concatenation with f-strings (e.g., `f"Server started on port {DEFAULT_PORT}"`)

8. **Lines 104, 126, 163**: Replace string concatenation in log_message calls with f-strings

9. **Lines 151-156**: Remove the try/except AttributeError block. Replace with just `buffer += data.decode('utf-8')`. The `except AttributeError` branch is dead code -- `socket.recv()` always returns `bytes` in Python 3.

10. **Lines 169-174**: Remove the try/except AttributeError block. Replace with just `client.sendall(json.dumps(response).encode('utf-8'))`.

11. **Lines 189-193**: Same fix as above for the error response sendall.

12. **Line 194**: Replace `except: break` with `except Exception as e: self.log_message(f"[ERROR] Failed to send error response, connection dead: {e}"); break`

13. **Line 206**: Replace `except: pass` with `except Exception as e: self.log_message(f"[ERROR] Error closing client socket: {e}")`

14. **Line 820**: Replace `except: return "unknown"` with `except Exception as e: self.log_message(f"[ERROR] Could not determine device type: {e}"); return "unknown"`

15. **Throughout**: Replace remaining `.format()` calls with f-strings. Replace remaining string concatenation with `+` in log_message and show_message calls with f-strings.

16. Add type hints to `__init__`, `disconnect`, `start_server`, `_server_thread`, `_handle_client` method signatures (parameter and return types).

Modify `MCP_Server/server.py`:

17. **Line 211**: Replace bare `except: pass` in `get_ableton_connection()` connection cleanup with `except Exception as e: logger.warning(f"Error during connection cleanup: {e}")`

IMPORTANT: Do NOT change the buffer accumulation logic in _handle_client beyond removing the Py2 compat branches. The buffer approach will be entirely replaced by length-prefix framing in Plan 02. Do NOT change `receive_full_response()` in server.py -- that is Plan 02's responsibility.

IMPORTANT: Do NOT change the if/elif dispatch chain in _process_command. That is Plan 03's responsibility.
  </action>
  <verify>
    <automated>cd /i/ableton-mcp && uv run pytest tests/test_python3_cleanup.py -x -v</automated>
  </verify>
  <done>All 6 cleanup tests pass green. No `from __future__`, no Queue compat hack, no AttributeError decode branches, super() used for init and disconnect, zero bare except blocks in either file, f-strings used throughout. Both files still have the same functional behavior -- only dead code and style was changed.</done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/test_python3_cleanup.py -v` -- all 6 tests pass
2. Grep confirmation: `grep -rn "from __future__" AbletonMCP_Remote_Script/ MCP_Server/` returns no results
3. Grep confirmation: `grep -rn "except:" AbletonMCP_Remote_Script/__init__.py MCP_Server/server.py` returns no results (bare excepts eliminated)
4. Grep confirmation: `grep -n "super().__init__" AbletonMCP_Remote_Script/__init__.py` returns a match
5. Python syntax check: `python -c "import ast; ast.parse(open('AbletonMCP_Remote_Script/__init__.py').read())"` succeeds (valid Python 3)
</verification>

<success_criteria>
- Zero Python 2 compatibility code in either source file
- Zero bare except blocks in either source file
- Python 3.11 idioms used consistently (super(), f-strings, direct queue import)
- All existing functionality preserved (no behavioral changes)
- Test infrastructure operational with pytest
- All 6 verification tests pass green
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-repair/01-01-SUMMARY.md`
</output>
