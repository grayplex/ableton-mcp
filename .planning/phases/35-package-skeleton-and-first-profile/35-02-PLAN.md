---
phase: 35-package-skeleton-and-first-profile
plan: 02
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - MCP_Server/sounds/wavetable.py
autonomous: false
requirements:
  - INST-01

must_haves:
  truths:
    - "Browser root path 'Instruments/Wavetable' has been tested against a live Ableton session via get_browser_items_at_path"
    - "If the path is wrong, the correct path has been identified from the error response and wavetable.py updated"
    - "The validated path is loadable via load_instrument_or_effect"
  artifacts:
    - path: "MCP_Server/sounds/wavetable.py"
      provides: "Wavetable profile with validated browser root path"
      contains: "\"root\":"
  key_links:
    - from: "MCP_Server/sounds/wavetable.py"
      to: "Ableton Live browser"
      via: "get_browser_items_at_path MCP tool"
      pattern: "root.*Instruments"
---

<objective>
Validate the Wavetable browser root path against a live Ableton session and fix it if wrong.

Purpose: The browser root path in wavetable.py is an assumption ("Instruments/Wavetable"). This plan confirms it works with real Ableton, or discovers and applies the correct path. Per D-06, if validation fails, we log a warning and keep the path -- but we should try to get it right.
Output: wavetable.py with a confirmed-correct browser root path, or a documented warning if Ableton is unavailable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-package-skeleton-and-first-profile/35-CONTEXT.md
@.planning/phases/35-package-skeleton-and-first-profile/35-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate browser root path against live Ableton</name>
  <files>MCP_Server/sounds/wavetable.py</files>
  <read_first>
    - MCP_Server/sounds/wavetable.py (current browser root path value)
    - MCP_Server/tools/browser.py (get_browser_items_at_path implementation -- understand what it returns on success and error)
  </read_first>
  <action>
    Use the MCP tool `get_browser_items_at_path` to validate the Wavetable browser root path:

    1. Call `get_browser_items_at_path(path="Instruments/Wavetable")` via MCP.
    2. **If successful** (returns a list of items/categories): The path is correct. No changes needed to wavetable.py.
    3. **If error** (returns error with `available_categories` or similar): The path format is wrong.
       - Read the error response to identify available top-level categories.
       - Try alternative paths: `Sounds/Instruments/Wavetable`, `Instruments/Instrument Rack/Wavetable`, or whatever the error suggests.
       - Once the correct root path is found, update `MCP_Server/sounds/wavetable.py` PROFILE dict: change `"root": "Instruments/Wavetable"` to the correct value.
    4. **If Ableton is not running** (connection error): Per D-06, log a warning in the summary but keep the assumed path "Instruments/Wavetable". The profile still works without live validation.

    After validation, call `load_instrument_or_effect(path="{validated_root_path}")` to confirm the instrument actually loads. This is the end-to-end proof that the browser path is usable by Claude for sound selection.
  </action>
  <verify>
    <automated>cd /home/user/ableton-mcp && python -m pytest tests/test_sounds.py -x</automated>
  </verify>
  <acceptance_criteria>
    - MCP_Server/sounds/wavetable.py contains `"root":` with a validated browser path string
    - `python -m pytest tests/test_sounds.py -x` exits 0 (no regressions from any path update)
    - The SUMMARY documents whether validation succeeded, failed, or was skipped (no Ableton)
  </acceptance_criteria>
  <done>Browser root path in wavetable.py has been validated against live Ableton (or documented as unvalidated if Ableton unavailable). Tests still pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm browser path validation result</name>
  <files>MCP_Server/sounds/wavetable.py</files>
  <action>
    Present the browser path validation outcome to the user for confirmation. Report one of three outcomes:
    - SUCCESS: `get_browser_items_at_path("Instruments/Wavetable")` returned items and `load_instrument_or_effect` loaded the instrument.
    - FIXED: The original path was wrong, corrected to `{new_path}` in wavetable.py, and the new path loads successfully.
    - SKIPPED: Ableton was not running. Path kept as assumed value "Instruments/Wavetable" per D-06.
  </action>
  <what-built>The Wavetable browser root path has been validated against live Ableton (or attempted). The executor will report the outcome in the plan summary.</what-built>
  <how-to-verify>
    1. Check the executor's output for validation result:
       - SUCCESS: The path worked and instrument loaded
       - FIXED: The path was wrong, corrected to a new value
       - SKIPPED: Ableton was not running, path kept as assumption
    2. If Ableton is available, optionally verify yourself:
       - In Claude Desktop or MCP client, call: `get_browser_items_at_path(path="Instruments/Wavetable")`
       - Confirm it returns category items (Pads, Leads, Bass, etc.)
    3. If path was changed, confirm the new path makes sense for Wavetable instrument
  </how-to-verify>
  <verify>
    <automated>cd /home/user/ableton-mcp && python -c "from MCP_Server.sounds import get_profile; p = get_profile('wavetable'); print('root:', p['browser']['root'])"</automated>
  </verify>
  <acceptance_criteria>
    - User has reviewed the validation outcome and typed "approved"
    - MCP_Server/sounds/wavetable.py contains a `"root":` value that has been validated or documented as assumed
  </acceptance_criteria>
  <done>User has confirmed the browser path validation result is acceptable.</done>
  <resume-signal>Type "approved" if validation result is acceptable, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_sounds.py -x` -- all tests still pass after any path updates
2. `python -c "from MCP_Server.sounds import get_profile; print(get_profile('wavetable')['browser']['root'])"` -- prints the validated (or assumed) root path
3. Browser path validation documented in SUMMARY (success/fixed/skipped)
</verification>

<success_criteria>
- Wavetable browser root path validated against live Ableton OR documented as unvalidated
- If path was wrong, wavetable.py updated with correct path
- All existing tests still pass
- User has confirmed the validation result
</success_criteria>

<output>
After completion, create `.planning/phases/35-package-skeleton-and-first-profile/35-02-SUMMARY.md`
</output>
