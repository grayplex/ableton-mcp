---
phase: 36-instrument-profile-authoring
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - MCP_Server/sounds/analog.py
  - MCP_Server/sounds/operator.py
  - MCP_Server/sounds/drift.py
  - MCP_Server/sounds/simpler.py
  - MCP_Server/sounds/drum_rack.py
  - MCP_Server/sounds/wavetable.py
autonomous: false
requirements:
  - INST-02
  - INST-03
  - INST-04
  - INST-05
  - INST-06

must_haves:
  truths:
    - "Browser root paths for all 6 instruments have been tested against a live Ableton session via get_browser_items_at_path"
    - "Any paths that returned errors have been corrected in their respective profile modules"
    - "The Drum Rack root path specifically has been confirmed (highest-uncertainty path)"
    - "If Ableton is unavailable, all 6 paths are documented as assumed per D-06"
  artifacts:
    - path: "MCP_Server/sounds/drum_rack.py"
      provides: "Drum Rack profile with validated (or assumed) browser root"
      contains: "\"root\":"
    - path: "MCP_Server/sounds/analog.py"
      provides: "Analog profile with validated (or assumed) browser root"
      contains: "\"root\":"
  key_links:
    - from: "MCP_Server/sounds/drum_rack.py"
      to: "Ableton Live browser"
      via: "get_browser_items_at_path MCP tool"
      pattern: "root.*Instruments"
---

<objective>
Validate all 6 instrument browser root paths against a live Ableton session and fix any that are wrong.

Purpose: All 5 new profile browser paths are assumptions following the "Instruments/Name" pattern. Drum Rack is the highest-uncertainty path -- it may be "Drums" or "Instruments/Drum Rack". This plan confirms all paths work, or documents them as assumed if Ableton is unavailable.
Output: All profile modules with confirmed-correct browser root paths, or documented assumptions per D-06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/36-instrument-profile-authoring/36-CONTEXT.md
@.planning/phases/36-instrument-profile-authoring/36-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate all 6 browser root paths against live Ableton</name>
  <files>MCP_Server/sounds/analog.py, MCP_Server/sounds/operator.py, MCP_Server/sounds/drift.py, MCP_Server/sounds/simpler.py, MCP_Server/sounds/drum_rack.py, MCP_Server/sounds/wavetable.py</files>
  <read_first>
    - MCP_Server/sounds/analog.py (current root path)
    - MCP_Server/sounds/operator.py (current root path)
    - MCP_Server/sounds/drift.py (current root path)
    - MCP_Server/sounds/simpler.py (current root path)
    - MCP_Server/sounds/drum_rack.py (current root path)
    - MCP_Server/sounds/wavetable.py (current root path -- may still be unvalidated from Phase 35)
    - MCP_Server/tools/browser.py (get_browser_items_at_path implementation)
  </read_first>
  <action>
    For each of the 6 instruments, call `get_browser_items_at_path(path="{root}")` via MCP:

    1. Wavetable: `get_browser_items_at_path(path="Instruments/Wavetable")`
    2. Analog: `get_browser_items_at_path(path="Instruments/Analog")`
    3. Operator: `get_browser_items_at_path(path="Instruments/Operator")`
    4. Drift: `get_browser_items_at_path(path="Instruments/Drift")`
    5. Simpler: `get_browser_items_at_path(path="Instruments/Simpler")`
    6. Drum Rack: `get_browser_items_at_path(path="Instruments/Drum Rack")` -- also try `"Drums"` if this fails

    **For each result:**
    - SUCCESS (returns items): Path is correct. No change needed.
    - ERROR with alternatives: Read the error to identify the correct path. Update the profile module.
    - Drum Rack special case: If "Instruments/Drum Rack" fails, try "Drums" as the root. Update drum_rack.py with whichever works.

    **If Ableton is not running** (connection error on all calls): Per D-06, log a warning and keep all assumed paths. Document each path as ASSUMED in the summary.

    After confirming paths, attempt `load_instrument_or_effect(path="{root}")` for at least the Drum Rack path to confirm end-to-end loadability.
  </action>
  <verify>
    <automated>cd /home/user/ableton-mcp && python -m pytest tests/test_sounds.py -x</automated>
  </verify>
  <acceptance_criteria>
    - All 6 profile modules still have non-empty "root" paths
    - `python -m pytest tests/test_sounds.py -x` exits 0 (no regressions from any path updates)
    - Summary documents outcome for each path (validated/corrected/assumed)
  </acceptance_criteria>
  <done>All 6 instrument browser paths have been tested. Any corrections applied. Validation outcomes documented in summary.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm browser path validation results</name>
  <files>MCP_Server/sounds/drum_rack.py</files>
  <action>
    Present the browser path validation outcomes to the user. For each instrument, report one of:
    - VALIDATED: `get_browser_items_at_path` returned items -- path confirmed correct
    - CORRECTED: Original path was wrong; corrected to `{new_path}` in the profile module
    - ASSUMED: Ableton not running; path kept as assumed value per D-06

    Special attention to Drum Rack -- report its root path explicitly since it is the highest-uncertainty value.
  </action>
  <what-built>All 5 new instrument profile modules (Analog, Operator, Drift, Simpler, Drum Rack) have been authored following the Wavetable reference schema. Browser root paths have been validated against live Ableton or documented as assumed. The catalog now returns 6 instruments.</what-built>
  <how-to-verify>
    1. Check executor's path validation results for all 6 instruments
    2. Confirm Drum Rack root path is acceptable (validated as "Instruments/Drum Rack" or corrected to actual path)
    3. If Ableton is available, optionally verify:
       - Call `get_browser_items_at_path(path="Instruments/Drum Rack")` and confirm it returns drum rack presets
    4. Run: `python -c "from MCP_Server.sounds import list_profiles; print([p['id'] for p in list_profiles()])"` -- should return all 6 ids
  </how-to-verify>
  <verify>
    <automated>cd /home/user/ableton-mcp && python -c "from MCP_Server.sounds import list_profiles; ids = sorted([p['id'] for p in list_profiles()]); print(ids); assert len(ids) == 6"</automated>
    <automated>cd /home/user/ableton-mcp && python -m pytest tests/ -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - User has reviewed all 6 path validation outcomes and typed "approved"
    - All 6 profile modules contain a "root" path that has been validated or documented as assumed
    - Full test suite still green
  </acceptance_criteria>
  <done>User confirmed all browser path validation results. Phase 36 complete.</done>
  <resume-signal>Type "approved" if validation results are acceptable, or describe any issues with specific paths</resume-signal>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_sounds.py -v` -- all tests pass
2. `python -m pytest tests/ -x` -- full suite green
3. `python -c "from MCP_Server.sounds import list_profiles; print(sorted([p['id'] for p in list_profiles()]))"` -- prints all 6 ids
4. All 6 browser root path outcomes documented in summary (validated/corrected/assumed)
</verification>

<success_criteria>
- All 6 browser root paths tested against live Ableton (or documented as unavailable)
- Drum Rack browser root path confirmed or corrected
- All profile modules updated if corrections needed
- Full test suite green
- User has approved all validation outcomes
</success_criteria>

<output>
After completion, create `.planning/phases/36-instrument-profile-authoring/36-02-SUMMARY.md`
</output>
