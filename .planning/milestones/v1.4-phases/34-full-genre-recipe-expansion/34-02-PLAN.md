---
phase: 34-full-genre-recipe-expansion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - MCP_Server/mixing/hip_hop_trap.py
  - MCP_Server/mixing/disco_funk.py
  - MCP_Server/mixing/neo_soul_rnb.py
  - MCP_Server/mixing/lo_fi.py
  - MCP_Server/tools/mixing.py
  - MCP_Server/tools/intelligence.py
autonomous: true
requirements: [RECIP-02, MSTR-01]

must_haves:
  truths:
    - "User can retrieve a mix recipe for any role in hip_hop_trap, disco_funk, neo_soul_rnb, or lo_fi"
    - "User can retrieve a master bus recipe for hip_hop_trap, disco_funk, neo_soul_rnb, or lo_fi"
    - "All 4 new genre recipes have all 9 canonical roles"
    - "All recipe parameter names exist in the device CATALOG"
    - "Tool docstrings reference list_recipes() instead of hardcoded genre list"
    - "Error suggestion strings reference list_recipes() dynamically"
  artifacts:
    - path: "MCP_Server/mixing/hip_hop_trap.py"
      provides: "Hip-Hop/Trap RECIPE + MASTER_RECIPE"
      contains: "RECIPE"
    - path: "MCP_Server/mixing/disco_funk.py"
      provides: "Disco/Funk RECIPE + MASTER_RECIPE"
      contains: "RECIPE"
    - path: "MCP_Server/mixing/neo_soul_rnb.py"
      provides: "Neo-Soul/R&B RECIPE + MASTER_RECIPE"
      contains: "RECIPE"
    - path: "MCP_Server/mixing/lo_fi.py"
      provides: "Lo-Fi RECIPE + MASTER_RECIPE"
      contains: "RECIPE"
  key_links:
    - from: "MCP_Server/mixing/hip_hop_trap.py"
      to: "MCP_Server/mixing/catalog.py"
      via: "pkgutil auto-discovery"
      pattern: "RECIPE = "
    - from: "MCP_Server/tools/mixing.py"
      to: "MCP_Server/mixing/catalog.py"
      via: "list_recipes() import for docstrings"
      pattern: "list_recipes"
    - from: "MCP_Server/tools/intelligence.py"
      to: "MCP_Server/mixing/catalog.py"
      via: "list_recipes() import for docstrings"
      pattern: "list_recipes"
---

<objective>
Create 4 groove/organic genre recipe files (hip_hop_trap, disco_funk, neo_soul_rnb, lo_fi) with RECIPE and MASTER_RECIPE constants, and update tool docstrings per D-09.

Purpose: Delivers the groove/organic half of the 8-genre expansion (RECIP-02, MSTR-01) and ensures tool documentation stays accurate for 12 genres.
Output: 4 new recipe files auto-discovered by pkgutil, dynamic docstrings referencing list_recipes().
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-full-genre-recipe-expansion/34-CONTEXT.md
@.planning/phases/34-full-genre-recipe-expansion/34-RESEARCH.md

@MCP_Server/mixing/house.py
@MCP_Server/mixing/drum_and_bass.py
@MCP_Server/mixing/catalog.py
@MCP_Server/devices/catalog.py
@MCP_Server/tools/mixing.py
@MCP_Server/tools/intelligence.py
@tests/test_mixing.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 4 groove/organic genre recipe files</name>
  <files>MCP_Server/mixing/hip_hop_trap.py, MCP_Server/mixing/disco_funk.py, MCP_Server/mixing/neo_soul_rnb.py, MCP_Server/mixing/lo_fi.py</files>
  <read_first>MCP_Server/mixing/house.py, MCP_Server/mixing/drum_and_bass.py, MCP_Server/devices/catalog.py</read_first>
  <action>
Create 4 new genre recipe files mirroring the exact structure of house.py. Each file must have:

1. Comment header referencing D-01 through D-04 (copy verbatim from house.py lines 1-14)
2. `RECIPE` dict with all 9 roles: kick, bass, lead, pad, chords, vocal, atmospheric, return, master
3. `MASTER_RECIPE` dict at bottom with GlueCompressor + MultibandDynamics + Limiter
4. Section comment separators between roles (matching house.py style)

**Per D-01:** All values in natural units (Hz, dB, ms, %, raw 0-1 for normalized params).
**Per D-02:** Sound-shaping params only -- no Device On, LegacyMode, S/C Listen.
**Per D-03:** All 9 roles present in every file. Non-typical pairings get safe generic values.
**Per D-04:** Omit inapplicable devices entirely -- no None markers.

**CRITICAL -- Compressor2 Ratio uses 0.0-1.0 range (NOT raw ratio):**
- 0.35 ~ 2:1, 0.4 ~ 2.5:1, 0.5 ~ 3:1, 0.55 ~ 3.5:1, 0.6 ~ 4:1, 0.8 ~ 8:1

**CRITICAL -- GlueCompressor Dry/Wet is 100.0 (natural %) NOT 1.0**

**Device classes available per role (reference house.py for exact param names):**
- Every role: `Eq8` (8-band EQ), `StereoGain` (utility gain/width/mono)
- Most roles: `Compressor2` (compressor)
- Kick: `DrumBuss` (drum bus saturation)
- Lead/pad/chords/vocal/atmospheric/return: `Reverb`, `Delay` (as appropriate)
- Vocal: `Gate` (noise gate)
- Atmospheric/lead/pad: `AutoFilter` (as appropriate)

**Eq8 params** (copy exact param names from house.py):
- `1 Filter On A` through `8 Filter On A` (0=off, 1=on)
- `N Filter Type A` (0=48dB/oct, 1=12dB/oct, 2=Low Shelf, 3=Bell, 4=Notch, 5=High Shelf, 6=LP 12dB, 7=HP 12dB)
- `N Frequency A` (Hz), `N Gain A` (dB), `N Resonance A`
- `Output Gain` (dB), `Scale` (1.0), `Adaptive Q` (0 or 1)

**StereoGain params:** `Gain` (dB), `Stereo Width` (0.0=mono, 1.0=normal), `Mono` (0/1), `Bass Mono` (0/1), `Bass Freq` (0-1 raw), `Balance` (dB)

**Compressor2 params:** `Threshold` (dB), `Ratio` (0-1 raw), `Attack` (ms), `Release` (ms), `Output Gain` (dB), `Makeup` (0/1), `Dry/Wet` (%), `Knee` (dB), `Model` (0=Peak/1=RMS), `Env Mode` (0=Peak)

**DrumBuss params:** `Drive` (0-1), `Drive Type` (0=Soft/1=Med/2=Hard), `Crunch` (0-1), `Damping Freq` (0-1), `Transients` (0-1), `Boom Freq` (0-1), `Boom Amt` (0-1), `Boom Decay` (0-1), `Trim` (0-1), `Output Gain` (dB), `Dry/Wet` (%), `Compressor On` (0/1)

**Reverb params (reference house.py):** `DecayTime`, `Room Size`, `Stereo Image`, `ER Spin Rate`, `ER Spin Amount`, `Diffuse Delay`, `High Cut`, `Low Cut`, `ER Level`, `Diffuse Level`, `Dry/Wet`, `PreDelay`, `HiShelf Frequency`, `HiShelf Gain`, `LowShelf Frequency`, `LowShelf Gain`, `Input Filter Freq`, `Chorus Rate`

**Delay params (reference house.py):** `L Time`, `R Time`, `Feedback`, `Dry/Wet`

**Gate params (reference house.py):** `Threshold`, `Return`, `FlipMode`, `Lookahead`, `Attack`, `Hold`, `Release`, `Floor`

**AutoFilter params (reference house.py):** `Frequency`, `Resonance`, `Filter Type`, `LFO Amount`, `LFO Rate`, `LFO Phase`, `LFO Offset`, `Envelope Amount`, `Envelope Attack`, `Envelope Release`, `Dry/Wet`

**MASTER_RECIPE template (identical param set for all 4 genres, values vary):**
```python
MASTER_RECIPE = {
    "GlueCompressor": {
        "Threshold": ...,        # dB (-40 to 0)
        "Ratio": ...,            # 0-2 raw range
        "Attack": ...,           # 0-6 raw range
        "Release": ...,          # 0-6 raw range
        "Makeup": ...,           # dB (-15 to 15)
        "Dry/Wet": 100.0,        # % always 100
        "Peak Clip In": 0,       # off
        "Range": ...,            # 0-70 raw range
    },
    "MultibandDynamics": {
        "Master Output": 0.0,
        "Band Activator (High)": 1,
        "Band Activator (Mid)": 1,
        "Band Activator (Low)": 1,
        "Above Threshold (Low)": ...,
        "Above Ratio (Low)": ...,
        "Above Threshold (Mid)": ...,
        "Above Ratio (Mid)": ...,
        "Above Threshold (High)": ...,
        "Above Ratio (High)": ...,
        "Input Gain (Low)": 0.0,
        "Input Gain (Mid)": 0.0,
        "Input Gain (High)": 0.0,
    },
    "Limiter": {
        "Input Gain": ...,       # dB (-15 to 15)
        "Ceiling": ...,          # 0-1 raw
        "Link": 1.0,
        "Lookahead": 1,          # 0=off, 1=1ms, 2=6ms
    },
}
```

**Genre-specific guidance (Claude's discretion for exact values):**

**hip_hop_trap.py:** Hard-hitting kick/808, crisp vocals, dry mix overall. DrumBuss on kick (Drive 0.5-0.7, Hard type). Gate on vocals. Minimal reverb except on vocals/atmospheric. Bass: heavy 808 sub, mono. Kick: very punchy, aggressive compression (Ratio 0.6-0.7). Vocal: crisp, present, gated. Master: very loud, heavy limiting (Input Gain 7-9 dB), aggressive multiband compression.

**disco_funk.py:** Groovy, warm, dynamic. Less compression than electronic genres (Ratio 0.3-0.4 to preserve dynamics). Warm EQ (low shelf boosts). Moderate reverb (plate-style, 1-2s). Kick: warm, round. Bass: groovy, warm. Lead: bright, funky. Master: moderate loudness, preserve dynamics, gentle limiting.

**neo_soul_rnb.py:** Smooth, warm, vocal-forward. Gentle compression (Ratio 0.3-0.35). Warm low-shelf EQ boosts. Intimate reverb (short-medium, 1-2s). Vocal: the star -- warm, present, gentle compression. Pad: warm, enveloping. Master: moderate loudness, warm character, gentle limiting.

**lo_fi.py:** Deliberately imperfect, warm, filtered. High-shelf cuts for muffled sound (-3 to -6 dB at 8-10kHz). DrumBuss for warmth on kick/bass (Soft drive). Short reverb (0.5-1.5s). AutoFilter on atmospheric elements. Master: gentle limiting, lo-fi character (roll off highs in master EQ), moderate loudness.
  </action>
  <verify>
    <automated>cd I:/ableton-mcp && python -m pytest tests/test_mixing.py::TestRecipeParameterNames -x -q && python -m pytest tests/test_mixing.py::TestRecipeCompleteness -x -q</automated>
  </verify>
  <acceptance_criteria>
    - MCP_Server/mixing/hip_hop_trap.py contains `RECIPE = {` and `MASTER_RECIPE = {`
    - MCP_Server/mixing/disco_funk.py contains `RECIPE = {` and `MASTER_RECIPE = {`
    - MCP_Server/mixing/neo_soul_rnb.py contains `RECIPE = {` and `MASTER_RECIPE = {`
    - MCP_Server/mixing/lo_fi.py contains `RECIPE = {` and `MASTER_RECIPE = {`
    - Each file contains all 9 role keys: "kick", "bass", "lead", "pad", "chords", "vocal", "atmospheric", "return", "master"
    - Each file contains comment header with D-01, D-02, D-03, D-04 references
    - No file contains `Device On` or `LegacyMode` or `S/C Listen` (per D-02)
    - `pytest tests/test_mixing.py::TestRecipeParameterNames -x` exits 0
    - `pytest tests/test_mixing.py::TestRecipeCompleteness -x` exits 0
  </acceptance_criteria>
  <done>4 groove/organic genre recipe files created, all pass parameter name validation and role completeness tests</done>
</task>

<task type="auto">
  <name>Task 2: Update tool docstrings and error messages per D-09</name>
  <files>MCP_Server/tools/mixing.py, MCP_Server/tools/intelligence.py</files>
  <read_first>MCP_Server/tools/mixing.py, MCP_Server/tools/intelligence.py, MCP_Server/mixing/catalog.py</read_first>
  <action>
**Per D-09:** Replace hardcoded genre lists in docstrings and `format_error` suggestion strings with dynamic references to `list_recipes()`.

**MCP_Server/tools/mixing.py -- 3 tools to update:**

1. `get_mix_recipe` (line 14-29):
   - Docstring line: `- genre: Genre (house, techno, ambient, dnb/drum_and_bass)` -> `- genre: Genre (use list_recipes() to see all available genres)`
   - `format_error` suggestion (line 26-28): Change `"Genres: house, techno, ambient, dnb"` to use `', '.join(list_recipes())` dynamically. Import `list_recipes` from `MCP_Server.mixing.catalog` (already imported as `from MCP_Server.mixing.catalog import get_master_recipe, get_recipe` -- add `list_recipes` to this import).
   - New suggestion string: `f"Roles: kick, bass, lead, pad, chords, vocal, atmospheric, return, master. Genres: {', '.join(list_recipes())}"`

2. `apply_mix_recipe` (line 33-60):
   - Docstring line: `- genre: Genre (house, techno, ambient, dnb/drum_and_bass)` -> `- genre: Genre (use list_recipes() to see all available genres)`
   - `format_error` suggestion (line 47-49): Same pattern -- `f"Roles: kick, bass, lead, pad, chords, vocal, atmospheric, return, master. Genres: {', '.join(list_recipes())}"`

3. `apply_master_recipe` (line 63-86):
   - Docstring line: `- genre: Genre (house, techno, ambient, dnb/drum_and_bass)` -> `- genre: Genre (use list_recipes() to see all available genres)`
   - `format_error` suggestion (line 75): Change `"Genres with master recipes: house, techno, ambient, dnb"` to `f"Genres with master recipes: {', '.join(list_recipes())}"`

**MCP_Server/tools/intelligence.py -- 1 tool to update:**

4. `suggest_mix_adjustments` (line 91-199):
   - Docstring line 103: `genre: Genre for recipe lookup (house, techno, ambient, dnb)` -> `genre: Genre for recipe lookup (use list_recipes() to see all available genres)`
   - `format_error` suggestion (line 133): Change `"Available genres: house, techno, ambient, dnb"` to `f"Available genres: {', '.join(list_recipes())}"`
   - Add `list_recipes` to the import from `MCP_Server.mixing.catalog`: Change `from MCP_Server.mixing.catalog import get_recipe` to `from MCP_Server.mixing.catalog import get_recipe, list_recipes`
  </action>
  <verify>
    <automated>cd I:/ableton-mcp && python -m pytest tests/test_mixing.py -x -q && python -c "from MCP_Server.tools.mixing import get_mix_recipe; r = get_mix_recipe(None, 'kick', 'invalid'); assert 'house' not in r or 'hip_hop_trap' in r; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - MCP_Server/tools/mixing.py does NOT contain the string `Genres: house, techno, ambient, dnb"` (hardcoded removed)
    - MCP_Server/tools/mixing.py contains `list_recipes()` (at least 3 occurrences -- one per tool)
    - MCP_Server/tools/mixing.py import line contains `list_recipes`
    - MCP_Server/tools/intelligence.py does NOT contain `Available genres: house, techno, ambient, dnb"` (hardcoded removed)
    - MCP_Server/tools/intelligence.py contains `list_recipes()` (at least 1 occurrence)
    - MCP_Server/tools/intelligence.py import line contains `list_recipes`
    - All 4 tool docstrings contain `list_recipes()` reference
    - `pytest tests/test_mixing.py -x` exits 0
  </acceptance_criteria>
  <done>All 4 mixing tool docstrings and error messages reference list_recipes() dynamically instead of hardcoded genre lists</done>
</task>

</tasks>

<verification>
- `pytest tests/test_mixing.py -x -q` passes (all 12 genres validated for params, roles, master recipes)
- `python -c "from MCP_Server.mixing.catalog import list_recipes; print(list_recipes())"` returns sorted list of 12 genres
- `python -c "from MCP_Server.mixing.catalog import get_recipe; print(get_recipe('kick', 'hip_hop_trap'))"` returns a dict
- `python -c "from MCP_Server.mixing.catalog import get_master_recipe; print(get_master_recipe('lo_fi'))"` returns a dict
- `grep -c 'list_recipes' MCP_Server/tools/mixing.py` returns >= 3
- `grep -c 'list_recipes' MCP_Server/tools/intelligence.py` returns >= 1
</verification>

<success_criteria>
- 4 new genre recipe files exist in MCP_Server/mixing/ and are auto-discovered
- All 4 genres return valid recipes for all 9 roles
- All 4 genres have MASTER_RECIPE with GlueCompressor + MultibandDynamics + Limiter
- Tool docstrings and error messages use dynamic genre lists
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/34-full-genre-recipe-expansion/34-02-SUMMARY.md`
</output>
