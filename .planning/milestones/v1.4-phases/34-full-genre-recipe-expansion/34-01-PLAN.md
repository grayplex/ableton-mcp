---
phase: 34-full-genre-recipe-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MCP_Server/mixing/synthwave.py
  - MCP_Server/mixing/dubstep.py
  - MCP_Server/mixing/trance.py
  - MCP_Server/mixing/future_bass.py
  - MCP_Server/mixing/catalog.py
  - tests/test_mixing.py
autonomous: true
requirements: [RECIP-02, MSTR-01]

must_haves:
  truths:
    - "User can retrieve a mix recipe for any role in synthwave, dubstep, trance, or future_bass"
    - "User can retrieve a master bus recipe for synthwave, dubstep, trance, or future_bass"
    - "All 4 new genre recipes have all 9 canonical roles"
    - "All recipe parameter names exist in the device CATALOG"
    - "Genre aliases hip-hop and r&b resolve correctly"
    - "Master recipe tests dynamically cover all registered genres (not just hardcoded 4)"
  artifacts:
    - path: "MCP_Server/mixing/synthwave.py"
      provides: "Synthwave RECIPE + MASTER_RECIPE"
      contains: "RECIPE"
    - path: "MCP_Server/mixing/dubstep.py"
      provides: "Dubstep RECIPE + MASTER_RECIPE"
      contains: "RECIPE"
    - path: "MCP_Server/mixing/trance.py"
      provides: "Trance RECIPE + MASTER_RECIPE"
      contains: "RECIPE"
    - path: "MCP_Server/mixing/future_bass.py"
      provides: "Future Bass RECIPE + MASTER_RECIPE"
      contains: "RECIPE"
  key_links:
    - from: "MCP_Server/mixing/synthwave.py"
      to: "MCP_Server/mixing/catalog.py"
      via: "pkgutil auto-discovery"
      pattern: "RECIPE = "
    - from: "MCP_Server/mixing/catalog.py"
      to: "_GENRE_ALIASES"
      via: "alias dict entries"
      pattern: "hip_hop.*hip_hop_trap"
    - from: "tests/test_mixing.py"
      to: "_master_registry"
      via: "dynamic list replaces hardcoded _MASTER_GENRES"
      pattern: "list\\(_master_registry"
---

<objective>
Create 4 electronic/synth-heavy genre recipe files (synthwave, dubstep, trance, future_bass) with RECIPE and MASTER_RECIPE constants, add D-08 genre aliases, and make master recipe tests dynamic.

Purpose: Delivers the electronic half of the 8-genre expansion (RECIP-02, MSTR-01), plus infrastructure fixes (aliases, test coverage).
Output: 4 new recipe files auto-discovered by pkgutil, alias resolution for hip-hop/R&B inputs, dynamic master test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-full-genre-recipe-expansion/34-CONTEXT.md
@.planning/phases/34-full-genre-recipe-expansion/34-RESEARCH.md

@MCP_Server/mixing/house.py
@MCP_Server/mixing/drum_and_bass.py
@MCP_Server/mixing/catalog.py
@MCP_Server/devices/catalog.py
@tests/test_mixing.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 4 electronic genre recipe files</name>
  <files>MCP_Server/mixing/synthwave.py, MCP_Server/mixing/dubstep.py, MCP_Server/mixing/trance.py, MCP_Server/mixing/future_bass.py</files>
  <read_first>MCP_Server/mixing/house.py, MCP_Server/mixing/drum_and_bass.py, MCP_Server/devices/catalog.py</read_first>
  <action>
Create 4 new genre recipe files mirroring the exact structure of house.py. Each file must have:

1. Comment header referencing D-01 through D-04 (copy verbatim from house.py lines 1-14)
2. `RECIPE` dict with all 9 roles: kick, bass, lead, pad, chords, vocal, atmospheric, return, master
3. `MASTER_RECIPE` dict at bottom with GlueCompressor + MultibandDynamics + Limiter
4. Section comment separators between roles (matching house.py style)

**Per D-01:** All values in natural units (Hz, dB, ms, %, raw 0-1 for normalized params).
**Per D-02:** Sound-shaping params only -- no Device On, LegacyMode, S/C Listen.
**Per D-03:** All 9 roles present in every file. Non-typical pairings get safe generic values.
**Per D-04:** Omit inapplicable devices entirely -- no None markers.

**CRITICAL -- Compressor2 Ratio uses 0.0-1.0 range (NOT raw ratio):**
- 0.35 ~ 2:1, 0.4 ~ 2.5:1, 0.5 ~ 3:1, 0.55 ~ 3.5:1, 0.6 ~ 4:1, 0.8 ~ 8:1

**CRITICAL -- GlueCompressor Dry/Wet is 100.0 (natural %) NOT 1.0**

**Device classes available per role (reference house.py for exact param names):**
- Every role: `Eq8` (8-band EQ), `StereoGain` (utility gain/width/mono)
- Most roles: `Compressor2` (compressor)
- Kick: `DrumBuss` (drum bus saturation)
- Lead/pad/chords/vocal/atmospheric/return: `Reverb`, `Delay` (as appropriate)
- Vocal: `Gate` (noise gate)
- Atmospheric/lead/pad: `AutoFilter` (as appropriate)

**Eq8 params** (copy exact param names from house.py):
- `1 Filter On A` through `8 Filter On A` (0=off, 1=on)
- `N Filter Type A` (0=48dB/oct, 1=12dB/oct, 2=Low Shelf, 3=Bell, 4=Notch, 5=High Shelf, 6=LP 12dB, 7=HP 12dB)
- `N Frequency A` (Hz), `N Gain A` (dB), `N Resonance A`
- `Output Gain` (dB), `Scale` (1.0), `Adaptive Q` (0 or 1)

**StereoGain params:** `Gain` (dB), `Stereo Width` (0.0=mono, 1.0=normal), `Mono` (0/1), `Bass Mono` (0/1), `Bass Freq` (0-1 raw), `Balance` (dB)

**Compressor2 params:** `Threshold` (dB), `Ratio` (0-1 raw), `Attack` (ms), `Release` (ms), `Output Gain` (dB), `Makeup` (0/1), `Dry/Wet` (%), `Knee` (dB), `Model` (0=Peak/1=RMS), `Env Mode` (0=Peak)

**DrumBuss params:** `Drive` (0-1), `Drive Type` (0=Soft/1=Med/2=Hard), `Crunch` (0-1), `Damping Freq` (0-1), `Transients` (0-1), `Boom Freq` (0-1), `Boom Amt` (0-1), `Boom Decay` (0-1), `Trim` (0-1), `Output Gain` (dB), `Dry/Wet` (%), `Compressor On` (0/1)

**Reverb params (reference house.py):** `DecayTime`, `Room Size`, `Stereo Image`, `ER Spin Rate`, `ER Spin Amount`, `Diffuse Delay`, `High Cut`, `Low Cut`, `ER Level`, `Diffuse Level`, `Dry/Wet`, `PreDelay`, `HiShelf Frequency`, `HiShelf Gain`, `LowShelf Frequency`, `LowShelf Gain`, `Input Filter Freq`, `Chorus Rate`

**Delay params (reference house.py):** `L Time`, `R Time`, `Feedback`, `Dry/Wet`

**Gate params (reference house.py):** `Threshold`, `Return`, `FlipMode`, `Lookahead`, `Attack`, `Hold`, `Release`, `Floor`

**AutoFilter params (reference house.py):** `Frequency`, `Resonance`, `Filter Type`, `LFO Amount`, `LFO Rate`, `LFO Phase`, `LFO Offset`, `Envelope Amount`, `Envelope Attack`, `Envelope Release`, `Dry/Wet`

**MASTER_RECIPE template (identical param set for all 4 genres, values vary):**
```python
MASTER_RECIPE = {
    "GlueCompressor": {
        "Threshold": ...,        # dB (-40 to 0)
        "Ratio": ...,            # 0-2 raw range
        "Attack": ...,           # 0-6 raw range
        "Release": ...,          # 0-6 raw range
        "Makeup": ...,           # dB (-15 to 15)
        "Dry/Wet": 100.0,        # % always 100
        "Peak Clip In": 0,       # off
        "Range": ...,            # 0-70 raw range
    },
    "MultibandDynamics": {
        "Master Output": 0.0,
        "Band Activator (High)": 1,
        "Band Activator (Mid)": 1,
        "Band Activator (Low)": 1,
        "Above Threshold (Low)": ...,
        "Above Ratio (Low)": ...,
        "Above Threshold (Mid)": ...,
        "Above Ratio (Mid)": ...,
        "Above Threshold (High)": ...,
        "Above Ratio (High)": ...,
        "Input Gain (Low)": 0.0,
        "Input Gain (Mid)": 0.0,
        "Input Gain (High)": 0.0,
    },
    "Limiter": {
        "Input Gain": ...,       # dB (-15 to 15)
        "Ceiling": ...,          # 0-1 raw
        "Link": 1.0,
        "Lookahead": 1,          # 0=off, 1=1ms, 2=6ms
    },
}
```

**Genre-specific guidance (Claude's discretion for exact values):**

**synthwave.py:** Warm, lush, 80s analog aesthetic. Heavy reverb on leads/pads (DecayTime 3-5s), wide stereo (StereoGain Width 1.0-1.2), gentle compression (Ratio 0.35-0.4). Kick: moderate punch. Bass: warm, wide sub. Lead: bright, reverb-drenched. Pad: very wide, long reverb. Master: moderate loudness, warm low end, gentle limiting.

**dubstep.py:** Aggressive bass, heavy sub, distorted mids. DrumBuss on kick (Drive 0.6-0.8, Hard type) and bass. Aggressive compression (Ratio 0.7-0.8). Kick: very punchy, mono. Bass: heavy sub, aggressive processing. Lead: mid-heavy, distorted feel. Master: very loud, heavy limiting (Input Gain 6-8 dB), strong low band compression.

**trance.py:** Driving, euphoric, wide stereo pads. Long reverb tails on pads/leads (4-6s), moderate compression. Kick: tight, punchy. Bass: clean sub. Lead: bright, soaring, long delay. Pad: very wide, ethereal, long reverb. Master: loud, punchy, clean high end, moderate limiting.

**future_bass.py:** Heavy sidechain feel, lush chords, wide stereo. Aggressive chord compression (Ratio 0.6-0.7), reverb-heavy pads, wide stereo image. Kick: punchy but not as aggressive as dubstep. Bass: modern, clean. Chords: the star -- heavy compression, wide stereo, reverb. Master: loud but not crushed, wide stereo image.
  </action>
  <verify>
    <automated>cd I:/ableton-mcp && python -m pytest tests/test_mixing.py::TestRecipeParameterNames -x -q && python -m pytest tests/test_mixing.py::TestRecipeCompleteness -x -q</automated>
  </verify>
  <acceptance_criteria>
    - MCP_Server/mixing/synthwave.py contains `RECIPE = {` and `MASTER_RECIPE = {`
    - MCP_Server/mixing/dubstep.py contains `RECIPE = {` and `MASTER_RECIPE = {`
    - MCP_Server/mixing/trance.py contains `RECIPE = {` and `MASTER_RECIPE = {`
    - MCP_Server/mixing/future_bass.py contains `RECIPE = {` and `MASTER_RECIPE = {`
    - Each file contains all 9 role keys: "kick", "bass", "lead", "pad", "chords", "vocal", "atmospheric", "return", "master"
    - Each file contains comment header with D-01, D-02, D-03, D-04 references
    - No file contains `Device On` or `LegacyMode` or `S/C Listen` (per D-02)
    - `pytest tests/test_mixing.py::TestRecipeParameterNames -x` exits 0
    - `pytest tests/test_mixing.py::TestRecipeCompleteness -x` exits 0
  </acceptance_criteria>
  <done>4 electronic genre recipe files created, all pass parameter name validation and role completeness tests</done>
</task>

<task type="auto">
  <name>Task 2: Add genre aliases and make master recipe tests dynamic</name>
  <files>MCP_Server/mixing/catalog.py, tests/test_mixing.py</files>
  <read_first>MCP_Server/mixing/catalog.py, tests/test_mixing.py</read_first>
  <action>
**catalog.py -- Add D-08 aliases to `_GENRE_ALIASES` dict (line ~44-49):**

Add these entries to the existing `_GENRE_ALIASES` dict:
```python
"hip_hop": "hip_hop_trap",
"r_b": "neo_soul_rnb",
```

Note: `_normalize()` already handles `hip-hop` -> `hip_hop` and `r&b` -> `r_b`, so these two entries cover all D-08 alias cases. `disco_funk` is already canonical (no alias needed).

**tests/test_mixing.py -- Replace hardcoded `_MASTER_GENRES` with dynamic list (line 319):**

Change:
```python
_MASTER_GENRES = ["house", "techno", "ambient", "drum_and_bass"]
```

To:
```python
def _get_master_genres():
    """Dynamic list of all genres with MASTER_RECIPE."""
    _ensure_initialized()
    return sorted(_master_registry.keys())
```

Then update all references to `_MASTER_GENRES` in the `TestMasterRecipeData` class to call `_get_master_genres()`:
- `test_all_genres_have_master_recipe`: iterate `_get_master_genres()` instead of `_MASTER_GENRES`
- `test_master_recipe_has_required_device_keys`: iterate `_get_master_genres()` instead of `_MASTER_GENRES`
- `test_all_master_recipe_params_in_catalog`: iterate `_get_master_genres()` instead of `_MASTER_GENRES`
- `test_master_recipe_values_are_numeric`: iterate `_get_master_genres()` instead of `_MASTER_GENRES`
  </action>
  <verify>
    <automated>cd I:/ableton-mcp && python -m pytest tests/test_mixing.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - MCP_Server/mixing/catalog.py contains `"hip_hop": "hip_hop_trap"`
    - MCP_Server/mixing/catalog.py contains `"r_b": "neo_soul_rnb"`
    - tests/test_mixing.py does NOT contain `_MASTER_GENRES = ["house"` (hardcoded list removed)
    - tests/test_mixing.py contains `_master_registry.keys()` (dynamic approach)
    - `pytest tests/test_mixing.py -x` exits 0
  </acceptance_criteria>
  <done>Genre aliases resolve hip-hop and R&B inputs correctly; master recipe tests dynamically cover all registered genres</done>
</task>

</tasks>

<verification>
- `pytest tests/test_mixing.py -x -q` passes (all recipe param names in CATALOG, all roles present, master recipes validated)
- `python -c "from MCP_Server.mixing.catalog import get_recipe; print(get_recipe('kick', 'synthwave'))"` returns a dict (not None)
- `python -c "from MCP_Server.mixing.catalog import get_master_recipe; print(get_master_recipe('trance'))"` returns a dict (not None)
- `python -c "from MCP_Server.mixing.catalog import get_recipe; print(get_recipe('kick', 'hip-hop'))"` returns a dict (alias resolution works)
</verification>

<success_criteria>
- 4 new genre recipe files exist in MCP_Server/mixing/ and are auto-discovered
- All 4 genres return valid recipes for all 9 roles
- All 4 genres have MASTER_RECIPE with GlueCompressor + MultibandDynamics + Limiter
- Genre aliases hip-hop and r&b resolve correctly
- Master recipe test coverage is dynamic (covers all genres, not hardcoded 4)
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/34-full-genre-recipe-expansion/34-01-SUMMARY.md`
</output>
