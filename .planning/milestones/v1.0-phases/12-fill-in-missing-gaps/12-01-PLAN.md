---
phase: 12-fill-in-missing-gaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AbletonMCP_Remote_Script/handlers/transport.py
  - AbletonMCP_Remote_Script/handlers/scenes.py
  - MCP_Server/tools/transport.py
  - MCP_Server/tools/scenes.py
  - MCP_Server/connection.py
  - tests/conftest.py
  - tests/test_transport.py
  - tests/test_scenes.py
autonomous: true
requirements:
  - SESS-01
  - SESS-03
  - SESS-04
  - SESS-05
  - SESS-06
  - SESS-07
  - SESS-08
  - SESS-09
  - SESS-10
  - SCLE-01
  - SCLE-02
  - SCLE-03
  - SCLE-04
  - ARR-05
  - ARR-06
  - ARR-07

must_haves:
  truths:
    - "User can get/set root note, scale name, and scale mode"
    - "User can get scale intervals (read-only)"
    - "User can get/set cue points, jump to next/prev cue"
    - "User can capture currently playing clips as a new scene"
    - "User can capture recently played MIDI input"
    - "User can tap tempo, toggle metronome, set groove/swing amount"
    - "User can jump by beats, play arrangement selection, get song length"
    - "User can duplicate a scene"
    - "User can get/set clip trigger quantization, session record, record mode"
  artifacts:
    - path: "AbletonMCP_Remote_Script/handlers/transport.py"
      provides: "Extended transport handlers with scale, cue, capture, session controls, navigation"
      contains: "def _get_scale_info"
    - path: "AbletonMCP_Remote_Script/handlers/scenes.py"
      provides: "Extended scene handlers with duplicate_scene"
      contains: "def _duplicate_scene"
    - path: "MCP_Server/tools/transport.py"
      provides: "MCP tools for scale, cue, capture, session controls, navigation"
      contains: "def get_scale_info"
    - path: "MCP_Server/tools/scenes.py"
      provides: "MCP tool for duplicate_scene"
      contains: "def duplicate_scene"
    - path: "MCP_Server/connection.py"
      provides: "Updated _WRITE_COMMANDS with new write commands"
      contains: "set_scale"
  key_links:
    - from: "MCP_Server/tools/transport.py"
      to: "AbletonMCP_Remote_Script/handlers/transport.py"
      via: "send_command"
      pattern: "send_command.*get_scale_info"
    - from: "MCP_Server/tools/scenes.py"
      to: "AbletonMCP_Remote_Script/handlers/scenes.py"
      via: "send_command"
      pattern: "send_command.*duplicate_scene"
---

<objective>
Implement all Song-level gaps: scale/key awareness, cue points, capture workflows, session controls, navigation, and duplicate_scene.

Purpose: Give Claude scale/key awareness for musically-correct note generation, arrangement navigation for timeline work, and capture workflows for AI composition.
Output: ~16 new Remote Script commands + ~12 new MCP tools + smoke tests.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-fill-in-missing-gaps/12-CONTEXT.md
@.planning/phases/12-fill-in-missing-gaps/12-RESEARCH.md
@.planning/phases/11-check-for-live-object-model-gaps/11-GAP-REPORT.md

<interfaces>
<!-- Existing patterns the executor must follow -->

From AbletonMCP_Remote_Script/registry.py:
```python
from AbletonMCP_Remote_Script.registry import command

# Read command (default timeout 10s):
@command("command_name")
def _command_name(self, params=None): ...

# Write command (timeout 15s):
@command("command_name", write=True)
def _command_name(self, params): ...
```

From AbletonMCP_Remote_Script/handlers/transport.py (extend this class):
```python
class TransportHandlers:
    # All methods use self._song for Song object access
    # Pattern: validate params, call LOM API, return dict
```

From AbletonMCP_Remote_Script/handlers/scenes.py (extend this class):
```python
class SceneHandlers:
    # Uses self._song.scenes for scene access
    # Pattern: validate index, perform action, return result dict
```

From MCP_Server/tools/transport.py (follow this pattern):
```python
from mcp.server.fastmcp import Context
from MCP_Server.connection import format_error, get_ableton_connection
from MCP_Server.server import mcp

@mcp.tool()
def tool_name(ctx: Context, param: type) -> str:
    try:
        ableton = get_ableton_connection()
        result = ableton.send_command("command_name", {"param": param})
        return json.dumps(result, indent=2)
    except Exception as e:
        return format_error("Failed to ...", detail=str(e), suggestion="...")
```

From MCP_Server/connection.py:
```python
_WRITE_COMMANDS = frozenset([...])  # Add new write commands here
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remote Script handlers for Song-level gaps</name>
  <read_first>
    - AbletonMCP_Remote_Script/handlers/transport.py
    - AbletonMCP_Remote_Script/handlers/scenes.py
    - AbletonMCP_Remote_Script/handlers/base.py
    - AbletonMCP_Remote_Script/registry.py
    - .planning/phases/11-check-for-live-object-model-gaps/11-GAP-REPORT.md
    - .planning/phases/12-fill-in-missing-gaps/12-CONTEXT.md
  </read_first>
  <files>
    AbletonMCP_Remote_Script/handlers/transport.py,
    AbletonMCP_Remote_Script/handlers/scenes.py
  </files>
  <action>
Extend TransportHandlers class in transport.py with these new command handlers, following the exact existing pattern (try/except, self._song, return dict):

**Scale & Key (4 commands):**
1. `@command("get_scale_info")` → `_get_scale_info(self, params=None)`:
   - Returns `{"root_note": self._song.root_note, "scale_name": self._song.scale_name, "scale_intervals": list(self._song.scale_intervals), "scale_mode": self._song.scale_mode}`
   - root_note is int 0-11 (C=0, C#=1...B=11)

2. `@command("set_scale", write=True)` → `_set_scale(self, params)`:
   - Accepts optional params: root_note (int 0-11), scale_name (str), scale_mode (int)
   - Validate root_note in range 0-11 if provided
   - Only set provided values (conditional dict building pattern)
   - Returns same fields as get_scale_info after setting

**Cue Points (3 commands):**
3. `@command("get_cue_points")` → `_get_cue_points(self, params=None)`:
   - Returns `{"cue_points": [{"name": cp.name, "time": cp.time} for cp in self._song.cue_points]}`

4. `@command("set_or_delete_cue", write=True)` → `_set_or_delete_cue(self, params=None)`:
   - Calls `self._song.set_or_delete_cue()` (toggles cue at current time, no params)
   - Returns `{"toggled": True, "position": self._song.current_song_time}`

5. `@command("jump_to_cue", write=True)` → `_jump_to_cue(self, params)`:
   - Accepts `direction` param: "next" or "prev"
   - Optionally accepts `index` param (int) to jump to specific cue by index
   - If index provided: `self._song.cue_points[index].jump()`
   - If direction == "next": `self._song.jump_to_next_cue()`
   - If direction == "prev": `self._song.jump_to_prev_cue()`
   - Returns `{"jumped": True, "direction": direction, "position": self._song.current_song_time}`

**Capture (2 commands):**
6. `@command("capture_scene", write=True)` → `_capture_scene(self, params=None)`:
   - Calls `self._song.capture_and_insert_scene()`
   - Returns `{"captured": True, "scene_count": len(self._song.scenes)}`

7. `@command("capture_midi", write=True)` → `_capture_midi(self, params=None)`:
   - Calls `self._song.capture_midi()`
   - Returns `{"captured": True}`

**Session Controls (6 commands):**
8. `@command("tap_tempo", write=True)` → `_tap_tempo(self, params=None)`:
   - Calls `self._song.tap_tempo()`
   - Returns `{"tapped": True, "tempo": self._song.tempo}`

9. `@command("set_metronome", write=True)` → `_set_metronome(self, params)`:
   - Accepts `enabled` (bool)
   - Sets `self._song.metronome = enabled`
   - Returns `{"metronome": self._song.metronome}`

10. `@command("set_groove_amount", write=True)` → `_set_groove_amount(self, params)`:
    - Accepts `amount` (float 0.0-1.0), validate range
    - Sets `self._song.groove_amount = amount`
    - Returns `{"groove_amount": self._song.groove_amount}`

11. `@command("set_swing_amount", write=True)` → `_set_swing_amount(self, params)`:
    - Accepts `amount` (float 0.0-1.0), validate range
    - Sets `self._song.swing_amount = amount`
    - Returns `{"swing_amount": self._song.swing_amount}`

12. `@command("set_clip_trigger_quantization", write=True)` → `_set_clip_trigger_quantization(self, params)`:
    - Accepts `quantization` (int 0-14)
    - Validate range
    - Sets `self._song.clip_trigger_quantization = quantization`
    - Returns `{"clip_trigger_quantization": self._song.clip_trigger_quantization}`

13. `@command("set_session_record", write=True)` → `_set_session_record(self, params)`:
    - Accepts `enabled` (bool) and optional `record_mode` (bool)
    - Sets `self._song.session_record = enabled`
    - If record_mode provided: `self._song.record_mode = record_mode`
    - Returns `{"session_record": self._song.session_record, "record_mode": self._song.record_mode}`

**Navigation (3 commands):**
14. `@command("jump_by", write=True)` → `_jump_by(self, params)`:
    - Accepts `beats` (float, positive or negative)
    - Calls `self._song.jump_by(beats)`
    - Returns `{"jumped": True, "beats": beats, "position": self._song.current_song_time}`

15. `@command("play_selection", write=True)` → `_play_selection(self, params=None)`:
    - Calls `self._song.play_selection()`
    - Returns `{"playing_selection": True}`

16. `@command("get_song_length")` → `_get_song_length(self, params=None)`:
    - Returns `{"song_length": self._song.song_length, "last_event_time": self._song.last_event_time}`

**In scenes.py, extend SceneHandlers:**
17. `@command("duplicate_scene", write=True)` → `_duplicate_scene(self, params)`:
    - Accepts `scene_index` (int)
    - Validate index in range
    - Calls `self._song.duplicate_scene(scene_index)`
    - Returns `{"duplicated": True, "source_index": scene_index, "scene_count": len(self._song.scenes)}`
  </action>
  <verify>
    <automated>cd /home/user/ableton-mcp && python -c "
import sys
sys.modules['_Framework'] = type(sys)('_Framework')
sys.modules['_Framework.ControlSurface'] = type(sys)('_Framework.ControlSurface')
sys.modules['_Framework.ControlSurface'].ControlSurface = type('ControlSurface', (), {})
from AbletonMCP_Remote_Script.registry import CommandRegistry
import AbletonMCP_Remote_Script.handlers
registered = {e[0] for e in CommandRegistry._entries}
new_cmds = {'get_scale_info','set_scale','get_cue_points','set_or_delete_cue','jump_to_cue','capture_scene','capture_midi','tap_tempo','set_metronome','set_groove_amount','set_swing_amount','set_clip_trigger_quantization','set_session_record','jump_by','play_selection','get_song_length','duplicate_scene'}
missing = new_cmds - registered
assert not missing, f'Missing commands: {missing}'
print(f'OK: all {len(new_cmds)} new commands registered, total={len(registered)}')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - transport.py contains `def _get_scale_info(self`
    - transport.py contains `def _set_scale(self`
    - transport.py contains `def _get_cue_points(self`
    - transport.py contains `def _set_or_delete_cue(self`
    - transport.py contains `def _jump_to_cue(self`
    - transport.py contains `def _capture_scene(self`
    - transport.py contains `def _capture_midi(self`
    - transport.py contains `def _tap_tempo(self`
    - transport.py contains `def _set_metronome(self`
    - transport.py contains `def _set_groove_amount(self`
    - transport.py contains `def _set_swing_amount(self`
    - transport.py contains `def _set_clip_trigger_quantization(self`
    - transport.py contains `def _set_session_record(self`
    - transport.py contains `def _jump_by(self`
    - transport.py contains `def _play_selection(self`
    - transport.py contains `def _get_song_length(self`
    - scenes.py contains `def _duplicate_scene(self`
    - All 17 commands use `@command(` decorator
    - Verify command exits 0 showing all 17 new commands registered
  </acceptance_criteria>
  <done>17 new Remote Script command handlers registered: 16 in transport.py, 1 in scenes.py</done>
</task>

<task type="auto">
  <name>Task 2: MCP tools, smoke tests, and connection wiring for Song-level gaps</name>
  <read_first>
    - MCP_Server/tools/transport.py
    - MCP_Server/tools/scenes.py
    - MCP_Server/connection.py
    - tests/conftest.py
    - tests/test_transport.py
    - tests/test_scenes.py
    - MCP_Server/tools/__init__.py
  </read_first>
  <files>
    MCP_Server/tools/transport.py,
    MCP_Server/tools/scenes.py,
    MCP_Server/connection.py,
    tests/conftest.py,
    tests/test_transport.py,
    tests/test_scenes.py
  </files>
  <action>
**MCP Tools in transport.py** — Add these @mcp.tool() functions following the exact existing pattern (get_ableton_connection, send_command, json.dumps, format_error):

1. `get_scale_info(ctx: Context) -> str` — calls send_command("get_scale_info"), returns JSON with root_note, scale_name, scale_intervals, scale_mode

2. `set_scale(ctx: Context, root_note: int | None = None, scale_name: str | None = None, scale_mode: int | None = None) -> str` — conditional param building (only non-None), calls send_command("set_scale", params)

3. `get_cue_points(ctx: Context) -> str` — calls send_command("get_cue_points")

4. `set_or_delete_cue(ctx: Context) -> str` — calls send_command("set_or_delete_cue"), docstring: "Toggle a cue point at the current playback position"

5. `jump_to_cue(ctx: Context, direction: str = "next", index: int | None = None) -> str` — conditional param building, calls send_command("jump_to_cue", params)

6. `capture_scene(ctx: Context) -> str` — calls send_command("capture_scene"), docstring: "Capture currently playing clips as a new scene"

7. `capture_midi(ctx: Context) -> str` — calls send_command("capture_midi"), docstring: "Capture recently played MIDI input"

8. `tap_tempo(ctx: Context) -> str` — calls send_command("tap_tempo")

9. `set_metronome(ctx: Context, enabled: bool) -> str` — calls send_command("set_metronome", {"enabled": enabled})

10. `set_groove_amount(ctx: Context, amount: float) -> str` — calls send_command("set_groove_amount", {"amount": amount})

11. `set_swing_amount(ctx: Context, amount: float) -> str` — calls send_command("set_swing_amount", {"amount": amount})

12. `set_clip_trigger_quantization(ctx: Context, quantization: int) -> str` — calls send_command("set_clip_trigger_quantization", {"quantization": quantization})

13. `set_session_record(ctx: Context, enabled: bool, record_mode: bool | None = None) -> str` — conditional param building

14. `jump_by(ctx: Context, beats: float) -> str` — calls send_command("jump_by", {"beats": beats})

15. `play_selection(ctx: Context) -> str` — calls send_command("play_selection")

16. `get_song_length(ctx: Context) -> str` — calls send_command("get_song_length")

**MCP Tool in scenes.py:**
17. `duplicate_scene(ctx: Context, scene_index: int) -> str` — calls send_command("duplicate_scene", {"scene_index": scene_index})

**connection.py** — Add these to `_WRITE_COMMANDS` frozenset (after the `# Phase 10` comment, add `# Phase 12: Song Gaps` comment):
```
"set_scale",
"set_or_delete_cue",
"jump_to_cue",
"capture_scene",
"capture_midi",
"tap_tempo",
"set_metronome",
"set_groove_amount",
"set_swing_amount",
"set_clip_trigger_quantization",
"set_session_record",
"jump_by",
"play_selection",
"duplicate_scene",
```

**tests/test_transport.py** — Add smoke tests following existing pattern (async def, mcp_server + mock_connection fixtures, result[0][0].text):
- `test_song_gap_tools_registered` — verify all 16 new transport tool names in mcp_server.list_tools()
- `test_get_scale_info_calls_send_command` — mock returns {"root_note": 0, "scale_name": "Major"}, verify send_command called with "get_scale_info"
- `test_set_scale_calls_send_command` — call with root_note=2, verify send_command params
- `test_get_cue_points_calls_send_command` — mock returns {"cue_points": []}, verify
- `test_capture_scene_calls_send_command` — mock returns {"captured": True}, verify
- `test_tap_tempo_calls_send_command` — mock returns {"tapped": True, "tempo": 120}, verify
- `test_set_metronome_calls_send_command` — call with enabled=True, verify params
- `test_jump_by_calls_send_command` — call with beats=4.0, verify params
- `test_get_song_length_calls_send_command` — mock returns {"song_length": 128.0}, verify

**tests/test_scenes.py** — Add smoke test:
- `test_duplicate_scene_calls_send_command` — call with scene_index=0, verify send_command("duplicate_scene", {"scene_index": 0})

**tests/conftest.py** — No changes needed (tools are in existing modules already patched).
  </action>
  <verify>
    <automated>cd /home/user/ableton-mcp && python -m pytest tests/test_transport.py tests/test_scenes.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - MCP_Server/tools/transport.py contains `def get_scale_info(`
    - MCP_Server/tools/transport.py contains `def set_scale(`
    - MCP_Server/tools/transport.py contains `def get_cue_points(`
    - MCP_Server/tools/transport.py contains `def capture_scene(`
    - MCP_Server/tools/transport.py contains `def capture_midi(`
    - MCP_Server/tools/transport.py contains `def tap_tempo(`
    - MCP_Server/tools/transport.py contains `def set_metronome(`
    - MCP_Server/tools/transport.py contains `def jump_by(`
    - MCP_Server/tools/transport.py contains `def get_song_length(`
    - MCP_Server/tools/scenes.py contains `def duplicate_scene(`
    - connection.py contains `"set_scale"` in _WRITE_COMMANDS
    - connection.py contains `"duplicate_scene"` in _WRITE_COMMANDS
    - All transport and scenes tests pass
  </acceptance_criteria>
  <done>16 new MCP transport tools + 1 scene tool registered, smoke tests pass, connection.py write commands updated</done>
</task>

</tasks>

<verification>
- All 17 new commands registered in CommandRegistry
- All 17 new MCP tools registered in FastMCP server
- All smoke tests pass: `python -m pytest tests/test_transport.py tests/test_scenes.py -x -q`
- Write commands added to connection.py _WRITE_COMMANDS
</verification>

<success_criteria>
- 17 new Song-level commands implemented in Remote Script handlers
- 17 new MCP tools accessible via FastMCP
- Scale/key info readable and settable
- Cue point management functional
- Capture workflows callable
- Session controls (metronome, groove, swing, trigger quant, record) settable
- Navigation (jump_by, play_selection, song_length) functional
- Duplicate scene functional
- All smoke tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-fill-in-missing-gaps/12-01-SUMMARY.md`
</output>
