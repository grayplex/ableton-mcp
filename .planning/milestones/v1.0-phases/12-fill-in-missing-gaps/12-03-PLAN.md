---
phase: 12-fill-in-missing-gaps
plan: 03
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - AbletonMCP_Remote_Script/handlers/clips.py
  - AbletonMCP_Remote_Script/handlers/notes.py
  - AbletonMCP_Remote_Script/handlers/audio_clips.py
  - MCP_Server/tools/clips.py
  - MCP_Server/tools/notes.py
  - MCP_Server/tools/audio_clips.py
  - MCP_Server/connection.py
  - tests/test_clips.py
  - tests/test_notes.py
  - tests/test_audio_clips.py
  - tests/test_registry.py
autonomous: true
requirements:
  - CLNC-01
  - CLNC-02
  - CLNC-03
  - CLNC-04
  - CLNC-05
  - NOTE-01
  - NOTE-02
  - NOTE-03
  - NOTE-04
  - NOTE-05
  - NOTE-06
  - NOTE-07
  - ACRT-03

must_haves:
  truths:
    - "User can get/set clip launch mode, launch quantization, legato, velocity amount"
    - "User can get/set clip muted state (clip activator)"
    - "User can crop a clip, duplicate its loop, or duplicate a region"
    - "User can get/add/move/remove warp markers on audio clips"
    - "User can apply note modifications in-place by note ID"
    - "User can select/deselect notes and get selected notes"
    - "User can get/remove/duplicate notes by ID"
    - "User can use native quantize with swing support"
    - "Registry test passes with updated total command count"
  artifacts:
    - path: "AbletonMCP_Remote_Script/handlers/clips.py"
      provides: "Extended clip handlers with launch settings, state, editing"
      contains: "def _set_clip_launch_settings"
    - path: "AbletonMCP_Remote_Script/handlers/notes.py"
      provides: "Extended note handlers with ID ops, modifications, selection, native quantize"
      contains: "def _apply_note_modifications"
    - path: "AbletonMCP_Remote_Script/handlers/audio_clips.py"
      provides: "Extended audio clip handlers with warp marker operations"
      contains: "def _get_warp_markers"
    - path: "tests/test_registry.py"
      provides: "Updated registry test with correct command count"
      contains: "assert len(registered) =="
  key_links:
    - from: "MCP_Server/tools/clips.py"
      to: "AbletonMCP_Remote_Script/handlers/clips.py"
      via: "send_command"
      pattern: "send_command.*set_clip_launch_settings"
    - from: "MCP_Server/tools/notes.py"
      to: "AbletonMCP_Remote_Script/handlers/notes.py"
      via: "send_command"
      pattern: "send_command.*apply_note_modifications"
    - from: "MCP_Server/tools/audio_clips.py"
      to: "AbletonMCP_Remote_Script/handlers/audio_clips.py"
      via: "send_command"
      pattern: "send_command.*get_warp_markers"
---

<objective>
Implement Clip + Note Expression gaps: clip launch settings, clip state/editing, warp markers, note operations (apply_modifications, ID ops, selection, native quantize). Update registry test to final count.

Purpose: Complete note manipulation and clip control capabilities. Warp markers enable audio timing control. Note ID ops enable precise in-place editing.
Output: Extended clips/notes/audio_clips handlers + MCP tools + smoke tests, ~19 commands total.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-fill-in-missing-gaps/12-CONTEXT.md
@.planning/phases/12-fill-in-missing-gaps/12-RESEARCH.md
@.planning/phases/11-check-for-live-object-model-gaps/11-GAP-REPORT.md
@.planning/phases/12-fill-in-missing-gaps/12-01-SUMMARY.md
@.planning/phases/12-fill-in-missing-gaps/12-02-SUMMARY.md

<interfaces>
<!-- Existing patterns the executor must follow -->

From AbletonMCP_Remote_Script/handlers/clips.py:
```python
def _resolve_clip_slot(song, track_index, clip_index):
    """Returns (clip_slot, track) tuple."""

def _clip_info_dict(clip):
    """Build standard clip info dictionary."""

class ClipHandlers:
    # All methods use self._song, _resolve_clip_slot for clip access
```

From AbletonMCP_Remote_Script/handlers/notes.py:
```python
class NoteHandlers:
    # Uses _resolve_clip_slot, Live.Clip imported inside method bodies
    # spec_kwargs pattern for MidiNoteSpecification
    # hasattr guard for Live 11+ note fields
```

From AbletonMCP_Remote_Script/handlers/audio_clips.py:
```python
class AudioClipHandlers:
    # Uses _resolve_clip_slot, validates is_audio_clip before operations
    # Returns before/after for changed properties
```

From tests/test_registry.py:
```python
assert len(registered) == 69  # Current count — will need updating
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remote Script handlers for Clip + Note gaps</name>
  <read_first>
    - AbletonMCP_Remote_Script/handlers/clips.py
    - AbletonMCP_Remote_Script/handlers/notes.py
    - AbletonMCP_Remote_Script/handlers/audio_clips.py
    - AbletonMCP_Remote_Script/registry.py
    - .planning/phases/12-fill-in-missing-gaps/12-RESEARCH.md
  </read_first>
  <files>
    AbletonMCP_Remote_Script/handlers/clips.py,
    AbletonMCP_Remote_Script/handlers/notes.py,
    AbletonMCP_Remote_Script/handlers/audio_clips.py
  </files>
  <action>
**Extend ClipHandlers in clips.py — add 6 new commands:**

1. `@command("set_clip_launch_settings", write=True)` → `_set_clip_launch_settings(self, params)`:
   - Accepts: track_index, clip_index, and optional: launch_mode (int 0-3), launch_quantization (int 0-14), legato (bool), velocity_amount (float 0.0-1.0)
   - Resolve clip via _resolve_clip_slot, validate has_clip
   - For each provided param, validate range and set on clip:
     - `clip.launch_mode = launch_mode` (0=Trigger, 1=Gate, 2=Toggle, 3=Repeat)
     - `clip.launch_quantization = launch_quantization`
     - `clip.legato = legato`
     - `clip.velocity_amount = velocity_amount`
   - Use conditional param building pattern (only set non-None values)
   - Returns dict with all current values: `{"launch_mode": clip.launch_mode, "launch_quantization": clip.launch_quantization, "legato": clip.legato, "velocity_amount": clip.velocity_amount}`

2. `@command("get_clip_launch_settings")` → `_get_clip_launch_settings(self, params)`:
   - Accepts: track_index, clip_index
   - Returns same fields as set_clip_launch_settings response

3. `@command("set_clip_muted", write=True)` → `_set_clip_muted(self, params)`:
   - Accepts: track_index, clip_index, muted (bool)
   - Sets `clip.muted = muted`
   - Returns `{"clip_name": clip.name, "muted": clip.muted}`

4. `@command("crop_clip", write=True)` → `_crop_clip(self, params)`:
   - Accepts: track_index, clip_index
   - Calls `clip.crop()` (no params, crops to loop/markers)
   - Returns `{"cropped": True, "clip_name": clip.name, "length": clip.length}`

5. `@command("duplicate_clip_loop", write=True)` → `_duplicate_clip_loop(self, params)`:
   - Accepts: track_index, clip_index
   - Calls `clip.duplicate_loop()` (doubles loop length + duplicates content)
   - Returns `{"duplicated": True, "clip_name": clip.name, "length": clip.length, "loop_end": clip.loop_end}`

6. `@command("duplicate_clip_region", write=True)` → `_duplicate_clip_region(self, params)`:
   - Accepts: track_index, clip_index, region_start (float), region_end (float), destination_time (float)
   - Calls `clip.duplicate_region(region_start, region_end, destination_time)`
   - Returns `{"duplicated": True, "clip_name": clip.name, "region_start": region_start, "region_end": region_end, "destination_time": destination_time}`

**Extend NoteHandlers in notes.py — add 9 new commands:**

7. `@command("apply_note_modifications", write=True)` → `_apply_note_modifications(self, params)`:
   - `import Live.Clip` inside method body
   - Accepts: track_index, clip_index, notes (list of dicts with note_id required, plus optional pitch, start_time, duration, velocity, mute, probability, velocity_deviation, release_velocity)
   - Resolve clip, validate has_clip
   - Build list of MidiNoteSpecification objects (like add_notes but with note_id set)
   - For each note dict: `spec_kwargs = {"note_id": note["note_id"]}`, add all provided fields
   - Create `Live.Clip.MidiNoteSpecification(**spec_kwargs)` for each
   - Call `clip.apply_note_modifications(note_specs)`
   - Returns `{"modified": True, "count": len(notes)}`

8. `@command("select_all_notes", write=True)` → `_select_all_notes(self, params)`:
   - Accepts: track_index, clip_index
   - Calls `clip.select_all_notes()`
   - Returns `{"selected": True}`

9. `@command("deselect_all_notes", write=True)` → `_deselect_all_notes(self, params)`:
   - Accepts: track_index, clip_index
   - Calls `clip.deselect_all_notes()`
   - Returns `{"deselected": True}`

10. `@command("select_notes_by_id", write=True)` → `_select_notes_by_id(self, params)`:
    - Accepts: track_index, clip_index, note_ids (list of int)
    - Calls `clip.select_notes_by_id(note_ids)`
    - Returns `{"selected": True, "count": len(note_ids)}`

11. `@command("get_notes_by_id")` → `_get_notes_by_id(self, params)`:
    - Accepts: track_index, clip_index, note_ids (list of int)
    - Calls `clip.get_notes_by_id(note_ids)`
    - Build note dicts like existing get_notes handler (pitch, start_time, duration, velocity, mute + hasattr guards for note_id, probability, velocity_deviation, release_velocity)
    - Returns `{"notes": [...]}`

12. `@command("remove_notes_by_id", write=True)` → `_remove_notes_by_id(self, params)`:
    - Accepts: track_index, clip_index, note_ids (list of int)
    - Calls `clip.remove_notes_by_id(note_ids)`
    - Returns `{"removed": True, "count": len(note_ids)}`

13. `@command("duplicate_notes_by_id", write=True)` → `_duplicate_notes_by_id(self, params)`:
    - Accepts: track_index, clip_index, note_ids (list of int)
    - Calls `clip.duplicate_notes_by_id(note_ids)`
    - Returns `{"duplicated": True, "count": len(note_ids)}`

14. `@command("get_selected_notes")` → `_get_selected_notes(self, params)`:
    - Accepts: track_index, clip_index
    - Calls `clip.get_selected_notes_extended(from_time=0, time_span=2147483647, from_pitch=0, pitch_span=128)`
    - Build note dicts same as get_notes pattern
    - Returns `{"notes": [...]}`

15. `@command("native_quantize", write=True)` → `_native_quantize(self, params)`:
    - Accepts: track_index, clip_index, grid (float, e.g. 0.5 for 1/8), amount (float 0.0-1.0), pitch (int or None)
    - If pitch provided: `clip.quantize_pitch(pitch, grid, amount)`
    - Else: `clip.quantize(grid, amount)`
    - Returns `{"quantized": True, "grid": grid, "amount": amount}`
    - Note: This is DIFFERENT from existing "quantize_notes" which is our manual implementation. This uses the native Clip.quantize which respects Song.swing_amount.

**Extend AudioClipHandlers in audio_clips.py — add 4 new commands:**

16. `@command("get_warp_markers")` → `_get_warp_markers(self, params)`:
    - Accepts: track_index, clip_index
    - Resolve clip, validate is_audio_clip and clip.warping is True
    - Iterate `clip.warp_markers`: build list of `{"sample_time": wm.sample_time, "beat_time": wm.beat_time}`
    - Returns `{"clip_name": clip.name, "warp_markers": [...]}`

17. `@command("insert_warp_marker", write=True)` → `_insert_warp_marker(self, params)`:
    - Accepts: track_index, clip_index, beat_time (float), sample_time (float)
    - Validate is_audio_clip and clip.warping
    - Call `clip.insert_warp_marker(beat_time, sample_time)`
    - Returns `{"inserted": True, "beat_time": beat_time, "sample_time": sample_time}`

18. `@command("move_warp_marker", write=True)` → `_move_warp_marker(self, params)`:
    - Accepts: track_index, clip_index, marker_index (int), new_beat_time (float)
    - Validate is_audio_clip and warping
    - Get markers list, validate index
    - Call `clip.move_warp_marker(marker_index, new_beat_time)` (Note: check LOM -- may need old_beat_time instead of index)
    - Returns `{"moved": True, "marker_index": marker_index, "new_beat_time": new_beat_time}`

19. `@command("remove_warp_marker", write=True)` → `_remove_warp_marker(self, params)`:
    - Accepts: track_index, clip_index, marker_index (int)
    - Validate is_audio_clip and warping
    - Get markers list, validate index
    - Call `clip.remove_warp_marker(marker_index)` (Note: check LOM for exact API)
    - Returns `{"removed": True, "marker_index": marker_index}`
  </action>
  <verify>
    <automated>cd /home/user/ableton-mcp && python -c "
import sys
sys.modules['_Framework'] = type(sys)('_Framework')
sys.modules['_Framework.ControlSurface'] = type(sys)('_Framework.ControlSurface')
sys.modules['_Framework.ControlSurface'].ControlSurface = type('ControlSurface', (), {})
from AbletonMCP_Remote_Script.registry import CommandRegistry
import AbletonMCP_Remote_Script.handlers
registered = {e[0] for e in CommandRegistry._entries}
new_cmds = {'set_clip_launch_settings','get_clip_launch_settings','set_clip_muted','crop_clip','duplicate_clip_loop','duplicate_clip_region','apply_note_modifications','select_all_notes','deselect_all_notes','select_notes_by_id','get_notes_by_id','remove_notes_by_id','duplicate_notes_by_id','get_selected_notes','native_quantize','get_warp_markers','insert_warp_marker','move_warp_marker','remove_warp_marker'}
missing = new_cmds - registered
assert not missing, f'Missing commands: {missing}'
print(f'OK: all {len(new_cmds)} new commands registered, total={len(registered)}')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - clips.py contains `def _set_clip_launch_settings(self`
    - clips.py contains `def _get_clip_launch_settings(self`
    - clips.py contains `def _set_clip_muted(self`
    - clips.py contains `def _crop_clip(self`
    - clips.py contains `def _duplicate_clip_loop(self`
    - clips.py contains `def _duplicate_clip_region(self`
    - notes.py contains `def _apply_note_modifications(self`
    - notes.py contains `def _select_all_notes(self`
    - notes.py contains `def _select_notes_by_id(self`
    - notes.py contains `def _get_notes_by_id(self`
    - notes.py contains `def _remove_notes_by_id(self`
    - notes.py contains `def _duplicate_notes_by_id(self`
    - notes.py contains `def _get_selected_notes(self`
    - notes.py contains `def _native_quantize(self`
    - audio_clips.py contains `def _get_warp_markers(self`
    - audio_clips.py contains `def _insert_warp_marker(self`
    - audio_clips.py contains `def _move_warp_marker(self`
    - audio_clips.py contains `def _remove_warp_marker(self`
    - Verify command exits 0 showing all 19 new commands registered
  </acceptance_criteria>
  <done>19 new Remote Script commands: 6 clip, 9 note, 4 warp marker handlers registered</done>
</task>

<task type="auto">
  <name>Task 2: MCP tools, smoke tests, connection wiring, and registry test update</name>
  <read_first>
    - MCP_Server/tools/clips.py
    - MCP_Server/tools/notes.py
    - MCP_Server/tools/audio_clips.py
    - MCP_Server/connection.py
    - tests/test_clips.py
    - tests/test_notes.py
    - tests/test_audio_clips.py
    - tests/test_registry.py
  </read_first>
  <files>
    MCP_Server/tools/clips.py,
    MCP_Server/tools/notes.py,
    MCP_Server/tools/audio_clips.py,
    MCP_Server/connection.py,
    tests/test_clips.py,
    tests/test_notes.py,
    tests/test_audio_clips.py,
    tests/test_registry.py
  </files>
  <action>
**MCP Tools in clips.py** — Add @mcp.tool() functions:

1. `set_clip_launch_settings(ctx: Context, track_index: int, clip_index: int, launch_mode: int | None = None, launch_quantization: int | None = None, legato: bool | None = None, velocity_amount: float | None = None) -> str`
   - Conditional param building, send_command("set_clip_launch_settings", params)
   - Docstring includes launch_mode values: "0=Trigger, 1=Gate, 2=Toggle, 3=Repeat"

2. `get_clip_launch_settings(ctx: Context, track_index: int, clip_index: int) -> str`

3. `set_clip_muted(ctx: Context, track_index: int, clip_index: int, muted: bool) -> str`
   - Docstring: "Set clip muted/active state (clip activator). Different from track mute."

4. `crop_clip(ctx: Context, track_index: int, clip_index: int) -> str`

5. `duplicate_clip_loop(ctx: Context, track_index: int, clip_index: int) -> str`
   - Docstring: "Double the loop length and duplicate the content."

6. `duplicate_clip_region(ctx: Context, track_index: int, clip_index: int, region_start: float, region_end: float, destination_time: float) -> str`

**MCP Tools in notes.py** — Add @mcp.tool() functions:

7. `apply_note_modifications(ctx: Context, track_index: int, clip_index: int, notes: str) -> str`
   - `notes` param is JSON string (MCP limitation for complex types). Parse with json.loads.
   - Each note dict must have `note_id`, plus optional pitch/start_time/duration/velocity/mute/probability/velocity_deviation/release_velocity
   - Docstring: "Modify notes in-place by ID. More efficient than remove+re-add. The notes parameter is a JSON string: [{\"note_id\": 1, \"velocity\": 100}, ...]"

8. `select_all_notes(ctx: Context, track_index: int, clip_index: int) -> str`

9. `deselect_all_notes(ctx: Context, track_index: int, clip_index: int) -> str`

10. `select_notes_by_id(ctx: Context, track_index: int, clip_index: int, note_ids: str) -> str`
    - `note_ids` is comma-separated string (MCP simple type limitation, same pattern as track_indices)
    - Parse: `[int(x.strip()) for x in note_ids.split(",")]`

11. `get_notes_by_id(ctx: Context, track_index: int, clip_index: int, note_ids: str) -> str`
    - Same comma-separated pattern

12. `remove_notes_by_id(ctx: Context, track_index: int, clip_index: int, note_ids: str) -> str`

13. `duplicate_notes_by_id(ctx: Context, track_index: int, clip_index: int, note_ids: str) -> str`

14. `get_selected_notes(ctx: Context, track_index: int, clip_index: int) -> str`

15. `native_quantize(ctx: Context, track_index: int, clip_index: int, grid: float, amount: float, pitch: int | None = None) -> str`
    - Docstring: "Quantize notes using native Ableton quantize (respects Song.swing_amount). Grid: e.g. 0.5 for 1/8 notes."

**MCP Tools in audio_clips.py** — Add @mcp.tool() functions:

16. `get_warp_markers(ctx: Context, track_index: int, clip_index: int) -> str`

17. `insert_warp_marker(ctx: Context, track_index: int, clip_index: int, beat_time: float, sample_time: float) -> str`

18. `move_warp_marker(ctx: Context, track_index: int, clip_index: int, marker_index: int, new_beat_time: float) -> str`

19. `remove_warp_marker(ctx: Context, track_index: int, clip_index: int, marker_index: int) -> str`

**Update `MCP_Server/connection.py` `_WRITE_COMMANDS`:**
Add after the Phase 12 Track + Arrangement block:
```
# Phase 12: Clip + Note Gaps
"set_clip_launch_settings",
"set_clip_muted",
"crop_clip",
"duplicate_clip_loop",
"duplicate_clip_region",
"apply_note_modifications",
"select_all_notes",
"deselect_all_notes",
"select_notes_by_id",
"remove_notes_by_id",
"duplicate_notes_by_id",
"native_quantize",
"insert_warp_marker",
"move_warp_marker",
"remove_warp_marker",
```

**Smoke Tests:**

Add to `tests/test_clips.py`:
- `test_clip_gap_tools_registered` — verify set_clip_launch_settings, get_clip_launch_settings, set_clip_muted, crop_clip, duplicate_clip_loop, duplicate_clip_region in tool names
- `test_set_clip_launch_settings_calls_send_command` — call with launch_mode=0, verify send_command params include "launch_mode": 0
- `test_set_clip_muted_calls_send_command` — call with muted=True, verify
- `test_crop_clip_calls_send_command` — mock return {"cropped": True}, verify

Add to `tests/test_notes.py`:
- `test_note_gap_tools_registered` — verify apply_note_modifications, select_all_notes, deselect_all_notes, select_notes_by_id, get_notes_by_id, remove_notes_by_id, duplicate_notes_by_id, get_selected_notes, native_quantize in tool names
- `test_apply_note_modifications_calls_send_command` — call with notes='[{"note_id": 1, "velocity": 80}]', verify send_command params
- `test_native_quantize_calls_send_command` — call with grid=0.5, amount=1.0, verify
- `test_get_notes_by_id_calls_send_command` — call with note_ids="1,2,3", verify parsed to list

Add to `tests/test_audio_clips.py`:
- `test_warp_marker_tools_registered` — verify get_warp_markers, insert_warp_marker, move_warp_marker, remove_warp_marker in tool names
- `test_get_warp_markers_calls_send_command` — mock return {"warp_markers": []}, verify
- `test_insert_warp_marker_calls_send_command` — call with beat_time=1.0, sample_time=44100.0, verify

**Update `tests/test_registry.py`:**
- Change `assert len(registered) == 69` to `assert len(registered) == 115` (69 original + 17 song + 10 track/arrangement + 19 clip/note = 115)
- Add all new command names to the `expected` set:
  - Song (17): get_scale_info, set_scale, get_cue_points, set_or_delete_cue, jump_to_cue, capture_scene, capture_midi, tap_tempo, set_metronome, set_groove_amount, set_swing_amount, set_clip_trigger_quantization, set_session_record, jump_by, play_selection, get_song_length, duplicate_scene
  - Track/Arrangement (10): create_arrangement_midi_clip, create_arrangement_audio_clip, get_arrangement_clips, duplicate_clip_to_arrangement, insert_device, move_device, stop_track_clips, get_track_freeze_state, get_input_routing_channels, get_output_routing_channels
  - Clip/Note (19): set_clip_launch_settings, get_clip_launch_settings, set_clip_muted, crop_clip, duplicate_clip_loop, duplicate_clip_region, apply_note_modifications, select_all_notes, deselect_all_notes, select_notes_by_id, get_notes_by_id, remove_notes_by_id, duplicate_notes_by_id, get_selected_notes, native_quantize, get_warp_markers, insert_warp_marker, move_warp_marker, remove_warp_marker

Count: 69 + 46 = 115 total commands.
  </action>
  <verify>
    <automated>cd /home/user/ableton-mcp && python -m pytest tests/test_clips.py tests/test_notes.py tests/test_audio_clips.py tests/test_registry.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - MCP_Server/tools/clips.py contains `def set_clip_launch_settings(`
    - MCP_Server/tools/clips.py contains `def set_clip_muted(`
    - MCP_Server/tools/clips.py contains `def crop_clip(`
    - MCP_Server/tools/notes.py contains `def apply_note_modifications(`
    - MCP_Server/tools/notes.py contains `def select_all_notes(`
    - MCP_Server/tools/notes.py contains `def get_notes_by_id(`
    - MCP_Server/tools/notes.py contains `def native_quantize(`
    - MCP_Server/tools/audio_clips.py contains `def get_warp_markers(`
    - MCP_Server/tools/audio_clips.py contains `def insert_warp_marker(`
    - connection.py contains `"set_clip_launch_settings"` in _WRITE_COMMANDS
    - connection.py contains `"apply_note_modifications"` in _WRITE_COMMANDS
    - connection.py contains `"insert_warp_marker"` in _WRITE_COMMANDS
    - test_registry.py contains `== 115`
    - All tests pass including registry test
  </acceptance_criteria>
  <done>19 new MCP tools (6 clip, 9 note, 4 warp), smoke tests pass, registry test updated to 115 commands</done>
</task>

</tasks>

<verification>
- All 19 new Clip+Note commands registered in CommandRegistry
- All 19 new MCP tools registered in FastMCP server
- Registry test passes with 115 total commands
- All smoke tests pass: `python -m pytest tests/test_clips.py tests/test_notes.py tests/test_audio_clips.py tests/test_registry.py -x -q`
- Full test suite passes: `python -m pytest tests/ -x -q`
</verification>

<success_criteria>
- Clip launch settings (mode, quantization, legato, velocity) get/set works
- Clip mute state settable
- Clip editing (crop, duplicate_loop, duplicate_region) works
- Warp markers readable and manipulable on audio clips
- Note modifications apply in-place by ID
- Note selection works (select_all, deselect_all, by ID)
- Note ID operations (get, remove, duplicate by ID) work
- Native quantize callable with swing support
- Registry test passes at 115 total commands
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/12-fill-in-missing-gaps/12-03-SUMMARY.md`
</output>
