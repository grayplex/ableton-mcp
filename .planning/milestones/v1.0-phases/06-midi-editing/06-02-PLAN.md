---
phase: 06-midi-editing
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - MCP_Server/tools/notes.py
  - MCP_Server/tools/clips.py
  - MCP_Server/tools/__init__.py
  - tests/conftest.py
  - tests/test_notes.py
autonomous: true
requirements: [MIDI-01, MIDI-02, MIDI-03, MIDI-04, MIDI-05]

must_haves:
  truths:
    - "All 5 note MCP tools are registered and discoverable via list_tools"
    - "add_notes_to_clip returns JSON with note_count and clip_name (not plain text f-string)"
    - "get_notes, remove_notes, quantize_notes, transpose_notes tools send correct params to send_command"
    - "remove_notes has optional pitch_min/pitch_max/start_time_min/start_time_max parameters"
    - "All note tool responses use json.dumps(result, indent=2) format"
  artifacts:
    - path: "MCP_Server/tools/notes.py"
      provides: "4 MCP tool functions: get_notes, remove_notes, quantize_notes, transpose_notes"
      exports: ["get_notes", "remove_notes", "quantize_notes", "transpose_notes"]
    - path: "MCP_Server/tools/clips.py"
      provides: "Updated add_notes_to_clip returning JSON"
      contains: "json.dumps"
    - path: "tests/test_notes.py"
      provides: "Smoke tests for all 5 note tools"
      min_lines: 50
  key_links:
    - from: "MCP_Server/tools/notes.py"
      to: "MCP_Server/connection.py"
      via: "get_ableton_connection().send_command()"
      pattern: "send_command"
    - from: "MCP_Server/tools/__init__.py"
      to: "MCP_Server/tools/notes.py"
      via: "import notes module"
      pattern: "notes"
    - from: "tests/conftest.py"
      to: "MCP_Server/tools/notes.py"
      via: "patch target for get_ableton_connection"
      pattern: "MCP_Server.tools.notes.get_ableton_connection"
---

<objective>
Create MCP tool definitions for all note operations, update add_notes_to_clip to return JSON, wire the new tools module, and add comprehensive smoke tests.

Purpose: Plan 01 created the Remote Script handlers. This plan exposes them as MCP tools that AI clients can discover and call, completing the MIDI editing feature surface.

Output: 4 new MCP tools in tools/notes.py, updated add_notes_to_clip in tools/clips.py, wiring in __init__.py and conftest.py, smoke tests in test_notes.py.
</objective>

<execution_context>
@C:/Users/ksthu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ksthu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-midi-editing/06-CONTEXT.md
@.planning/phases/06-midi-editing/06-RESEARCH.md
@.planning/phases/06-midi-editing/06-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From MCP_Server/server.py:
```python
from mcp.server.fastmcp import FastMCP
mcp = FastMCP("AbletonMCP")
# Import mcp from here: from MCP_Server.server import mcp
```

From MCP_Server/connection.py:
```python
def get_ableton_connection() -> AbletonConnection: ...
def format_error(message: str, detail: str = "", suggestion: str = "") -> str: ...
```

From MCP_Server/tools/clips.py (add_notes_to_clip -- to be updated):
```python
@mcp.tool()
def add_notes_to_clip(
    ctx: Context, track_index: int, clip_index: int, notes: list[dict[str, int | float | bool]]
) -> str:
    # Currently returns f-string, needs to return json.dumps(result, indent=2)
```

From MCP_Server/tools/__init__.py:
```python
from . import browser, clips, devices, mixer, session, tracks, transport  # noqa: F401
# Must add: notes
```

From tests/conftest.py:
```python
_GAC_PATCH_TARGETS = [
    "MCP_Server.connection.get_ableton_connection",
    "MCP_Server.tools.session.get_ableton_connection",
    # ... other targets
]
# Must add: "MCP_Server.tools.notes.get_ableton_connection"
```

Handler wire commands (from Plan 01):
- "add_notes_to_clip" -- params: {track_index, clip_index, notes}
- "get_notes" -- params: {track_index, clip_index}
- "remove_notes" -- params: {track_index, clip_index, pitch_min?, pitch_max?, start_time_min?, start_time_max?}
- "quantize_notes" -- params: {track_index, clip_index, grid_size?, strength?}
- "transpose_notes" -- params: {track_index, clip_index, semitones}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tools/notes.py, update add_notes_to_clip, and wire imports</name>
  <files>MCP_Server/tools/notes.py, MCP_Server/tools/clips.py, MCP_Server/tools/__init__.py, tests/conftest.py</files>
  <action>
1. Create MCP_Server/tools/notes.py with 4 MCP tool functions. Follow the exact pattern from tools/clips.py and tools/mixer.py:
   - Import json, Context from mcp.server.fastmcp, format_error and get_ableton_connection from connection, mcp from server
   - Each tool: @mcp.tool() decorator, ctx: Context first param, returns str

   a. `get_notes(ctx, track_index: int, clip_index: int) -> str`
      - Docstring: "Get all MIDI notes from a clip. Returns notes sorted by start_time then pitch, each with pitch, start_time, duration, velocity, and mute properties."
      - send_command("get_notes", {track_index, clip_index})
      - Return json.dumps(result, indent=2)
      - Error suggestion: "Verify clip exists with get_clip_info"

   b. `remove_notes(ctx, track_index: int, clip_index: int, pitch_min: int | None = None, pitch_max: int | None = None, start_time_min: float | None = None, start_time_max: float | None = None) -> str`
      - Docstring: "Remove MIDI notes from a clip within a time and pitch range. All range parameters are optional -- omit to remove all notes."
      - Build params dict conditionally (same pattern as set_clip_loop): base dict with track_index/clip_index, then conditionally add each optional param if not None
      - send_command("remove_notes", params)
      - Return json.dumps(result, indent=2)
      - Error suggestion: "Verify clip has notes with get_notes"

   c. `quantize_notes(ctx, track_index: int, clip_index: int, grid_size: float = 0.25, strength: float = 1.0) -> str`
      - Docstring: "Quantize all MIDI notes in a clip to a grid. Grid size is in beats: 0.25 = 1/16th, 0.5 = 1/8th, 1.0 = 1/4. Strength 0.0-1.0 controls how much notes snap to grid (1.0 = full snap)."
      - send_command("quantize_notes", {track_index, clip_index, grid_size, strength})
      - Return json.dumps(result, indent=2)
      - Error suggestion: "Verify clip has notes with get_notes"

   d. `transpose_notes(ctx, track_index: int, clip_index: int, semitones: int) -> str`
      - Docstring: "Transpose all MIDI notes in a clip by a number of semitones. Positive = up, negative = down. Errors if any note would go outside MIDI range (0-127)."
      - send_command("transpose_notes", {track_index, clip_index, semitones})
      - Return json.dumps(result, indent=2)
      - Error suggestion: "Check note ranges with get_notes before transposing. Notes must stay within 0-127."

2. Update MCP_Server/tools/clips.py -- modify add_notes_to_clip to return json.dumps(result, indent=2) instead of f-string. The handler now returns {"note_count": N, "clip_name": "..."} so just pass through with json.dumps.

3. Update MCP_Server/tools/__init__.py -- add notes to the import line:
   `from . import browser, clips, devices, mixer, notes, session, tracks, transport`

4. Update tests/conftest.py -- add "MCP_Server.tools.notes.get_ableton_connection" to _GAC_PATCH_TARGETS list.
  </action>
  <verify>
    <automated>cd I:/ableton-mcp && python -c "from MCP_Server.server import mcp; tools = [t.name for t in mcp._tool_manager.list_tools()]; print(f'Total tools: {len(tools)}'); note_tools = [t for t in tools if 'note' in t or t == 'get_notes']; print(f'Note tools: {note_tools}'); assert 'get_notes' in tools; assert 'remove_notes' in tools; assert 'quantize_notes' in tools; assert 'transpose_notes' in tools; print('All note tools registered')"</automated>
  </verify>
  <done>4 new MCP tools registered (get_notes, remove_notes, quantize_notes, transpose_notes). add_notes_to_clip returns JSON. tools/__init__.py imports notes module. conftest.py patches notes module's get_ableton_connection.</done>
</task>

<task type="auto">
  <name>Task 2: Create comprehensive smoke tests for all note tools</name>
  <files>tests/test_notes.py</files>
  <action>
Create tests/test_notes.py with smoke tests following the exact pattern from tests/test_clips.py and tests/test_mixer.py. Use pytest-asyncio with mcp_server and mock_connection fixtures.

Tests to implement:

1. `test_note_tools_registered(mcp_server)` -- verify all 5 note tools are registered: add_notes_to_clip, get_notes, remove_notes, quantize_notes, transpose_notes. Use [t.name for t in mcp_server._tool_manager.list_tools()].

2. `test_add_notes_returns_json(mcp_server, mock_connection)` -- mock send_command returns {"note_count": 3, "clip_name": "test"}. Call add_notes_to_clip with track_index=0, clip_index=0, notes=[{"pitch":60,"start_time":0,"duration":0.5,"velocity":100,"mute":False}]. Assert result is valid JSON with note_count. Use result[0][0].text pattern (MCP SDK 1.26.0 tuple return).

3. `test_get_notes_returns_json(mcp_server, mock_connection)` -- mock returns {"note_count": 2, "notes": [{"pitch":60,"start_time":0,"duration":0.5,"velocity":100,"mute":False}]}. Call get_notes with track_index=0, clip_index=0. Assert result is valid JSON with notes array.

4. `test_remove_notes_returns_json(mcp_server, mock_connection)` -- mock returns {"removed_count": 3}. Call remove_notes with track_index=0, clip_index=0. Assert JSON result with removed_count.

5. `test_remove_notes_optional_params(mcp_server, mock_connection)` -- mock returns {"removed_count": 1}. Call remove_notes with pitch_min=60, pitch_max=72. Verify send_command was called with params including pitch_min and pitch_max.

6. `test_quantize_notes_returns_json(mcp_server, mock_connection)` -- mock returns {"quantized_count": 4}. Call quantize_notes with grid_size=0.5, strength=0.8. Assert JSON result with quantized_count. Verify send_command params include grid_size and strength.

7. `test_transpose_notes_returns_json(mcp_server, mock_connection)` -- mock returns {"transposed_count": 4}. Call transpose_notes with semitones=7. Assert JSON result with transposed_count.

8. `test_transpose_notes_error(mcp_server, mock_connection)` -- mock send_command raises Exception("Transposing pitch 120 by 12..."). Call transpose_notes with semitones=12. Assert result contains "Error" (format_error output).

9. `test_add_notes_error(mcp_server, mock_connection)` -- mock raises Exception("Velocity 0 out of range"). Call add_notes_to_clip. Assert result contains "Error".

Use `@pytest.mark.asyncio` on all async tests. Import json for parsing. All call_tool results use `result[0][0].text` access pattern.
  </action>
  <verify>
    <automated>cd I:/ableton-mcp && pytest tests/test_notes.py -x -q</automated>
  </verify>
  <done>9 smoke tests pass covering all 5 note tools: registration, JSON responses, optional params, and error handling. Full test suite (pytest tests/ -x) passes with zero regressions.</done>
</task>

</tasks>

<verification>
- All 5 note MCP tools registered and discoverable
- add_notes_to_clip returns JSON (not f-string)
- tools/__init__.py imports notes module
- conftest.py has notes patch target
- All 9 smoke tests pass
- Full test suite passes: pytest tests/ -x
</verification>

<success_criteria>
- get_notes, remove_notes, quantize_notes, transpose_notes exist as @mcp.tool() functions
- remove_notes has 4 optional range parameters with conditional dict building
- add_notes_to_clip returns json.dumps(result, indent=2) instead of f-string
- tests/test_notes.py has 9 passing tests
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/06-midi-editing/06-02-SUMMARY.md`
</output>
