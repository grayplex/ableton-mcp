# Plan 11-01: LOM Gap Report, Correctness Fixes & Requirements Update

## Goal
Produce a structured gap report organized by LOM class, fix correctness issues found during audit, and update REQUIREMENTS.md with new v2 entries for all "Add" tier gaps.

## Tasks

### Task 1: Create Gap Report
**File:** `.planning/phases/11-check-for-live-object-model-gaps/11-GAP-REPORT.md`

Create structured gap report from RESEARCH.md findings, organized by LOM class with:
- Each gap entry: Add/Backlog tier, LOM property/function, description, AI production value
- Summary statistics (total gaps, Add vs Backlog counts)
- Correctness issues section
- Classes: Song, Track, Clip, ClipSlot, Scene, Device, PluginDevice, SimplerDevice, MixerDevice, GroovePool, CuePoint, DrumPad

### Task 2: Fix Correctness Issues Inline
**Files to modify:**
1. `AbletonMCP_Remote_Script/handlers/notes.py` — Add `probability`, `velocity_deviation`, `release_velocity` as optional fields in `add_notes_to_clip` and expose them in `get_notes`
2. `MCP_Server/tools/notes.py` — Update tool docstrings to document new optional note fields

**NOT fixing:**
- pitch_fine range: Keep current -500..500 per STATE.md decision (needs live testing to confirm)
- quantize: Keep manual implementation (works correctly, native method is optional future enhancement)

### Task 3: Update REQUIREMENTS.md
Add new v2 requirement categories derived from gap analysis:

**New categories:**
- **Arrangement** (ARR-xx): arrangement clips, session→arrangement bridge, play_selection, jump_by
- **Scale & Key** (SCLE-xx): root_note, scale_name, scale_intervals, scale_mode
- **Clip Launch** (CLNC-xx): launch_mode, launch_quantization, legato, velocity_amount, clip muted
- **Note Expression** (NOTE-xx): probability, velocity_deviation, release_velocity, note selection, apply_note_modifications
- **Scene Extended** (SCNX-xx): scene color, scene tempo, scene time sig, fire_as_selected, capture_and_insert_scene
- **Device Extended** (DEVX-xx): insert_device, move_device, plugin presets, A/B compare
- **Simpler** (SMPL-xx): crop, reverse, warp, slicing, playback mode
- **Audio Creation** (ACRT-xx): create_audio_clip (session + arrangement), warp markers
- **Groove** (GRVX-xx): groove pool access, groove settings per clip

Update existing v2 categories where gaps map to existing requirements.

### Task 4: Commit All Changes
Commit gap report, correctness fixes, and requirements update.

## Verification
- [ ] 11-GAP-REPORT.md exists with all LOM classes audited
- [ ] REQUIREMENTS.md has new v2 entries for all Add-tier gaps
- [ ] Correctness fixes pass existing tests (run pytest)
- [ ] All changes committed

## Dependencies
- 11-RESEARCH.md (completed)
- 11-CONTEXT.md (completed)
