---
phase: quick
plan: 260402-p7k
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [DEV-DEPS]
must_haves:
  truths:
    - "pytest can import mcp and run MCP server tests"
    - "pytest-asyncio is installed and async tests execute"
    - "tiktoken is installed and test_genre_quality.py imports succeed"
    - "Full test suite runs without ModuleNotFoundError"
  artifacts: []
  key_links: []
---

<objective>
Install missing dev dependencies (mcp, pytest-asyncio, tiktoken) into the current Python environment so the test suite passes.

Purpose: The pyproject.toml declares these deps but they are not installed. The `[dependency-groups]` section uses PEP 735 format which requires `uv` or pip>=25.x. Since `uv` is available on this system, use it.
Output: Working test environment with all dev deps installed and tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dev dependency group via uv</name>
  <files></files>
  <action>
Use `uv` to install the project with dev dependencies. The pyproject.toml uses PEP 735 `[dependency-groups]` format.

Steps:
1. Run `uv pip install -e .` to install the project and its core dependencies (including `mcp[cli]>=1.3.0`).
2. Run `uv pip install --group dev` or equivalently `uv pip install pytest>=8.3 pytest-asyncio>=0.25 pytest-timeout>=2.0 tiktoken>=0.7 ruff>=0.15.6` to install the dev group packages.
   - If `uv pip install --group dev` is not supported in the installed uv version, fall back to installing the packages explicitly by name.
3. Verify all three key packages are importable: `python -c "import mcp; import pytest_asyncio; import tiktoken; print('OK')"`.
  </action>
  <verify>
    <automated>python -c "import mcp; import pytest_asyncio; import tiktoken; print('All dev deps OK')"</automated>
  </verify>
  <done>mcp, pytest-asyncio, and tiktoken are all importable in the current Python environment.</done>
</task>

<task type="auto">
  <name>Task 2: Run test suite and confirm no import errors</name>
  <files></files>
  <action>
Run the full test suite with pytest to confirm:
1. No ModuleNotFoundError for mcp, pytest-asyncio, or tiktoken.
2. Tests that were previously failing due to missing imports now execute.

Command: `python -m pytest tests/ -x --timeout=10 -q`

If any tests fail for reasons UNRELATED to the dependency fix (pre-existing failures), that is acceptable -- the goal is zero import-related failures.

Note: Some tests may fail because they need a live Ableton connection or other runtime dependencies. That is expected and not a blocker.
  </action>
  <verify>
    <automated>python -m pytest tests/ --timeout=10 -q 2>&1 | head -50</automated>
  </verify>
  <done>Test suite runs without ModuleNotFoundError for mcp, pytest_asyncio, or tiktoken. Import-related failures are resolved.</done>
</task>

</tasks>

<verification>
- `python -c "import mcp; import pytest_asyncio; import tiktoken"` exits 0
- `python -m pytest tests/ -q --timeout=10` shows no ModuleNotFoundError
</verification>

<success_criteria>
All three missing dev dependencies (mcp, pytest-asyncio, tiktoken) are installed and the test suite runs without import errors for these packages.
</success_criteria>

<output>
After completion, create `.planning/quick/260402-p7k-fix-missing-dev-dependencies-mcp-pytest-/260402-p7k-SUMMARY.md`
</output>
