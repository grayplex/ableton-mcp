---
phase: quick
plan: 260402-l9f
type: execute
wave: 1
depends_on: []
files_modified:
  - MCP_Server/tools/scaffold.py
  - MCP_Server/tools/tracks.py
  - MCP_Server/tools/clips.py
  - MCP_Server/tools/devices.py
  - .planning/codebase/CONCERNS.md
autonomous: true
requirements: [BUG-checkpoint-cache]
must_haves:
  truths:
    - "After any write tool call, the next get_checkpoint returns fresh data"
    - "Read-only tool calls do NOT invalidate the cache"
  artifacts:
    - path: "MCP_Server/tools/scaffold.py"
      contains: "invalidate_checkpoint_cache"
    - path: "MCP_Server/tools/tracks.py"
      contains: "invalidate_checkpoint_cache"
    - path: "MCP_Server/tools/clips.py"
      contains: "invalidate_checkpoint_cache"
    - path: "MCP_Server/tools/devices.py"
      contains: "invalidate_checkpoint_cache"
  key_links:
    - from: "MCP_Server/tools/*.py (write functions)"
      to: "MCP_Server/orchestration/checkpoint.invalidate_checkpoint_cache"
      via: "direct function call after successful send_command"
      pattern: "invalidate_checkpoint_cache\\(\\)"
---

<objective>
Fix stale checkpoint cache after write operations.

`invalidate_checkpoint_cache()` exists in `MCP_Server/orchestration/checkpoint.py:268` but is never called by any MCP tool after a write operation. This causes `get_checkpoint` to return stale data for up to 30 seconds after mutations, making Claude re-attempt already-completed steps.

Purpose: Ensure checkpoint always reflects current Ableton state after any tool mutates it.
Output: All four tool files call `invalidate_checkpoint_cache()` after successful write operations. CONCERNS.md updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@MCP_Server/orchestration/checkpoint.py (source of invalidate_checkpoint_cache)
@MCP_Server/orchestration/__init__.py (exports invalidate_checkpoint_cache)
@MCP_Server/tools/scaffold.py
@MCP_Server/tools/tracks.py
@MCP_Server/tools/clips.py
@MCP_Server/tools/devices.py
@.planning/codebase/CONCERNS.md

<interfaces>
From MCP_Server/orchestration/checkpoint.py:268:
```python
def invalidate_checkpoint_cache(genre: str = None):
    """Clear cached checkpoint for a genre, or all if genre is None."""
    if genre is None:
        _checkpoint_cache.clear()
    else:
        _checkpoint_cache.pop(genre, None)
```

From MCP_Server/orchestration/__init__.py:
```python
from .checkpoint import get_checkpoint, invalidate_checkpoint_cache
__all__ = ["AGENDA_CATALOG", "get_agenda", "get_checkpoint", "invalidate_checkpoint_cache", ...]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add invalidate_checkpoint_cache() calls to all write-operation tool functions</name>
  <files>MCP_Server/tools/scaffold.py, MCP_Server/tools/tracks.py, MCP_Server/tools/clips.py, MCP_Server/tools/devices.py</files>
  <action>
Add `from MCP_Server.orchestration.checkpoint import invalidate_checkpoint_cache` to each of the four tool files.

Call `invalidate_checkpoint_cache()` (with no args, clearing ALL genre cache entries) after every successful write operation. Place the call INSIDE the try block, AFTER the successful `send_command` call (or after the last write command in multi-step functions like `scaffold_arrangement`), BEFORE the return statement.

**Pattern for each write function:**
```python
# ... existing code ...
result = ableton.send_command("some_write_command", params)
invalidate_checkpoint_cache()  # <-- add this line
return json.dumps(result, indent=2)
```

**scaffold.py — 1 write function:**
- `scaffold_arrangement`: Add call after line 148 (after building `response` dict, before `return json.dumps(response)`). This function does multiple write commands (locators + tracks), so invalidate once at the end.

**tracks.py — 8 write functions (DO NOT touch the 3 read functions):**
- Write: `create_midi_track`, `create_audio_track`, `create_return_track`, `create_group_track`, `delete_track`, `duplicate_track`, `set_track_name`, `set_track_color`, `set_group_fold`, `stop_track_clips`
- Read (NO change): `get_track_info`, `get_all_tracks`, `get_track_freeze_state`

**clips.py — write functions (DO NOT touch read functions):**
- Write: `create_clip`, `add_notes_to_clip`, `set_clip_name`, `fire_clip`, `stop_clip`, `delete_clip`, `duplicate_clip`, `set_clip_color`, `set_clip_launch_settings`, `set_clip_muted`, `crop_clip`, `duplicate_clip_loop`, `duplicate_clip_region`, `set_clip_groove`, `create_session_audio_clip`, `set_clip_loop`
- Read (NO change): `get_clip_info`, `get_clip_launch_settings`

**devices.py — write functions (DO NOT touch read functions):**
- Write: `load_instrument_or_effect` (only on success path where `result.get("loaded", False)` is True), `set_device_parameter`, `delete_device`, `insert_device`, `move_device`, `crop_simpler`, `reverse_simpler`, `warp_simpler`, `set_simpler_playback_mode`, `insert_simpler_slice`, `move_simpler_slice`, `remove_simpler_slice`, `clear_simpler_slices`, `set_drum_pad_mute`, `set_drum_pad_solo`, `delete_drum_pad_chains`, `set_plugin_preset`, `compare_ab`, `set_wavetable_oscillator`, `set_wavetable_voice_config`, `add_wavetable_modulation`, `set_wavetable_modulation_value`, `set_compressor_sidechain`, `rack_variation_action`, `rack_macro_action`, `insert_rack_chain`, `copy_drum_pad`, `set_drum_chain_config`, `re_enable_parameter_automation`, `set_drift_mod_matrix`, `looper_action`, `looper_export_to_clip_slot`, `set_spectral_resonator_config`, `set_eq8_mode`, `create_take_lane_clip`, `set_tuning_system`, `set_chain_mute_solo`, `set_chain_name_color`
- Read (NO change): `get_device_parameters`, `get_rack_chains`, `get_simpler_info`, `get_wavetable_info`, `get_wavetable_modulation_value`, `get_compressor_sidechain`, `get_rack_variations`, `get_drum_chain_config`, `get_parameter_automation_state`, `get_drift_mod_matrix`, `get_looper_info`, `get_spectral_resonator_info`, `get_eq8_info`, `get_take_lanes`, `get_take_lane_clips`, `get_tuning_system`, `get_chain_info`, `list_plugin_presets`

**Important:** For `load_instrument_or_effect`, only invalidate on the success path (inside `if result.get("loaded", False):` block, before the return).
  </action>
  <verify>
    <automated>cd /home/user/ableton-mcp && python -c "
from MCP_Server.tools import scaffold, tracks, clips, devices
import inspect
for mod_name, mod in [('scaffold', scaffold), ('tracks', tracks), ('clips', clips), ('devices', devices)]:
    src = inspect.getsource(mod)
    assert 'invalidate_checkpoint_cache' in src, f'{mod_name} missing invalidate_checkpoint_cache'
    assert 'from MCP_Server.orchestration.checkpoint import invalidate_checkpoint_cache' in src, f'{mod_name} missing import'
print('All 4 modules import and call invalidate_checkpoint_cache')
# Verify read functions are NOT touched
tracks_src = inspect.getsource(tracks.get_all_tracks)
assert 'invalidate_checkpoint_cache' not in tracks_src, 'get_all_tracks should NOT invalidate'
clips_src = inspect.getsource(clips.get_clip_info)
assert 'invalidate_checkpoint_cache' not in clips_src, 'get_clip_info should NOT invalidate'
print('Read functions correctly untouched')
"</automated>
  </verify>
  <done>All write-operation tool functions in scaffold.py, tracks.py, clips.py, and devices.py call invalidate_checkpoint_cache() after successful mutations. Read-only functions are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Update CONCERNS.md to mark the bug as resolved</name>
  <files>.planning/codebase/CONCERNS.md</files>
  <action>
In `.planning/codebase/CONCERNS.md`, remove the "Checkpoint cache not invalidated after write operations" bug entry (lines 35-38). This bug is now fixed.

Leave all other bugs and concerns intact.
  </action>
  <verify>
    <automated>cd /home/user/ableton-mcp && ! grep -q "Checkpoint cache not invalidated" .planning/codebase/CONCERNS.md && echo "Bug entry removed"</automated>
  </verify>
  <done>CONCERNS.md no longer lists the checkpoint cache invalidation bug.</done>
</task>

</tasks>

<verification>
- `grep -r "invalidate_checkpoint_cache" MCP_Server/tools/` shows calls in all 4 files
- `grep -c "invalidate_checkpoint_cache" MCP_Server/tools/tracks.py` returns 11 (1 import + 10 write functions)
- `grep -c "invalidate_checkpoint_cache" MCP_Server/tools/clips.py` returns 17 (1 import + 16 write functions)
- Read-only functions (get_track_info, get_all_tracks, get_clip_info, get_device_parameters, etc.) do NOT contain `invalidate_checkpoint_cache`
</verification>

<success_criteria>
- Every write-operation MCP tool in scaffold.py, tracks.py, clips.py, and devices.py calls invalidate_checkpoint_cache() after successful mutation
- No read-only tool function calls invalidate_checkpoint_cache()
- CONCERNS.md bug entry removed
- Existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/quick/260402-l9f-fix-checkpoint-cache-not-invalidated-aft/260402-l9f-SUMMARY.md`
</output>
