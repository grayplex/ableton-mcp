---
phase: quick
plan: 260401-qc5
type: execute
wave: 1
depends_on: []
files_modified:
  - MCP_Server/orchestration/checkpoint.py
  - tests/test_checkpoint.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Master short-circuit does NOT fire when session has only master bus template and no instrument tracks or clips"
    - "Master short-circuit still fires when session has actual production work (tracks >= 2, mix phase done)"
  artifacts:
    - path: "MCP_Server/orchestration/checkpoint.py"
      provides: "Guarded master short-circuit"
      contains: "len(tracks) >= 2"
    - path: "tests/test_checkpoint.py"
      provides: "Regression test for false-positive"
      contains: "test_master_shortcircuit_requires_production_work"
  key_links:
    - from: "MCP_Server/orchestration/checkpoint.py"
      to: "MCP_Server/orchestration/phase_detection.py"
      via: "_COMPRESSOR constant import"
      pattern: "_COMPRESSOR in all_device_classes"
---

<objective>
Guard the master-chain short-circuit in `_infer_completed_phases` so it only fires when real production work exists, preventing a pre-loaded master bus template from reporting 100% completion.

Purpose: Fix false-positive where GlueCompressor + Limiter2 on master with zero instrument tracks reports all phases complete.
Output: Patched checkpoint.py + regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@MCP_Server/orchestration/checkpoint.py
@MCP_Server/orchestration/phase_detection.py
@tests/test_checkpoint.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add guard to master short-circuit and regression test</name>
  <files>MCP_Server/orchestration/checkpoint.py, tests/test_checkpoint.py</files>
  <behavior>
    - Test: master devices (GlueCompressor + Limiter2) present but only 1 track and no Compressor2 on any track -> completed_phases should NOT contain all phases; should fall through to normal phase walk
    - Test: existing test_master_complete still passes (2 tracks with clips + master devices -> all phases complete via short-circuit, since the existing test also needs Compressor2 added to a track device list to satisfy the new guard)
  </behavior>
  <action>
1. In `MCP_Server/orchestration/checkpoint.py`, lines 55-58, add two guards to the master short-circuit condition:
   - `len(tracks) >= 2` — proves setup phase is done (tracks exist)
   - `_COMPRESSOR in all_device_classes` — proves mix phase is done (a non-master track has Compressor2)

   Change line 57 from:
   ```python
   if _GLUE_COMPRESSOR in master_class_names and _LIMITER in master_class_names:
   ```
   to:
   ```python
   if (_GLUE_COMPRESSOR in master_class_names and _LIMITER in master_class_names
           and len(tracks) >= 2 and _COMPRESSOR in all_device_classes):
   ```

   `_COMPRESSOR` is already imported from `phase_detection`. `all_device_classes` is computed on lines 50-53, right above.

2. In `tests/test_checkpoint.py`, add a new test `test_master_shortcircuit_requires_production_work` in `TestCheckpoint`:
   - Scenario: 0 instrument tracks (or 1 empty track), master has GlueCompressor + Limiter2, no Compressor2 anywhere
   - Assert: `completed_phases` does NOT equal all phases; active_phase should be "setup" (since len(tracks) < 2)

3. Update the existing `test_master_complete` test fixture to include a `Compressor2` device on one of its tracks, so the short-circuit still triggers. Add `devices=[{"class_name": "Compressor2"}]` to one of the `_make_track` calls in that test.
  </action>
  <verify>
    <automated>cd I:/ableton-mcp && python -m pytest tests/test_checkpoint.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>
    - Master short-circuit only fires when tracks >= 2 AND Compressor2 present on a non-master track
    - New test proves empty session with master template does not report all-complete
    - Existing test_master_complete still passes with updated fixture
    - All other checkpoint tests pass unchanged
  </done>
</task>

</tasks>

<verification>
All tests in test_checkpoint.py pass: `python -m pytest tests/test_checkpoint.py -x -v`
</verification>

<success_criteria>
- `_infer_completed_phases` no longer returns all phases for a session with only a master bus template
- `_infer_completed_phases` still returns all phases when real production work exists (tracks + mix + master)
- Zero test regressions
</success_criteria>

<output>
After completion, create `.planning/quick/260401-qc5-master-phase-short-circuits-phase-walk-w/260401-qc5-SUMMARY.md`
</output>
