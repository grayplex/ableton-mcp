---
phase: quick
plan: 260402-t7c
type: execute
wave: 1
depends_on: []
files_modified:
  - AbletonMCP_Remote_Script/handlers/browser.py
  - .planning/codebase/CONCERNS.md
autonomous: true
must_haves:
  truths:
    - "Browser load verification uses 4-tick delay instead of 1-tick"
    - "Browser load retry uses 4-tick delay instead of 1-tick"
    - "Browser load gets 2 retries instead of 1 before giving up"
    - "CONCERNS.md no longer lists browser timing as a fragile area"
  artifacts:
    - path: "AbletonMCP_Remote_Script/handlers/browser.py"
      provides: "Increased schedule_message delays and retry count"
      contains: "schedule_message(4,"
    - path: ".planning/codebase/CONCERNS.md"
      provides: "Updated concerns without browser timing entry"
  key_links: []
---

<objective>
Increase browser load verification tick delay from 1 to 4 and retry count from 1 to 2, improving reliability under load without meaningful latency impact.

Purpose: Under heavy load (plugin scanning, large sessions), a single Ableton scheduler tick is insufficient for device count to update after `load_item`. Increasing to 4 ticks and adding an extra retry prevents false `{"loaded": False}` results.
Output: Updated browser.py with safer timing, updated CONCERNS.md with fragile area resolved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AbletonMCP_Remote_Script/handlers/browser.py (lines 455-580 — do_load, _verify_load, retry_load)
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Increase schedule_message delays and retry count in browser.py</name>
  <files>AbletonMCP_Remote_Script/handlers/browser.py</files>
  <action>
Three targeted edits in AbletonMCP_Remote_Script/handlers/browser.py:

1. Line ~455: Change `def do_load(retries_remaining=1)` to `def do_load(retries_remaining=2)` — gives 2 retries instead of 1.

2. Line ~462: Change `self.schedule_message(1,` (inside `do_load`, the initial verification delay) to `self.schedule_message(4,` — 4 ticks breathing room for initial load verification.

3. Line ~551: Change `self.schedule_message(1,` (inside `retry_load`, the retry verification delay) to `self.schedule_message(4,` — 4 ticks breathing room for retry verification.

Do NOT change:
- The `self.schedule_message(0, do_load)` on line ~479 (that schedules do_load itself on the main thread, not a delay).
- The `self.schedule_message(0, retry_load)` on line ~565 (same — schedules retry_load on the main thread).
- The `response_queue.get(timeout=30.0)` on line ~484 (outer timeout ceiling stays at 30s).

All three changes are safe — the CONCERNS.md entry explicitly says "do not decrease the schedule delay" and the 30-second outer timeout provides ample headroom.
  </action>
  <verify>
    <automated>cd /home/user/ableton-mcp && grep -n 'schedule_message(4,' AbletonMCP_Remote_Script/handlers/browser.py | wc -l | grep -q '^2$' && grep -n 'retries_remaining=2' AbletonMCP_Remote_Script/handlers/browser.py | wc -l | grep -q '^1$' && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>browser.py has schedule_message(4, ...) at both verification sites and retries_remaining=2 default</done>
</task>

<task type="auto">
  <name>Task 2: Update CONCERNS.md to mark browser timing fragile area as resolved</name>
  <files>.planning/codebase/CONCERNS.md</files>
  <action>
In .planning/codebase/CONCERNS.md, update the "Browser item loading depends on 1-tick schedule_message timing" entry under "## Fragile Areas" to mark it as resolved:

Change the entry to:
```
**Browser item loading depends on 1-tick schedule_message timing:** RESOLVED (260402-t7c) -- Increased schedule_message delay from 1 to 4 ticks at both verification sites (do_load and retry_load) and increased default retries from 1 to 2. The 30-second outer timeout provides ample headroom.
```

This follows the same RESOLVED pattern used for other entries in the file (e.g., the sentinel value resolution entry).
  </action>
  <verify>
    <automated>cd /home/user/ableton-mcp && grep -q 'RESOLVED (260402-t7c)' .planning/codebase/CONCERNS.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>CONCERNS.md fragile area entry marked as resolved with explanation</done>
</task>

</tasks>

<verification>
- `grep -n 'schedule_message(4,' AbletonMCP_Remote_Script/handlers/browser.py` shows exactly 2 matches (lines ~462 and ~551)
- `grep -n 'schedule_message(1,' AbletonMCP_Remote_Script/handlers/browser.py` shows 0 matches in the _verify_load / do_load region
- `grep 'retries_remaining=2' AbletonMCP_Remote_Script/handlers/browser.py` shows 1 match
- `grep 'RESOLVED (260402-t7c)' .planning/codebase/CONCERNS.md` shows 1 match
</verification>

<success_criteria>
- Browser load verification uses 4 ticks instead of 1 at both sites
- Default retry count is 2 instead of 1
- No other timing or retry values changed
- CONCERNS.md entry marked resolved
</success_criteria>

<output>
After completion, create `.planning/quick/260402-t7c-increase-browser-load-verification-tick-/260402-t7c-SUMMARY.md`
</output>
