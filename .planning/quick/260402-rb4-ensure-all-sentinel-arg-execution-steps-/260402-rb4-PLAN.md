---
phase: quick
plan: 260402-rb4
type: execute
wave: 1
depends_on: []
files_modified:
  - MCP_Server/orchestration/execution.py
  - tests/test_phase_execution.py
  - .planning/codebase/CONCERNS.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Every step with a sentinel string in suggested_args has depends_on_step pointing to a query step"
    - "sound_design phase starts with get_arrangement_overview query step"
    - "mix phase starts with get_arrangement_overview query step"
    - "Existing tests still pass, new test enforces the invariant"
  artifacts:
    - path: "MCP_Server/orchestration/execution.py"
      provides: "Fixed _build_sound_design_steps and _build_mix_steps with prepended query steps"
    - path: "tests/test_phase_execution.py"
      provides: "Test asserting all sentinel steps have depends_on_step set"
  key_links:
    - from: "tests/test_phase_execution.py"
      to: "MCP_Server/orchestration/execution.py"
      via: "get_execution_plan() call"
      pattern: "get_execution_plan"
---

<objective>
Ensure every ExecutionStep that uses sentinel strings ("<track_index>", "<clip_index>", "<synth_track_index>", "<*_track_index>") in suggested_args has a non-None depends_on_step pointing to a query step that provides the needed value.

Purpose: Close the "Sentinel value resolution" fragile area in CONCERNS.md. Currently _build_sound_design_steps step 1 and _build_mix_steps first role step have depends_on_step=None while using sentinel args -- Claude has no explicit dependency chain to resolve those values.

Output: Fixed execution.py, new test, updated CONCERNS.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@MCP_Server/orchestration/execution.py
@tests/test_phase_execution.py
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add query steps to sound_design and mix, test sentinel depends_on invariant</name>
  <files>MCP_Server/orchestration/execution.py, tests/test_phase_execution.py</files>
  <behavior>
    - Test: For ALL 9 phase types x house genre, every step whose suggested_args contains a string matching r"^<.*>$" MUST have depends_on_step set to a non-None integer
    - Test: sound_design/house step 1 tool_name == "get_arrangement_overview" (query step)
    - Test: mix/house step 1 tool_name == "get_arrangement_overview" (query step)
    - Test: JSON serialized output still under 2000 chars for all phases (existing test_json_output_under_2000_chars must still pass)
  </behavior>
  <action>
    1. Write a new test `test_sentinel_steps_have_depends_on_step` in TestPhaseExecutionPlan:
       - For each of the 9 phase types (setup, drums, bass, harmony, melody, sound_design, arrangement, mix, master), call get_execution_plan(phase, "house").
       - For each step in the result, scan suggested_args values for strings matching the pattern `<...>` (sentinel pattern).
       - If any sentinel found, assert `"depends_on_step" in step and step["depends_on_step"] is not None`.
       - Also assert `isinstance(step["depends_on_step"], int)`.

    2. Write test `test_sound_design_starts_with_query_step`:
       - result = get_execution_plan("sound_design", "house")
       - assert result["steps"][0]["tool_name"] == "get_arrangement_overview"

    3. Write test `test_mix_starts_with_query_step`:
       - result = get_execution_plan("mix", "house")
       - assert result["steps"][0]["tool_name"] == "get_arrangement_overview"

    4. Run tests -- they MUST fail (RED).

    5. Fix `_build_sound_design_steps` in execution.py:
       - Prepend: `_step(1, "Query tracks to resolve synth track index", "get_arrangement_overview", {}, pt)`
       - Change existing step 1 (load Auto Filter) to step 2 with depends_on_step=1
       - Change step 2 (set param) to step 3 with depends_on_step=2
       - Change step 3 (load Reverb) to step 4 with depends_on_step=3
       - Change step 4 (set param) to step 5 with depends_on_step=4
       - Note: the re-numbering at line 551-552 handles step_number, but depends_on_step references within the builder must use the LOCAL step numbers as they will be re-numbered.
       - Actually: depends_on_step values are re-used as-is (they reference local step numbers within the phase). The renumbering loop at L551 only updates step_number, NOT depends_on_step. So depends_on_step values must match the LOCAL builder numbering. Currently they do (1->2->3->4). After fix: 1(query), 2(depends 1), 3(depends 2), 4(depends 3), 5(depends 4). This is correct.

    6. Fix `_build_mix_steps` in execution.py:
       - Prepend: `_step(1, "Query tracks to resolve role track indices", "get_arrangement_overview", {}, pt)` as the first step in the list.
       - Change the loop to start role steps from step 2: `steps.append(_step(i + 2, ...))` and set depends_on_step for the first role (i==0) to 1 (the query step) instead of None.
       - For subsequent roles (i > 0), depends_on_step should be `i + 1` (the previous role step).
       - Update the trailing steps (check_gain_staging, suggest_mix_adjustments) to account for the +1 offset: `n = len(steps)` still works because steps list now includes the query step.
       - Careful: current code uses `i if i > 0 else None` for depends_on_step. Change to `i + 1 if i > 0 else 1` so first role depends on query step (step 1), second role depends on first role (step 2), etc.

    7. Run tests -- all must pass (GREEN). Verify existing tests (json_output_under_2000_chars, step_numbers_sequential, every_step_has_phase_key) still pass.
  </action>
  <verify>
    <automated>cd /home/user/ableton-mcp && python -m pytest tests/test_phase_execution.py -x -v 2>&1 | tail -40</automated>
  </verify>
  <done>All sentinel steps across all 9 phases have non-None depends_on_step. sound_design and mix phases start with get_arrangement_overview query step. All existing tests pass. JSON output stays under 2000 chars.</done>
</task>

<task type="auto">
  <name>Task 2: Update CONCERNS.md to mark sentinel resolution fragile area as resolved</name>
  <files>.planning/codebase/CONCERNS.md</files>
  <action>
    In `.planning/codebase/CONCERNS.md`:

    1. In the "Fragile Areas" section, update the "Sentinel value resolution depends on Claude understanding description hints" entry. Change it to indicate RESOLVED, similar to how performance concerns are marked. New text:

    **Sentinel value resolution depends on Claude understanding description hints:** RESOLVED (260402-rb4) -- All sentinel steps now have `depends_on_step` pointing to a query step (`get_arrangement_overview`). `_build_sound_design_steps` and `_build_mix_steps` prepend a query step; instrument-phase builders (drums, bass, harmony, melody) chain from `create_midi_track`. Test `test_sentinel_steps_have_depends_on_step` enforces the invariant.

    2. In the "Architectural Risks" section, update the "`get_arrangement_state` track index sentinel resolution requires extra round-trips" entry to note the dependency chain is now explicit. Keep it as a risk note but acknowledge the improvement.

    3. In "Test Coverage Gaps", update "Sentinel resolution by Claude not tested end-to-end" to note the structural invariant test now exists (though true E2E with Claude remains untested).
  </action>
  <verify>
    <automated>grep -c "RESOLVED.*260402-rb4" /home/user/ableton-mcp/.planning/codebase/CONCERNS.md</automated>
  </verify>
  <done>CONCERNS.md sentinel fragile area marked as resolved with quick task ID reference. Related entries in Architectural Risks and Test Coverage Gaps updated.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_phase_execution.py -x -v` -- all tests pass
- `grep "depends_on_step" MCP_Server/orchestration/execution.py` shows depends_on_step usage in sound_design and mix builders
- No sentinel step has depends_on_step=None
</verification>

<success_criteria>
- Every ExecutionStep with a `<...>` sentinel in suggested_args has depends_on_step set to a valid step number
- _build_sound_design_steps and _build_mix_steps prepend get_arrangement_overview query steps
- All existing tests pass (especially json_output_under_2000_chars and step_numbers_sequential)
- New test enforces the sentinel-depends_on invariant for all 9 phase types
- CONCERNS.md fragile area entry marked resolved
</success_criteria>

<output>
After completion, create `.planning/quick/260402-rb4-ensure-all-sentinel-arg-execution-steps-/260402-rb4-SUMMARY.md`
</output>
