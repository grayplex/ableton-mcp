---
phase: quick-260401-p4t
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MCP_Server/orchestration/checkpoint.py
  - MCP_Server/orchestration/next_actions.py
autonomous: true
requirements: [QUICK-P4T]
must_haves:
  truths:
    - "get_checkpoint iterates ALL tracks when fetching arrangement clips, not just the first 8"
    - "get_transition_guidance iterates ALL tracks when fetching arrangement clips, not just the first 8"
  artifacts:
    - path: "MCP_Server/orchestration/checkpoint.py"
      provides: "Clip fetching loop without track cap"
    - path: "MCP_Server/orchestration/next_actions.py"
      provides: "Clip fetching loop without track cap"
  key_links: []
---

<objective>
Remove the hard-coded `tracks[:8]` slice in both `get_checkpoint` (checkpoint.py:158) and `get_transition_guidance` (next_actions.py:223) so that arrangement clips are fetched for ALL tracks in the session, not just the first 8.

Purpose: Sessions with more than 8 tracks currently have incomplete checkpoint/transition data, causing orchestration to miss clips on tracks 9+.
Output: Both functions iterate the full `tracks` list.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@MCP_Server/orchestration/checkpoint.py
@MCP_Server/orchestration/next_actions.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove tracks[:8] cap in checkpoint.py and next_actions.py</name>
  <files>MCP_Server/orchestration/checkpoint.py, MCP_Server/orchestration/next_actions.py</files>
  <action>
In `MCP_Server/orchestration/checkpoint.py` around line 158:
- Change `for track in tracks[:8]:` to `for track in tracks:`
- Update the comment on line 155 from "Fetch clips for up to 8 tracks (D-02)" to "Fetch clips for all tracks"

In `MCP_Server/orchestration/next_actions.py` around line 223:
- Change `for track in tracks[:8]:` to `for track in tracks:`
- Update the comment on line 220 from "Fetch clips for up to 8 tracks" to "Fetch clips for all tracks"

No other changes needed. The rest of the logic already handles arbitrary track counts.
  </action>
  <verify>
    <automated>cd I:/ableton-mcp && python -m pytest tests/test_checkpoint.py tests/test_next_actions.py -x -q</automated>
  </verify>
  <done>Both functions iterate the full tracks list with no [:8] cap. Existing tests pass.</done>
</task>

</tasks>

<verification>
- `grep -n "tracks\[:8\]" MCP_Server/orchestration/checkpoint.py MCP_Server/orchestration/next_actions.py` returns no matches
- All existing tests pass: `python -m pytest tests/test_checkpoint.py tests/test_next_actions.py -x -q`
</verification>

<success_criteria>
- No `tracks[:8]` slice remains in either file
- Existing test suite passes without modification
</success_criteria>

<output>
After completion, create `.planning/quick/260401-p4t-checkpoint-clips-by-track-is-capped-at-8/260401-p4t-SUMMARY.md`
</output>
