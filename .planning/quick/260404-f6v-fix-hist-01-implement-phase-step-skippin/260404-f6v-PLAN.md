---
phase: quick-260404-f6v
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/codebase/CONCERNS.md
autonomous: true
must_haves:
  truths:
    - "HIST-01 technical debt entry is removed from CONCERNS.md"
    - "Architectural risk section no longer claims HIST-01 is unimplemented"
  artifacts:
    - path: ".planning/codebase/CONCERNS.md"
      provides: "Updated concerns with HIST-01 resolved"
  key_links: []
---

<objective>
Remove the stale HIST-01 entry from CONCERNS.md.

The HIST-01 code fix (progress-based step-skipping in get_next_actions) was already implemented in commit cba674f (quick task 260403-hist01). The code at MCP_Server/orchestration/next_actions.py:198-209 correctly skips steps when active_phase_progress >= 0.3, and 10 tests in TestHistStepSkipping pass. The only remaining work is removing the stale CONCERNS.md entries that still claim HIST-01 is deferred.

Purpose: Keep CONCERNS.md accurate by removing resolved technical debt.
Output: Updated CONCERNS.md without HIST-01 references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove resolved HIST-01 entries from CONCERNS.md</name>
  <files>.planning/codebase/CONCERNS.md</files>
  <action>
1. Read .planning/codebase/CONCERNS.md.
2. Remove the entire HIST-01 technical debt block (lines 20-23):
   ```
   **HIST-01 — Phase step-skipping is deferred:**
   - When `active_phase_progress > 0.3`...
   - Impact: Claude receives redundant early steps...
   - Fix approach: Use `active_phase_progress` to offset...
   ```
3. In the Architectural Risks section (line 40), update the sentence that says "HIST-01 (execution log) and REFN-03 (refinement log) are unimplemented" to say "REFN-03 (refinement log) is unimplemented" — since HIST-01's step-skipping is now implemented. Keep the rest of the paragraph as-is.
4. If the Technical Debt section is now empty after removing HIST-01, remove the section header too.
5. Commit with message: "docs: remove resolved HIST-01 entry from CONCERNS.md"
  </action>
  <verify>
    <automated>grep -c "HIST-01" .planning/codebase/CONCERNS.md | grep -q "^0$" && echo "PASS: no HIST-01 references" || echo "FAIL: HIST-01 still present"</automated>
  </verify>
  <done>CONCERNS.md contains zero references to HIST-01. Technical debt section updated. Architectural risk text updated to reflect HIST-01 as resolved.</done>
</task>

</tasks>

<verification>
- `grep "HIST-01" .planning/codebase/CONCERNS.md` returns no matches
- `python -m pytest tests/test_next_actions.py::TestHistStepSkipping -x -q` still passes (no code changes, just docs)
</verification>

<success_criteria>
- CONCERNS.md has no HIST-01 references
- Architectural risk section accurately reflects current state
- Atomic commit created
</success_criteria>

<output>
After completion, create `.planning/quick/260404-f6v-fix-hist-01-implement-phase-step-skippin/260404-f6v-SUMMARY.md`
</output>
