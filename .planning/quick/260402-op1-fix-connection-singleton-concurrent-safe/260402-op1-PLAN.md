---
phase: quick-260402-op1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MCP_Server/connection.py
  - .planning/codebase/CONCERNS.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "connection.py has a module-level docstring explaining the two-level locking model"
    - "Each lock's purpose and scope is documented inline at declaration"
    - "The singleton invariant (all callers share one instance) is explicitly stated"
    - "CONCERNS.md no longer lists the connection singleton concern as open"
  artifacts:
    - path: "MCP_Server/connection.py"
      provides: "Documented locking model with clear invariant comments"
      contains: "Locking Model"
    - path: ".planning/codebase/CONCERNS.md"
      provides: "Concern marked resolved"
      contains: "Resolved"
  key_links: []
---

<objective>
Document and simplify the two-level locking design in connection.py, then mark the CONCERNS.md entry as resolved.

Purpose: The design IS correct but intricate. Clear documentation prevents future maintainers from breaking the invariants that make it safe.
Output: Well-documented connection.py, resolved concern in CONCERNS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@MCP_Server/connection.py
@.planning/codebase/CONCERNS.md
@tests/test_connection_thread_safety.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the two-level locking model in connection.py</name>
  <files>MCP_Server/connection.py</files>
  <action>
Add a module-level docstring section (or expand the existing one-liner at line 1) explaining the locking model. Place it after the imports, before the constants. The docstring should cover:

1. **Module-level docstring** — Add a "Locking Model" section explaining:
   - `_connection_lock` (module-level): Guards creation and liveness validation of the singleton `_ableton_connection`. Held only during `get_ableton_connection()`. Ensures exactly one `AbletonConnection` instance exists.
   - `_send_lock` (per-instance on `AbletonConnection`): Serializes socket write+read cycles on a single connection. Held during `send_command()`. Prevents interleaved messages from concurrent callers.
   - **Key invariant**: `get_ableton_connection()` always returns the SAME `AbletonConnection` instance (singleton). Multiple callers holding references to it are safe because `_send_lock` on that shared instance serializes all I/O. The design would break if callers could somehow get different instances pointing at the same socket.

2. **Inline comments** at key points:
   - At `_connection_lock` declaration (line 192): Comment explaining it guards singleton lifecycle.
   - At `_send_lock` field in `AbletonConnection` (line 96-98): Comment explaining it serializes I/O on this instance.
   - At the top of `get_ableton_connection()`: Brief comment: "Returns the singleton connection. Caller may call send_command concurrently -- _send_lock serializes I/O."
   - At `_healthy` field (line 99): Comment explaining it's a fast-path flag to skip ping round-trips.

3. **Simplification**: The `_healthy` flag already short-circuits the ping inside `_connection_lock`, which means healthy connections spend minimal time holding the module lock. This is already good. No structural changes needed -- the design is correct and already optimized. Just document WHY.

Do NOT change any logic, lock ordering, or control flow. This is documentation-only.
  </action>
  <verify>
    <automated>cd I:/ableton-mcp && python -m pytest tests/test_connection_thread_safety.py -x -q</automated>
  </verify>
  <done>connection.py has clear documentation of the two-level locking model, all existing tests pass unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Mark concern resolved in CONCERNS.md</name>
  <files>.planning/codebase/CONCERNS.md</files>
  <action>
In `.planning/codebase/CONCERNS.md`, update the "Connection singleton is not fully safe under concurrent tool calls" entry under "## Architectural Risks".

Change the entry to indicate it is resolved. Use the pattern already established in the file for resolved items (strikethrough or explicit "Resolved" label). Specifically:

1. Prefix the bold title with "RESOLVED:" so it reads: **RESOLVED: Connection singleton is not fully safe under concurrent tool calls:**
2. Keep the existing description text for historical context.
3. Append a resolution note: "Resolution (260402-op1): The two-level locking model (`_connection_lock` for singleton lifecycle, `_send_lock` for I/O serialization) is correct and now thoroughly documented in `connection.py`. The `_healthy` fast-path minimizes lock contention for the common case."

Read CONCERNS.md first to merge carefully -- do not overwrite other content.
  </action>
  <verify>
    <automated>cd I:/ableton-mcp && grep -c "RESOLVED.*Connection singleton" .planning/codebase/CONCERNS.md</automated>
  </verify>
  <done>CONCERNS.md shows the connection singleton concern as resolved with clear resolution note</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_connection_thread_safety.py -x -q` passes (no logic changes)
- `grep "Locking Model" MCP_Server/connection.py` finds the documentation
- `grep "RESOLVED.*Connection singleton" .planning/codebase/CONCERNS.md` returns 1
</verification>

<success_criteria>
- connection.py contains a clear explanation of the two-level locking model
- All existing thread-safety tests pass without modification
- CONCERNS.md entry is marked resolved
</success_criteria>

<output>
After completion, create `.planning/quick/260402-op1-fix-connection-singleton-concurrent-safe/260402-op1-SUMMARY.md`
</output>
