---
phase: quick
plan: 260401-ox3
type: execute
wave: 1
depends_on: []
files_modified:
  - AbletonMCP_Remote_Script/handlers/scaffold.py
  - tests/test_scaffold.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "get_arrangement_state returns an index field for each track"
    - "index is 0-based and matches the track's position in song.tracks"
    - "All existing tests still pass after the change"
  artifacts:
    - path: "AbletonMCP_Remote_Script/handlers/scaffold.py"
      provides: "index field in track dicts"
      contains: "\"index\""
    - path: "tests/test_scaffold.py"
      provides: "Mock data includes index field"
  key_links:
    - from: "AbletonMCP_Remote_Script/handlers/scaffold.py"
      to: "MCP_Server/orchestration/checkpoint.py"
      via: "send_command('get_arrangement_state') returns tracks with index"
      pattern: "\"index\""
---

<objective>
Add a 0-based `index` field to each track dict returned by the `_get_arrangement_state` RS handler, so downstream consumers (checkpoint, next_actions, execution tools) can identify tracks by position instead of relying solely on name matching.

Purpose: Sessions with duplicate track names (e.g., two tracks named "Pad") cause incorrect phase inference when only names are available.
Output: Updated handler and passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AbletonMCP_Remote_Script/handlers/scaffold.py
@tests/test_scaffold.py
@MCP_Server/tools/scaffold.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add index field to _get_arrangement_state and update all test mocks</name>
  <files>AbletonMCP_Remote_Script/handlers/scaffold.py, tests/test_scaffold.py</files>
  <behavior>
    - Test: _mock_overview_factory tracks include "index" key with correct 0-based values
    - Test: get_arrangement_overview still returns flat track name list (index not leaked to overview output)
    - Test: existing TestArrangementOverview tests pass unchanged in behavior
  </behavior>
  <action>
    1. In AbletonMCP_Remote_Script/handlers/scaffold.py, method `_get_arrangement_state` (line 67-69): change the track list comprehension to use `enumerate` and add `"index": i` to each track dict:
       ```python
       for i, track in enumerate(self._song.tracks):
           tracks.append({
               "index": i,
               "name": track.name,
               "has_devices": len(track.devices) > 0,
           })
       ```

    2. In tests/test_scaffold.py, update `_mock_overview_factory` default tracks (around line 274-278) to include index:
       ```python
       tracks = [
           {"index": 0, "name": "kick", "has_devices": True},
           {"index": 1, "name": "bass", "has_devices": True},
           {"index": 2, "name": "lead", "has_devices": True},
       ]
       ```

    3. Also update any other test that constructs track dicts for `get_arrangement_state` mocks to include the `index` field, ensuring consistency. Search test files for `"has_devices"` to find all instances.

    4. Do NOT change `get_arrangement_overview` in MCP_Server/tools/scaffold.py — it intentionally returns only track names (line 186). The index is available to consumers that call `get_arrangement_state` directly (checkpoint.py, next_actions.py, execution.py) but the overview tool keeps its compact format.
  </action>
  <verify>
    <automated>cd I:/ableton-mcp && python -m pytest tests/test_scaffold.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>
    - _get_arrangement_state returns tracks with {index, name, has_devices}
    - index is 0-based integer matching enumerate position
    - All tests in test_scaffold.py pass
    - get_arrangement_overview still returns flat name list (no index in output)
  </done>
</task>

</tasks>

<verification>
python -m pytest tests/test_scaffold.py tests/test_checkpoint.py tests/test_next_actions.py tests/test_execution.py -x -v
</verification>

<success_criteria>
- _get_arrangement_state track dicts contain "index" field
- All existing tests pass (no regressions)
- get_arrangement_overview output unchanged
</success_criteria>

<output>
After completion, create `.planning/quick/260401-ox3-get-arrangement-state-omits-track-index-/260401-ox3-SUMMARY.md`
</output>
