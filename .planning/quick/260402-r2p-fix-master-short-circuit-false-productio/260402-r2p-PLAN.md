---
phase: quick
plan: 260402-r2p
type: execute
wave: 1
depends_on: []
files_modified:
  - MCP_Server/orchestration/checkpoint.py
  - tests/test_checkpoint.py
  - .planning/codebase/CONCERNS.md
  - .planning/STATE.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "A session with master bus loaded + 2 bare scaffold tracks (one with Compressor2) does NOT report production complete"
    - "A genuinely complete production still reports all phases complete via the sequential walk"
    - "The master short-circuit block (lines 55-59) no longer exists in checkpoint.py"
  artifacts:
    - path: "MCP_Server/orchestration/checkpoint.py"
      provides: "_infer_completed_phases without short-circuit"
      contains: "# If master chain is complete"
      absent: true
    - path: "tests/test_checkpoint.py"
      provides: "Regression test for scaffold false-positive"
  key_links:
    - from: "tests/test_checkpoint.py"
      to: "MCP_Server/orchestration/checkpoint.py"
      via: "_infer_completed_phases sequential walk"
      pattern: "completed_phases.*!=.*all_phases"
---

<objective>
Remove the master short-circuit block (lines 55-59) from `_infer_completed_phases` in checkpoint.py. The block prematurely returns all phases as complete when master has GlueCompressor + Limiter2, tracks >= 2, and any track has Compressor2 -- but it skips per-phase checks (drums, bass, harmony, melody, etc.), allowing bare scaffold sessions to report 100% completion.

The sequential phase walk (lines 61-116) already handles the master phase correctly at lines 105-107, and the `break` on first incomplete phase (line 114) means a genuinely finished production naturally reports all phases complete.

Purpose: Eliminate false "production complete" for sessions with pre-loaded master bus but no actual content.
Output: Corrected checkpoint.py, regression test, updated CONCERNS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@MCP_Server/orchestration/checkpoint.py
@tests/test_checkpoint.py
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add regression test and remove short-circuit block</name>
  <files>tests/test_checkpoint.py, MCP_Server/orchestration/checkpoint.py</files>
  <behavior>
    - Test: A session with 2 scaffold tracks (one has Compressor2 device class, neither has clips or instruments matching phase names), master bus with GlueCompressor + Limiter2, should NOT report all phases complete. Expected: completed_phases is ["setup"] only (setup passes because len(tracks) >= 2, but drums fails because no drum-named track with instrument + clips).
    - Test: Existing test_master_complete still passes (genuine production with real tracks reports master complete via sequential walk).
  </behavior>
  <action>
RED phase:
1. In `tests/test_checkpoint.py`, add `test_scaffold_with_master_bus_not_complete` to class `TestCheckpoint`:
   - Create 2 tracks: `_make_track("Track 1", False, 0, device_classes=["Compressor2"])` and `_make_track("Track 2", False, 1)` -- both non-instrument, no clips, generic names
   - Master device_classes: `["GlueCompressor", "Limiter2"]`
   - Empty clips_by_track, one cue point, song_length > 0
   - Assert `result["completed_phases"] == ["setup"]` -- only setup passes (2 tracks)
   - Assert `result["active_phase"] == "drums"` -- sequential walk stops at drums
   - This test MUST FAIL before the fix (the short-circuit returns all phases)
2. Run `python -m pytest tests/test_checkpoint.py::TestCheckpoint::test_scaffold_with_master_bus_not_complete -x` -- confirm RED

GREEN phase:
3. In `MCP_Server/orchestration/checkpoint.py`, delete lines 55-59 (the entire short-circuit block):
   ```python
   # If master chain is complete (GlueCompressor + Limiter2 present), all phases done.
   master_class_names = set(master_devices)
   if (_GLUE_COMPRESSOR in master_class_names and _LIMITER in master_class_names
           and len(tracks) >= 2 and _COMPRESSOR in all_device_classes):
       return list(phase_order)
   ```
   Keep the blank line and the `completed = []` line that follows.
4. Run full test suite: `python -m pytest tests/test_checkpoint.py -x` -- ALL tests must pass including existing test_master_complete (which exercises the sequential walk's master check at lines 105-107).

Also update `test_master_shortcircuit_requires_production_work` (line 163) -- this test was written to guard the short-circuit. Now that the short-circuit is gone, rename it to `test_master_template_only_not_complete` and simplify: the assertion that `completed_phases != all_phases` is still valid, but add a comment noting the short-circuit was removed entirely.
  </action>
  <verify>
    <automated>cd /home/user/ableton-mcp && python -m pytest tests/test_checkpoint.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>Short-circuit block removed. New regression test passes. All existing checkpoint tests pass. Sequential walk handles all cases.</done>
</task>

<task type="auto">
  <name>Task 2: Update CONCERNS.md and STATE.md</name>
  <files>.planning/codebase/CONCERNS.md, .planning/STATE.md</files>
  <action>
1. In `.planning/codebase/CONCERNS.md`, under "## Fragile Areas", remove the entry "**Master short-circuit can produce false 'production complete':**" (lines 58-60) entirely. The short-circuit no longer exists.

2. In `.planning/STATE.md`, under "### Decisions", update the entry:
   - OLD: `[v1.9]: master phase short-circuits phase-walk — GlueCompressor+Limiter2 on master → all phases complete (avoids requiring sound_design devices)`
   - NEW: `[v1.9]: master phase short-circuit removed — sequential walk handles master detection at phase_type=="master" check; no premature all-complete bypass`
  </action>
  <verify>
    <automated>cd /home/user/ableton-mcp && grep -c "short-circuit can produce false" .planning/codebase/CONCERNS.md && echo "FAIL: entry still present" || echo "PASS: entry removed"</automated>
  </verify>
  <done>CONCERNS.md no longer lists the master short-circuit as a fragile area. STATE.md decision updated to reflect removal.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_checkpoint.py -x -v` -- all tests pass
2. `grep -n "all phases done" MCP_Server/orchestration/checkpoint.py` -- no matches (short-circuit comment gone)
3. `grep "short-circuit" .planning/codebase/CONCERNS.md` -- no matches in Fragile Areas section
</verification>

<success_criteria>
- The master short-circuit block is completely removed from _infer_completed_phases
- A scaffold session (2 generic tracks + master devices + Compressor2) reports only ["setup"] complete
- A genuine full production still reports all phases complete via the sequential walk
- All existing checkpoint tests pass without modification (except the renamed guard test)
- CONCERNS.md and STATE.md updated to reflect the fix
</success_criteria>

<output>
After completion, create `.planning/quick/260402-r2p-fix-master-short-circuit-false-productio/260402-r2p-SUMMARY.md`
</output>
