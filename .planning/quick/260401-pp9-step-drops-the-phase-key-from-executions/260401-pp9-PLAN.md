---
phase: quick
plan: 260401-pp9
type: execute
wave: 1
depends_on: []
files_modified:
  - MCP_Server/orchestration/execution.py
  - tests/test_phase_execution.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "_step() returns a dict that includes the 'phase' key"
    - "All ExecutionStep dicts conform to the ExecutionStep TypedDict schema"
  artifacts:
    - path: "MCP_Server/orchestration/execution.py"
      provides: "_step helper returning schema-conformant dicts"
      contains: "\"phase\": phase"
    - path: "tests/test_phase_execution.py"
      provides: "Test asserting phase key presence"
      contains: "phase"
  key_links:
    - from: "MCP_Server/orchestration/execution.py"
      to: "MCP_Server/orchestration/schema.py"
      via: "ExecutionStep TypedDict conformance"
      pattern: "phase"
---

<objective>
Include the `phase` key in dicts returned by `_step()` in execution.py so they conform to the `ExecutionStep` TypedDict declared in schema.py.

Purpose: Fix schema non-conformance that would cause a KeyError for any consumer accessing `step["phase"]`.
Output: Patched `_step()`, new test assertion, all tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@MCP_Server/orchestration/execution.py
@MCP_Server/orchestration/schema.py
@tests/test_phase_execution.py
</context>

<tasks>

<task type="auto">
  <name>Task 0: Rebase onto misc-fixes</name>
  <files>n/a</files>
  <action>
    Run `git fetch origin misc-fixes && git rebase origin/misc-fixes` to incorporate any parallel changes before applying the fix.
    If conflicts arise, resolve them (the change is small and unlikely to conflict).
  </action>
  <verify>
    <automated>git log --oneline -3</automated>
  </verify>
  <done>Branch is rebased on latest misc-fixes</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Add phase key to _step() and test it</name>
  <files>MCP_Server/orchestration/execution.py, tests/test_phase_execution.py</files>
  <behavior>
    - Every step dict returned by get_next_actions (for any phase) must contain a "phase" key
    - The "phase" value must be a non-empty string
  </behavior>
  <action>
    1. In tests/test_phase_execution.py, add a new test method (e.g. `test_every_step_has_phase_key`) that calls `get_next_actions` for each genre/phase combo and asserts that every step dict in `result["steps"]` contains a `"phase"` key with a non-empty string value. Run the test — it MUST fail (RED).

    2. In MCP_Server/orchestration/execution.py, line ~121-129, add `"phase": phase` to the dict literal in `_step()`. Remove the comment about omitting phase. The function already accepts `phase` as a parameter — it just discards it.

    Specifically change the dict from:
    ```python
    d = {
        "step_number": step_number,
        "tool_name": tool_name,
        "description": description,
        "suggested_args": suggested_args,
    }
    ```
    to:
    ```python
    d = {
        "step_number": step_number,
        "tool_name": tool_name,
        "description": description,
        "suggested_args": suggested_args,
        "phase": phase,
    }
    ```

    3. Run tests again — MUST pass (GREEN).
  </action>
  <verify>
    <automated>cd I:/ableton-mcp && python -m pytest tests/test_phase_execution.py -x -q</automated>
  </verify>
  <done>All steps returned by _step() include "phase" key; test proves it; all existing tests still pass</done>
</task>

</tasks>

<verification>
python -m pytest tests/ -x -q
</verification>

<success_criteria>
- _step() includes "phase" in every returned dict
- New test asserts phase key presence across all phases
- Full test suite passes
- Branch rebased on latest misc-fixes
</success_criteria>

<output>
After completion, create `.planning/quick/260401-pp9-step-drops-the-phase-key-from-executions/260401-pp9-SUMMARY.md`
</output>
