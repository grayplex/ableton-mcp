---
phase: quick
plan: 260401-po3
type: execute
wave: 1
depends_on: []
files_modified:
  - MCP_Server/orchestration/checkpoint.py
  - MCP_Server/orchestration/next_actions.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "get_ableton_connection is called once per invocation of get_checkpoint"
    - "get_ableton_connection is called once per invocation of get_transition_guidance"
    - "conn2 variable is removed from both files"
    - "All send_command calls in the clip loop use conn, not conn2"
  artifacts:
    - path: "MCP_Server/orchestration/checkpoint.py"
      provides: "Deduped connection usage in get_checkpoint"
    - path: "MCP_Server/orchestration/next_actions.py"
      provides: "Deduped connection usage in get_transition_guidance"
  key_links:
    - from: "checkpoint.py clip loop"
      to: "conn (first call)"
      via: "conn.send_command used throughout"
    - from: "next_actions.py clip loop"
      to: "conn (first call)"
      via: "conn.send_command used throughout"
---

<objective>
Remove redundant get_ableton_connection() calls in checkpoint.py and next_actions.py. Both functions call get_ableton_connection() twice: once for arrangement/mix state, then again via conn2 for the per-track clip loop. Since get_ableton_connection() returns the same global singleton, the second call is unnecessary and triggers an extra liveness ping under the mutex.

Purpose: Eliminate the unnecessary second mutex acquisition and liveness ping per call.
Output: Both files use a single conn variable throughout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebase onto misc-fixes</name>
  <files></files>
  <action>
    Before making any code changes, rebase the current worktree onto the latest misc-fixes branch to avoid conflicts with parallel agents:

    ```
    git fetch origin misc-fixes
    git rebase origin/misc-fixes
    ```

    If there are conflicts, resolve them before proceeding.
  </action>
  <verify>
    <automated>git log --oneline -5</automated>
  </verify>
  <done>Worktree is rebased onto origin/misc-fixes with no conflicts.</done>
</task>

<task type="auto">
  <name>Task 2: Remove conn2 from checkpoint.py and next_actions.py</name>
  <files>MCP_Server/orchestration/checkpoint.py, MCP_Server/orchestration/next_actions.py</files>
  <action>
    In MCP_Server/orchestration/checkpoint.py (~line 157):
    - Delete the line: `conn2 = get_ableton_connection()`
    - Change `conn2.send_command(` to `conn.send_command(` in the clip loop that follows (line ~160)

    In MCP_Server/orchestration/next_actions.py (~line 222):
    - Delete the line: `conn2 = get_ableton_connection()`
    - Change `conn2.send_command(` to `conn.send_command(` in the clip loop that follows (line ~225)

    Both clips loops already have their own try/except blocks, so removing conn2 does not affect error handling. The conn variable is already in scope from the earlier try block; no scope changes are needed.
  </action>
  <verify>
    <automated>grep -n "conn2" MCP_Server/orchestration/checkpoint.py MCP_Server/orchestration/next_actions.py</automated>
  </verify>
  <done>grep returns no matches — conn2 is fully removed from both files. conn.send_command is used throughout both clip loops.</done>
</task>

</tasks>

<verification>
1. `grep -n "conn2" MCP_Server/orchestration/checkpoint.py MCP_Server/orchestration/next_actions.py` returns no output.
2. `grep -n "conn.send_command" MCP_Server/orchestration/checkpoint.py` shows send_command calls in both the initial block and the clip loop.
3. `grep -n "conn.send_command" MCP_Server/orchestration/next_actions.py` shows send_command calls in both the initial block and the clip loop.
4. If a test suite is available: run it to confirm no regressions.
</verification>

<success_criteria>
Both checkpoint.py and next_actions.py call get_ableton_connection() exactly once per function invocation. The clip loops in both functions use conn.send_command() instead of conn2.send_command(). No conn2 variable exists in either file.
</success_criteria>

<output>
After completion, create `.planning/quick/260401-po3-deduplicate-get-ableton-connection-calls/260401-po3-SUMMARY.md` with what was changed, file paths, and commit hash.
</output>
